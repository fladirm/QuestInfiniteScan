# Σ‑PRISM‑16

## One native S16 medium, one generated Merkaba relation program, exact pullback and pure readouts

Canonical product baseline:

```text
CPQ4-2026-08-25-S16-v8.3
```

Status: canonical reconstruction and product specification.

This revision removes the last scanner-specific ontology. It does not replace it
with charts, branches, hypotheses, latent objects or a topology system carrying
native names. There is one native field, one exact relation-program authority and
different forward or reverse evaluations of that same authority. It also makes
the representation of locally varying sampling explicit: a revisioned exact
coordinate map may change the finite backing representative, but it never becomes
a second physical field.

---

# 0. Authority and replacement rule

This file is the sole normative reconstruction/product specification.

The runtime at `cac9ab012f4ce574e5eb9bee88290982fd9c4fe8` is forensic evidence and
an implementation donor only where this specification explicitly retains it. It
does not define physics.

The following earlier formulations are superseded:

```text
classical scanner stages renamed as native stages
an atlas of physical sheet/chart objects
physical ABSENT separate from the native field
one unary E22 bundle assumed to be the complete law
long-lived ShadowHypothesis / UnresolvedShadowBranch / BoundNativeBranch objects
inverse followed by stitch/topology as separate semantic operations
per-query kernel preservation assumed without a complete coupling proof
refinement as physical germ/chart lifecycle
```

Any prose, test or code conflicting with this file is wrong.

---

# 1. The sole canonical physical world

The reconstructed world is exactly

\[
\boxed{\Psi:\Sigma_2\rightarrow S16}.
\]

`Σ₂` is one logically unbounded intrinsic two-dimensional carrier/gauge.
`Ψ(ξ)` is one immutable sixteen-coefficient exact S16 algebra value at intrinsic
carrier locality `ξ`.

There is no additional canonical:

```text
XYZ world
surface/sheet/chart object world
topology graph
texture/material world
scene-history world
candidate/hypothesis world
mesh/splat/meshlet world
```

Disconnected surfaces, folds, two-sided sheets, thin front/back manifestations,
rooms and floors are patterns and relations of the same `Ψ`. They are not domain
objects stored beside it.

The carrier does not know the words wall, sheet, object, fold or floor.

The physical field is defined modulo exact harmless representation-gauge
reparameterization:

\[
\boxed{\text{physical state}=[\Psi]_{gauge}.}
\]

This does not make all address permutations harmless. A reparameterization is
gauge only when the complete generated program proves equality of every physical
query and native relation. The runtime stores and publishes one deterministic
normal finite representative of that equivalence class.

---

# 2. `Σ₂`, exact representation gauge and local sampling density

Do not conflate the intrinsic locality with its current finite backing address.
For one published representation revision `r`, let

\[
a=(i,j)\in A_r\subset\mathbb Z^2
\]

be an exact signed-64 backing address and let

\[
\boxed{\chi_r:A_r\rightarrow\Sigma_2}
\]

be the fingerprinted exact coordinate-gauge map. The stored samples are

\[
\widehat\Psi_r(a)=\Psi(\chi_r(a)).
\]

`chi` alone is not a complete sampling representation. It locates the native
sample degrees of freedom, but a locally non-uniform representation must also
define their exact intrinsic support, measure and reconstruction action so that
adding backing samples does not duplicate manifestation or leave values between
sample sites undefined. Let

\[
\kappa_r
\]

denote this fingerprinted exact representation plan. It is generated with the
admitted `chi` map family and may be implicit when that family uniquely determines
it. Every backing coefficient remains one full S16 value; `kappa` is not a set of
semantic channels or a physical interpolation field.

The finite representative is

\[
\boxed{
\mathcal R_r=(\chi_r,\kappa_r,\widehat\Psi_r,\mathcal W_r)
}
\]

where `W_r` contains only exact representation/equivalence witnesses required to
interpret or verify it. The logical field represented by it is written

\[
\operatorname{DecodeField}(\mathcal R_r).
\]

For a nodal map family the displayed equation
`Psi-hat(a)=Psi(chi(a))` is literal. If the authoritative law requires another
exact local reconstruction convention, N1R must generate it in `kappa`; storage
code may not invent interpolation, sample weights or footprint ownership. Two
representatives are gauge-equivalent only when their decoded fields are
the same full S16 field under an admissible exact carrier reparameterization, not
merely when their sample locations or current readouts look similar.

Normatively,

\[
\boxed{
\mathcal R_a\sim_g\mathcal R_b
\Longleftrightarrow
\exists\gamma\in G_{admissible}:\quad
\operatorname{DecodeField}(\mathcal R_b)(\gamma(\xi))
=
\operatorname{DecodeField}(\mathcal R_a)(\xi)
\quad\forall\xi\in\Sigma_2
}
\]

coefficient-for-coefficient in S16, including `ZEmpty`, together with exact
transport of every generated intrinsic relation and proof factor. `gamma` must be
a fingerprinted bijection from the generated admissible gauge family; a lossy
resampling, many-to-one map or hidden-mode projection is never gauge.

This pointwise full-S16 condition must imply, as a separate generated covariance/
faithfulness proof,

\[
\forall q\in Q_{physical}:\quad
\mathfrak M_q[\operatorname{DecodeField}(\mathcal R_a)]
=
\mathfrak M_q[\operatorname{DecodeField}(\mathcal R_b)].
\]

`Q_physical` is the full frozen admissible query family, not only views observed
so far. Readout equality alone is necessary but never sufficient to declare
gauge; otherwise an unobserved S16 direction could be deleted by representation.

The current uniform signed-64 `(u,v)` layout is one donor representative in which
`χ` is implicit. It is not a proof that every admissible locally refined
representative has uniform density.

`chi` and `kappa` are representation metadata, not another physical field, chart
database, surface identity or deformation world. They may be stored per exact
backing region or through an equivalent generated normal form. N1R must freeze a
finite exact map/reconstruction family and its arithmetic. An exact dyadic affine/Jacobian
lowering is a preferred Quest candidate, including anisotropic dyadic density
where the authoritative program proves it, but no dyadic formula is normative
until its complete-program representation theorem passes. Floating metric tensors
may not decide canonical mutation.

Where an exact local inverse exists, the Riemannian-looking quantity

\[
g_\chi=(D\chi^{-1})^T(D\chi^{-1})
\]

is only a derived description of representational sampling density. It is not a
new physical tensor beside `Ψ`, and a general floating `g_ξ` is not canonical ABI.
For an exact nonsingular local map with Jacobian `J_chi`, the corresponding
explanatory density is

\[
\rho_\chi=\sqrt{\det g_\chi}=|\det J_\chi|^{-1}.
\]

Canonical code uses the exact map/reconstruction record, not a floating
determinant. Singular maps, overlapping sample ownership or a change in total
query weight are illegal unless `kappa` and the complete-program equivalence proof
explicitly make them representation-neutral.

The useful coastline analogy is precise only at this level: more backing degrees
of freedom are admitted where retained observations prove more native variation,
just as a smaller ruler resolves more of the same coastline. The coastline and
the S16 medium are not recreated.

Exact intrinsic neighbourhoods induced by `chi_r/kappa_r` are the primary sparse
relation domain. Backing adjacency is usable only when the map proves that it
represents the required intrinsic neighbourhood. Page/sample adjacency alone
never defines physics.

They are not an ontological assertion that every native relation is restricted to
one backing `(i±1,j)` or `(i,j±1)` stencil. Where the authoritative program proves
that a gauge seam, closed manifestation or higher-arity native context requires
another carrier support tuple, it may generate that tuple directly from `Ψ`, the
query, the published representation and the fingerprinted relation law. Such
tuples and generation-keyed caches are disposable evaluation work. They never
become a canonical seam table, chart
incidence database or topology graph, and they may not be inferred from XYZ,
pixels, pages or proximity.

Storage pages do not add, remove or identify relations.

Supported manifestations may occupy disjoint carrier regions separated by the
native no-manifestation state. A fold is an S16 relation across intrinsic
neighbours. Two physically close sheets may occupy distant carrier regions. Two
carrier neighbours may manifest as a sharp fold.

Gauge allocation chooses a backing representative only after the exact native law
proves a non-default supported update. It may choose only among embeddings proven
gauge-equivalent under the complete generated program, not merely placements
indistinguishable to the current camera. Pixel order, observation revision,
provenance, XYZ, page and segment identity may never decide physical placement.

Before publication, every gauge-equivalent result is passed through a generated
constructive normalizer

\[
\boxed{
N_g(\chi,\kappa,\widehat\Psi,\mathcal W)
}
\]

where `W` denotes only the exact relation/representation witnesses required to
prove equivalence. The normal form must be independent of observation order,
allocation order, dispatch order and residency. Equivalent placements therefore
produce one byte-identical canonical representative. If the authoritative law
cannot construct a unique normal form for surviving placements, support remains
unresolved and no allocator choice may manufacture determinism.

Gauge allocation is not object creation.

---

# 3. Native no-manifestation and sparse backing

The native law shall define and fingerprint a canonical no-manifestation value or
harmless-equivalence class. The selected mathematical representative is denoted

\[
\boxed{z_\varnothing\in S16}.
\]

Its code-level generated symbol is exactly `ZEmpty`. No second spelling denotes a
different state.

Do not confuse two different operations:

1. changing a logical field value from supported `s` to `z_\varnothing`; and
2. changing only how an already-`z_\varnothing` logical value is backed.

Operation 1 is a physical field mutation. It may reveal a farther manifestation
and therefore change first-hit, depth, optical output and native relations. It is
legal only through `NativeCloseCommit`.

Operation 2 is representation-only and must satisfy, for every query and every
intrinsic context,

\[
\boxed{
\mathfrak M_q[\operatorname{Decode}_{unbacked}(\Psi)]
=
\mathfrak M_q[\operatorname{Decode}_{explicit\;z_\varnothing}(\Psi)]
}.
\]

Both terms denote the same logical field; only backing differs.

For intrinsic-relation queries, the program must define the exact interaction of
`z_\varnothing` with itself and with supported values. A default/support boundary
may produce a native null/contact relation; it is not required to be algebraically
silent. What is required is that an unbacked address behave exactly like an
allocated `ZEmpty` address in every such context. Geometry readout alone is
insufficient.

The logically unbounded default field must also be exactly quiescent. For every
generated relation arity and query family, an all-default relevant neighbourhood
must evaluate to the reducer identity and a generated `DEFAULT_SAT` relation:

\[
\boxed{
\mathfrak M_{rel}(z_\varnothing,\ldots,z_\varnothing)
=\mathrm{DEFAULT\_SAT}.
}
\]

It emits no active manifestation, reverse-support candidate, closure obligation or
GPU work. This does not silence a mixed `(z_\varnothing,s_{supported},\ldots)`
boundary.

Until this full-program gate passes, the current generated `ZNullDyad` is only a
candidate donor. The facts that current source proves `G z_null = 0`, initializes
GPU slots with it and encodes it as a zero-payload `NULL` block do not prove the
required complete native semantics.

After the gate passes:

```text
unbacked logical carrier region
allocated sample equal to ZEmpty
decoded NULL codec sample
```

are three storage representations of the same canonical S16 field value. Backing
absence is not a physical state and is never exposed to algebra, scan or readout.

If the complete provenance-bearing program cannot derive and prove such a
representative/equivalence, N1R is blocked. The runtime may not silently invent a
physical support bit beside S16, and query-boundary code may not redefine native
default semantics on its own.

---

# 4. Exact native algebra

Each value is

\[
s=\sum_{k=0}^{15}s_ke_k\in S16.
\]

The coefficients are not semantic property lanes. Position, appearance, detail,
support and relation class arise only through generated operations involving the
complete value.

Canonical numeric semantics are:

```text
num.fixed.q16_48.checked.nearest_even
```

- signed 16.48 stored in 64 bits;
- checked overflow;
- nearest-even point arithmetic;
- outward-rounded interval arithmetic;
- exact signed-XOR Cayley–Dickson multiplication tables;
- explicit conjugation;
- explicit product brackets;
- no floating-point canonical mutation.

Dense multiplication is a semantic oracle or proven fallback, not the default hot
path. Generated sparse actions, permutations and common subexpressions are used
after exact parity.

Nonassociativity is never optimized away:

\[
(ab)c\neq a(bc)
\]

may be physical information because the authoritative program owns the bracket
tree.

---

# 5. One generated Merkaba relation program

There is one semantic authority:

\[
\boxed{
\mathfrak M=
\operatorname{Compile}
(\mathcal A_{S16},I_{TOE},I_Q)
}.
\]

`I_TOE` is the supplied authoritative Merkaba/eigenmode operator/frame/kernel
artifact. `I_Q` is the frozen family of query-boundary laws and schemas for the
physical sensor transfer/finite footprint, XR eyes, intrinsic queries, export and
debug. Runtime calibration/pose epochs instantiate parameters of those schemas;
they do not rewrite native law.

`I_Q` is not a second world or a second physical solver. It specifies how an
instrument/readout couples to the native law. Every generated expression records
whether each node originates in exact S16 algebra, authoritative TOE law or a
query-boundary descriptor, and all provenance participates in the one bundle
fingerprint.

The sensor part of `I_Q` includes an exact photometric nuisance law. For one eye,
the optical observation is constrained by a relation of the form

\[
RGB_{obs}
\in
\mathfrak M_{optical}
(\Psi,q,L_t,E_t),
\]

where `L_t` is the admitted illumination region and `E_t` is the sensor exposure,
gain, transfer and white-balance region. These are immutable query/evidence
nuisance parameters, not canonical appearance fields and not a second texture
truth. They may widen only according to a frozen calibrated uncertainty law; they
may not become an unrestricted explanation for arbitrary RGB disagreement.

If the capture API supplies exposure/gain/transfer metadata, it is retained with
the observation. If it does not, a conservative derived photometric region with
explicit provenance is mandatory. Missing metadata may reduce optical information
but may not turn recency or a heuristic quality scalar into authority. This is the
mechanism by which dim, blurred or illumination-shifted RGB can remain broad while
valid depth still contracts geometry and previously supported appearance survives.

`𝔐` is the resulting fingerprinted generated exact relation-program IR.

It contains expression graphs with:

```text
explicit input arity and carrier neighbourhood
exact signed-XOR actions and S16 products
every bracket tree
ZD/annihilator relations
eigenmode/shadow coupling supplied by TOE
query parameters and contractions
whole-query reduction semantics
exact reverse contractors
canonical default-state proof
```

It is one authority, not one monolithic GPU kernel. The compiler may emit many
query-specialized immutable operator plans and physical kernels. All such plans
must be generated from the same IR and share one bundle fingerprint.

The existing `SigmaOperatorPlan` vocabulary, fingerprints, bracket descriptors and
CPU/HLSL parity machinery are implementation donors. The current collection of
independently authored geometry/RGB/topology formulas is not the final authority.

---

# 6. Optional E22 relation inventory

If the TOE artifact supplies exactly 22 named relations, they may be retained as a
generated inventory inside `𝔐`.

They are not 22 physical channels and are not assumed unary. Every relation keeps
its supplied arity, domain, bracket tree and coupling.

Let a supplied inventory contain relations

\[
E_k:S16^{a_k}\times C_k\rightarrow V_k,
\]

where `a_k`, operand positions, admissible context `C_k` and bracket tree are part
of the descriptor. Exclusive factorization of the complete native law through
this inventory is legal only if the family is contextually separating on the
canonical admissible domain:

\[
\boxed{
\left[
\forall k,p,c:\quad
E_k(c[s_a]_p)=E_k(c[s_b]_p)
\right]
\Longrightarrow
s_a\equiv s_b
}.
\]

Here `p` ranges every operand position in which the state may participate, `c`
ranges all admissible generated native/query contexts and `≡` is a frozen
harmless gauge/default equivalence. The proof must additionally show that equal
inventories imply equal outputs for every entry point and reverse predicate of
`𝔐`.

Only when the authoritative inventory is genuinely unary does this reduce to

\[
E_{22}(s_a)=E_{22}(s_b)\Longrightarrow s_a\equiv s_b.
\]

Without this proof, direct S16/eigenmode dependencies remain in `𝔐`. No native
direction may disappear merely because one relation inventory does not separate
it.

---

# 7. Queries are evaluations of the same program

A query `q` selects an entry point/contraction of `𝔐`:

\[
\boxed{Y_q=\mathfrak M_q[\Psi]}.
\]

Required query families include:

```text
sensor left/right RGB-D shadow
XR eye left/right retinal shadow
intrinsic neighbour/context relation
scanner prediction/support shadow
rich textured 3D export
debug/analysis projections
```

These are not separate models. They are evaluations of the same S16 field under
different query parameters and reductions.

Generated local evaluation and scene reduction may be separate physical stages.
They are compiler lowerings, not ontology.

---

# 8. Whole-field shadow and first hit

The physical sensor or eye shadow is the field-level result

\[
\mathfrak M_q[\Psi],
\]

not an observation of one carrier sample.

The query program owns all effects requiring more than one contribution:

```text
overlap
direct projective order
first manifested stratum / first hit
occlusion
fold or two-sheet collapse into one footprint
finite-footprint integration
query-relevant native-null/ZD manifestation
descriptor-owned nonassociative context
```

For a sensor footprint, only the clear pre-hit sector and first supported measured
stratum constrain the field. State behind the first hit receives no inclusive
constraint, exclusion or confidence change.

Direct order, exact zero-divisor structure and nonassociative context remain
distinct observables:

```text
direct order        front/back/first-hit ordering
exact ZD            exact algebraic null/singular relation
near singular       calibrated nonzero residual class
nonassociation      bracket-sensitive context
```

None may impersonate another.

"Whole-field" is semantic coverage, not permission for full-world traversal. The
compiler must generate a conservative region bound

\[
\boxed{
B_q(P)=0
\Longrightarrow
P\text{ contributes exactly zero to }\mathfrak M_q[\Psi]
}
\]

for each query family and indexed logical region `P`. `B_q(P) != 0` is only a
possible contribution: false positives are legal, false negatives are not. Bounds
must include finite footprint, direct order, native relation/ZD/nonassociative
context and the published `chi/kappa` representation; a cheap geometry box is insufficient unless the
generator proves it conservative for the complete query.

A disposable sparse query-support index stores/reduces these conservative bounds
over the finite non-default/mixed-boundary directory. It must produce a finite
worklist without scanning the entire logical field, include nonresident encoded
pages and remain complete after local refinement. It is derived from published
representation bytes and generated program fingerprints, can be deleted/rebuilt,
and never owns support identity. Missing, stale or corrupt summaries cause
rehydration/backpressure/fail-closed inclusion, never omission. Residency, page,
segment and index tree shape cannot prune physics.

All-default quiescence is what makes the infinite default remainder safely absent
from this index. N1R must generate the summary/bound law; N2R must compare indexed
worklists with exhaustive small-domain evaluation; N5R must persist enough
summary metadata to query nonresident storage without decoding the whole world.

---

# 9. Canonical rig observation

One canonically admitted rig frame is

\[
\boxed{Y_t=(O_L,O_R,\Gamma_t)}.
\]

Each eye shadow is

\[
O_e=(D_e,RGB_e,\Omega_e,F_e,P_e),
\]

where `Ω` is the finite footprint, `F` the first-hit/support role and `P` exact
provenance/independence plus the frozen sensor-uncertainty descriptor. It retains
range/incidence/baseline, pose/calibration uncertainty and optical sharpness/
motion/exposure validity as exact regions/categories, plus the calibrated
illumination and sensor-transfer nuisance regions required by `I_Q`, not one
confidence scalar.
`Γ_t` freezes timestamps, rig pose, calibration and epoch.

XR/spatial anchors may stabilize the query/world gauge across large spaces,
sleep/wake and relocalization. They are representation-neutral pose infrastructure:
they do not identify rooms, surfaces, carrier regions or physical objects. An
anchor change must transform query descriptors consistently and leave canonical
S16 bytes invariant for the same physical evidence.

Depth and optical/RGB leaves within one eye are separately retained,
non-bootstrap and non-weighted exact constraints of the same coherent eye shadow.
Neither may initialize, narrow or weight the other before their pullbacks meet in
the common reverse program. Their shared footprint, pose, calibration, first-hit
and occlusion context remains explicit in that joint query; separateness does not
mean four unrelated physical worlds or statistically independent sensors.

Optical nuisance parameters are evidence-side variables of this same query. They
may explain only variation admitted by the frozen photometric law. A genuine
appearance change that cannot be reconciled by that law remains an exact
contradiction and follows the static-correction/temporal boundary; it is not hidden
by broadening `L_t` or `E_t` after the fact.

The two eyes are coherent shadows of one field, not four physical source worlds.

An admitted frame is owned until a terminal disposition. Sensor capture may apply
a deterministic pre-admission sampling policy, but admitted evidence is never
silently overwritten or partially retained.

---

# 10. Scan is exact reverse evaluation

For measured region `O`, the observation preimage is

\[
\boxed{
\mathcal A_q(O)
=
\{\Phi:\mathfrak M_q[\Phi]\in O\}.
}
\]

Scanning is reverse constraint propagation through the exact same expression and
bracket DAG used by forward evaluation. It is not numerical matrix inversion and
not reconstruction of XYZ followed by S16 encoding.

For an expression such as

\[
e=(as)b,
\]

the reverse contractor propagates through precisely `(a s) b`; it may not replace
it with `a (s b)`.

Every contracted output interval rounds outward. Contradictions preserve exact
gaps and provenance. Independent constraints meet by set intersection, never
weighted averaging.

## 10.1 Native directional reverse action and first-hit mould

The exact sensor reverse program must retain the useful injection/mould behaviour
without introducing a pressure world.

For one measured footprint the scene-level reducer provides exactly:

```text
sensor origin
    -> proven clear pre-hit sector
    -> measured first-hit admissible stratum (the mould)
    -> behind-hit sector = NO_CLAIM
```

Reverse evaluation of this result generates a query-direction action/cotangent
certificate `Λ_{q,p}` over the affected native S16 directions. Conceptually, in a
region where a proven local linearization is valid,

\[
\lambda_{q,p}
\sim
D\mathfrak M_{q,p}[\Psi]^*W_{q,p}r_{q,p}.
\]

This formula explains direction and information geometry only. Canonical
execution is the outward-rounded exact reverse evaluation of the fingerprinted
bracket DAG in Q16.48. `W`, a floating gradient and an iterative force solver have
no mutation authority; nonlinearity, ZD structure and nonassociative brackets may
not be approximated away.

The exact semantics are:

- manifested support in a sensor-proven clear pre-hit sector receives an exclusion
  constraint whose reverse action points toward satisfaction of the measured
  first-hit relation;
- the measured first-hit stratum supplies the stopping/contact admissible region;
- every behind-hit state receives exactly no constraint and no reverse action;
- no ray carving, global erosion or occupancy subtraction is allowed;
- if the complete native relations prove that an erroneous near manifestation and
  the mould belong to one admissible deformation/reparameterization, closure
  corrects/refines the same `Ψ`;
- otherwise closure may replace proven false support by an exact admissible state
  (including `ZEmpty`) and admit mould-supported state only after the normal
  disjunction/gauge-equivalence proof; it may not drag an unrelated farther sheet;
- independent views contribute their own query-direction constraints, so a
  view-axis artefact contracts toward their common Merkaba-consistent feasible
  region rather than an averaged 3D point.

"Pressure" and "mould" are names for this exact reverse action and admissible
first-hit boundary. `Λ` is proof/scratch derived from observation plus `𝔐`; it is
not a canonical field beside `Ψ` and is never persisted as a physical object.

Depth and optical leaves generate separate reverse actions through the same
program and meet only in joint closure. Range, incidence, baseline, pose/
calibration uncertainty, blur, motion and exposure validity change their exact
admissible measurement regions and constrained native subspaces, never a weighted
blend. A sharp valid optical observation can tighten appearance modes at the
correct first-hit support; a broad dim/blurred/grazing observation cannot broaden
or overwrite stronger retained appearance/detail. Physical appearance remains in
`Ψ`, never in a second texture truth.

---

# 11. Disjunctive preimages are contractor state, not world objects

A shadow may admit alternative field supports:

\[
\mathcal A_q(O)=A\cup B\cup C.
\]

The union is mandatory. It means `A OR B OR C`.

During reverse execution, alternatives may be represented by bounded branch
masks, compact ranges or cold overflow records. These are disposable contractor
scratch. They have no physical identity and do not define charts, germs, objects
or topology.

If ambiguity survives a frame boundary, persist:

```text
the exact unresolved constraint/program cursor
source evidence references
provenance and independence
```

Do not persist a physical `ShadowHypothesis`, `UnresolvedShadowBranch` or
`BoundNativeBranch` identity.

A support key in scratch is a possible input address to the reverse program. It is
not evidence identity and cannot be embedded into the immutable observation.

---

# 12. One semantic close/commit

Let `𝒞_t` denote the exact accumulated feasible-set certificate for the canonical
field. It is epistemic proof state, not a second physical world.

For a new admitted observation:

\[
\mathcal C_{t+1}
=
\{\Phi\in\mathcal C_t:
\mathfrak M_{sensor,L}[\Phi]\in O_L
\land
\mathfrak M_{sensor,R}[\Phi]\in O_R
\land
\mathfrak M_{rel}[\Phi]\text{ satisfies all active native relations}
\}.
\]

The sole semantic mutation is

\[
\boxed{
\operatorname{NativeCloseCommit}([\Psi_t]_{gauge},\mathcal R_t,Y_t,\mathcal C_t).
}
\]

`𝓡_t` supplies the selected finite representative; it does not add another
physical argument. The result is a new feasible physical gauge class plus either
the unchanged representative or one normalized representation revision.

Sensor validity and intrinsic relation validity are solved in the same feasible
set. A sensor-valid intermediate may not be committed before relation closure.

Physical GPU phases may be separately profiled. They never become independent
semantic authorities.

---

# 13. Legal mutation and idempotence

The current field is tested first.

If

\[
\operatorname{DecodeField}(\mathcal R_t)\in\mathcal C_{t+1},
\]

canonical S16 bytes remain unchanged. Evidence/certificates may strengthen. This
is exact idempotence, not an ambiguity heuristic.

This theorem owns a generated exact no-change fast path. If the root-selected
forward shadow and relation witnesses prove that the complete current
representation already lies in every new measurement region and no newly active
relation/support frontier exists, the submission emits one `NoChangeReceipt` and:

```text
emits zero NativeStateDelta
emits zero NativeGaugeDelta
clones zero pages
recloses no relation outside the exact proof frontier
retains/minimizes evidence only where it adds independent proof
```

The fast path may use conservative summaries and cached witnesses only when their
generation/program/gauge fingerprints match the selected root. A failed or
inconclusive fast test falls through to the same exact closure; it never declares
no-change heuristically. Stable revisits must not pay the cost of a full native
solve merely to rediscover byte equality.

If the current field is infeasible, a canonical delta is legal only when:

1. reverse closure resolves one physical equivalence class; or
2. every surviving branch produces the same byte-identical delta over every
   affected carrier address; or
3. the generated native law proves a common delta valid over the complete union.

Otherwise no canonical state mutates. The unresolved constraint and evidence are
retained without physical branch identity.

No selector may choose `this wall`, `that wall` or `new support` by XYZ distance,
pixel distance, address order, prior distance or scalar confidence.

---

# 14. Selector and complete-relation fibres

After closure resolves one harmless-equivalence class, the generated selector may
choose its deterministic representative.

Preservation is defined against the complete accumulated Merkaba relation program,
not the kernel of one partial readout.

Define the active evaluation family

\[
\mathcal E_t(\Phi)
=
\{\mathfrak M_q[\Phi]\}_{q\in Q_t}
\]

including retained observations, native relation predicates and required
publication invariants. Two feasible fields lie on one invisible fibre only when
the generated program proves them indistinguishable under this complete family.

The selector preserves the prior representative only along that proven fibre.

A direction transparent to one projector may still affect visible response through
TOE-supplied eigenmode/shadow coupling. Such a direction is not frozen merely
because it lies in the kernel of one local matrix.

A linear right-lift or block-preserving update is an optimization only after the
generator proves exact decoupling for the complete active relation program. The
identity `R L_R = I` alone is insufficient.

---

# 15. Evidence and epistemic state

Evidence is not canonical physical state, but it is required to prove mutation.

Retain:

```text
immutable admitted observation packets
separately retained depth and optical leaves
pose/calibration epoch
finite footprint and first-hit role
exact source uncertainty regions
range/incidence/baseline and pose uncertainty
sharpness/motion/exposure validity descriptors
provenance/independence
unresolved reverse constraints when still required
```

The complete source journal survives until a deterministic certificate or durable
equivalent handoff proves it reclaimable. A reduced joint cell is only a cache or
witness, never a replacement for source evidence.

Repeated identical evidence in the same independence class does not gain strength
by vote count. New pose/baseline/footprint phase constrains the field only through
the frozen sensor uncertainty law.

Evidence lifetime must also be asymptotically bounded by unresolved or genuinely
new information, not by elapsed frame count. A deterministic minimizer may replace
raw observations only after proving equivalence of the retained certificate under
the complete generated forward/reverse program. It must preserve disjunctions,
cross-locality factors, independence classes, directional information, optical
nuisance regions and every receipt required for replay.

For a converged static locality, exact duplicate or weaker compatible revisits
must eventually reduce to an idempotent certificate representation. Ten thousand
such revisits may strengthen a bounded receipt/counter only where the frozen
independence law gives that operation exact meaning; they may not retain ten
thousand full journals. A new independent angle, finer supported bandwidth or an
unresolved contradiction may grow the certificate. Resolved redundant raw data is
released after durable equivalent handoff; unresolved source evidence remains.

The required growth law is therefore

\[
\boxed{
\operatorname{bytes}(proof)
=O(\text{unresolved structure}+\text{genuinely new native information}),
}
\]

not `O(number of admitted frames)`. This is an acceptance theorem, not permission
for lossy summarization.

No scalar precision/confidence is canonical physics. Derived scalar diagnostics
may not choose hypotheses, fuse state, allocate gauge, classify relations or
colour physical export.

## 15.1 Native cellular compression and directional information

For each currently sampled locality `ξ`, let

\[
\mathcal C_{\xi,t}
=
\operatorname{Proj}_{\xi}(\mathcal C_t)
\]

denote the exact admissible S16 marginal induced by the accumulated global
feasible-set certificate. It is epistemic state about `Ψ(ξ)`, not another
physical field. Because a projection can hide cross-locality/disjunctive coupling,
`\mathcal C_{\xi,t}` alone is never sufficient to authorize mutation; retained
coupled constraint/witness references remain part of `\mathcal C_t`.

An implementation may losslessly compress the locally relevant proof as an exact
directional information form `H_ξ`, interval/polytope factors, generated
eigenmode factors or an equivalent certificate. Any such form must be
reconstructible from retained evidence/certificates or carry an exact equivalence
proof. It is not stored in S16 lanes and is not a second world.

`H_\xi` may accelerate contractor ordering, rejection or refinement admission only
where the generator proves equivalence to the exact feasible constraints. It may
not rank non-equivalent supports, choose a gauge representative, authorize a
delta, erase a disjunction or resolve a contradiction.

For a compatible, genuinely independent observation and a proven local linear
factorization, its conceptual information update is

\[
H_{\xi,new}
=
H_{\xi,old}+J_q^T W_qJ_q
\]

only over native directions actually constrained by that query. This is an
information-geometry explanation, not a replacement for exact set intersection.
The canonical law remains

\[
\boxed{
\mathcal C_{new}
=
\mathcal C_{old}\cap\mathfrak M_q^{-1}(O_q).
}
\]

Therefore:

```text
compatible stronger evidence -> same or tighter native feasible region
compatible weaker evidence   -> may strengthen proof, never broaden prior state
exact duplicate/same class    -> no vote-count strength
incompatible evidence         -> explicit gap/disjunction, never averaging
```

The directional certificate must retain or reconstruct all information needed to
distinguish at least:

```text
source uncertainty law and independence class
finite metric footprint and range
incidence/query direction and stereo baseline
pose/calibration uncertainty
sharpness, motion and optical/exposure validity
independent-view diversity
constrained Merkaba/eigenmode subspace
residual/compatibility region
best supported sampling bandwidth
coupled cross-locality/native-relation witnesses
```

No scalar confidence may replace this structure. The useful old "cell
resistance" means simply that already-tight exact constraints remain in the
intersection: a broad weak revisit often leaves the current representative
feasible and unchanged. It does not allow old evidence to overrule a genuine exact
contradiction.

The compressed locality form and global certificate minimizer share one proof
boundary: reconstruction from the minimized form must yield the same feasible-set
predicates, common deltas, gauge/refinement decisions and forward verification as
the retained source journal. If this equivalence is not proved for a factor, that
factor and its exact source evidence remain unminimized.

---

# 16. Null-to-support in the same field

An unexplained observation does not create an object or branch entity.

Reverse closure considers the existing field and gauge-equivalent latent regions
whose current value is `ZEmpty`.

A delta

\[
z_\varnothing\rightarrow s_{supported}
\]

is legal only when:

- the current manifested field cannot satisfy the accumulated observation;
- all surviving physical preimages require non-default support;
- exact independent-support proof passes under the frozen sensor law;
- native relation closure permits the update;
- the placement is unique modulo proven gauge equivalence, or all placements yield
  one common normalized representation delta after gauge normalization;
- forward evaluation satisfies every retained relevant shadow.

The reverse contractor returns a relative intrinsic support pattern, its required
native-relation attachments and the complete gauge-equivalence class. It does not
return a pixel rectangle or physical object identity. `N_g` then constructs the
canonical `chi/kappa` embedding and backing addresses. Allocation merely realizes that
already-normalized representation.

If multiple non-equivalent placements survive, the constraint remains unresolved.
If multiple equivalent placements survive but no constructive byte-deterministic
normal form has been proved, the constraint also remains unresolved.

The first publication-capable replacement run must already implement the minimal
proved base-density case. An all-`ZEmpty` field must be able to admit uniquely or
gauge-equivalently resolved support without `NOVEL`, pending, a pixel bbox or a
legacy path. Adaptive local density may follow in the refinement run, but a fresh
world cannot be accepted while every observation is deliberately left unresolved.
That first live cut uses only the generated uniform/base-density
`(chi_0,kappa_0)` subset and fingerprints it in the root; it may not smuggle in an
unproved regional refinement before N4R.

---

# 17. Native relation, ZD and nonassociation

Neighbour continuity, folds, singularities and null/contact behaviour are relation
queries of `𝔐` over the same field, for example

\[
\tau_{ij}=\overline{\Psi_i}\Psi_j
\]

and descriptor-owned higher-arity expressions.

Exact zero-divisor evidence is

\[
\tau a_k=0.
\]

A nonzero calibrated residual is `NEAR_SINGULAR_Q48`, never exact ZD.

Every multi-factor relation preserves its bracket tree, for example

\[
(\tau_{ij}\tau_{jk})a_r
\quad\text{versus}\quad
\tau_{ij}(\tau_{jk}a_r).
\]

Relation results may include generated classes such as regular, exact-ZD,
near-singular, nonassociative-context, fold/crease, null/contact, no-relation and
unresolved, but only where the authoritative program defines them.

There is no topology or stitch world. A generation-keyed relation cache is a
disposable evaluation cache and owns no physical state.

---

# 18. Static correction and temporal evolution

Behind-hit remains no evidence.

Clear-path or pre-hit evidence may nevertheless correct a false manifestation of
the same static scene. If repeated independent observations prove that a prior
supported value is impossible, reverse closure replaces it only by an exact
admissible state, including `ZEmpty` when the complete feasible set requires it,
while preserving every unaffected native relation.

This is epistemic correction, not temporal scene evolution.

The native directional action is the primary static view-axis artefact correction:
clear pre-hit exclusion plus the measured first-hit mould contracts the same
feasible field from each independent direction. It either proves a common
deformation/refinement, or performs exact false-support removal plus separately
proved support admission. It never smears unrelated sheets or carves behind the
hit.

S4‑09 begins only when observations cannot be reconciled as one static field under
the admitted epoch/provenance model. It owns actual appearance/disappearance and
identity-preserving transport through time.

---

# 19. Multi-pass S16 tomography

For retained observations `q_1 ... q_n`, knowledge is

\[
\boxed{
\mathcal C^{(n)}
=
\mathcal C_{prior}
\cap
\bigcap_{k=1}^{n}
\mathfrak M_{q_k}^{-1}(O_k).
}
\]

Time is not a seventeenth lane and not a separate source solver. Each new query
cuts the same S16 field through a different full-program relation.

A revisit may:

```text
leave state bytes unchanged and strengthen proof
resolve a previous disjunction
contract directly visible native combinations
constrain modes previously transparent to another query
constrain indirectly coupled shadow modes
prove static exclusion
prove finer intrinsic variation
```

Strong-to-weak and weak-to-strong replay must be order invariant.

Directional reverse action and accumulated locality information are two views of
this one intersection, not two solvers:

```text
broad compatible new constraint + tightly informed locality -> no broadening
tighter independent compatible constraint                  -> further contraction
proof of finer variation                                   -> sampling refinement
incompatible constraint                                    -> unresolved/static
                                                             exclusion/temporal gate
```

---

# 20. Exact local gauge density and refinement of the same `Ψ`

There are two and only two refinement regimes.

## 20.1 Same-representation S16 tomography

Accumulated constraints first tighten admissible S16 values at the current
representation `(chi_r, kappa_r, Psi-hat_r)`. No coordinate map, reconstruction
plan or backing density changes while the retained evidence can be reproduced at
that capacity.

## 20.2 Representation refinement

The second regime begins only when retained observations prove variation finer
than the current intrinsic sampling/reconstruction plan can represent:

\[
\Psi(\xi+\delta_1)\neq\Psi(\xi+\delta_2).
\]

The runtime then increases backing degrees of freedom over the same intrinsic
region by replacing `(chi_r,kappa_r)` with a finer exact representation plan. Let

\[
\mathcal R_r=(\chi_r,\kappa_r,\widehat\Psi_r,\mathcal W_r)
\]

denote the published representation, exact reconstruction plan and only the
representation/equivalence witnesses `W_r` required to interpret it. A refinement
transform

\[
T_r:\mathcal R_r\rightarrow\mathcal R_{r+1}^{prior}
\]

is legal only if it proves

\[
\boxed{
\forall q\in Q_{retained}:\quad
\mathfrak M_q[\operatorname{DecodeField}(\mathcal R_r)]
=
\mathfrak M_q[\operatorname{DecodeField}(\mathcal R_{r+1}^{prior})]
}
\]

and equality of every active intrinsic relation predicate, complete-program fibre,
certificate interpretation and canonical default boundary. Only after this
representation-equivalent prior exists may newly resolved higher-frequency
information contract `Ψ̂_{r+1}`.

Refinement is legal only when:

1. retained evidence is jointly supportable;
2. current representation cannot satisfy it without contradiction or destructive
   broadening;
3. the exact map/reconstruction family, support measure, prolongation/transport
   and inverse interpretation are generated and proved against the complete `M`;
   no implicit interpolation, resampling weight or footprint ownership is
   invented by storage code;
4. all S16 state, feasible-set factors, source evidence, native relations,
   independent-view receipts, optical nuisance regions, directional information
   forms and best-supported bandwidth transport exactly;
5. the old field and all exact readouts/relations are bit-identical before adding
   new information;
6. the refined result forward-verifies all retained queries;
7. `N_g` normalizes the result independently of observation/allocation order;
8. backing decomposition and replay remain byte deterministic.

The map/reconstruction plan may be encoded regionally, but it may not create a
physical chart or child-germ hierarchy. Region boundaries are representation
boundaries only and must be invisible to every query and relation. A coarsening/
restriction is legal only under the symmetric exact theorem that it loses no
retained native information.

The coastline/Riemann intuition is deliberately non-physical: `χ` locally changes
how much exact representation capacity covers one intrinsic region. Flat/simple
native variation can remain sparse and compress as `CONST/AFFINE`; supported fine
variation can receive denser samples and use `DELTA/RAW`. Density follows proven
native bandwidth/complexity, not metres, camera pixels, range or a 3D resolution
heuristic.

No second detail world appears. The physical state remains `[Ψ]_{gauge}`.

---

# 21. Sparse immutable representation publication

Only a resolved full-S16 `NativeStateDelta` mutates the physical field. An exact
`NativeGaugeDelta` may mutate its finite representation without changing physical
content, but only with the complete equivalence and normalization proof from §20.

```text
NativeStateDelta
    normalized backing/intrinsic address
    prior state generation
    full S16[16] result
    changed mask
    relation-program witness
    evidence receipts

NativeGaugeDelta
    affected backing region
    prior gauge generation/fingerprint
    new exact chi/kappa representation
    old-to-new state/evidence/relation transport witness
    gauge-equivalence and normalization receipt
```

Both are carried by one `NativeFieldRevision`. A gauge-only revision changes no
physical query result. A refinement revision may contain both gauge and state
deltas, but the proof must expose the representation-equivalent pre-information
state even though no reader observes it as a separate root.

The published representation root selects atomically:

\[
\boxed{
(\chi_r,\kappa_r,\widehat\Psi_r,\text{program/default fingerprints},
\text{certificate manifest},\text{page directory})
}
\]

`chi/kappa`, certificates and directory metadata are not extra physical worlds;
they are required to interpret/prove the one field. A reader may never observe
new `Psi-hat` with an old representation plan, or a new representation plan with
old `Psi-hat`.

Publication order is:

```text
resolve and normalize all affected state/gauge deltas
retain complete evidence/certificate ownership
prepare all touched shadow page generations
scatter exact S16 and representation bytes
close page, gauge, certificate and directory manifests
validate all relations and mappings
exchange the immutable revision root last
```

Any fault before the final root exchange leaves the previous root authoritative.
Readers never observe a mixed revision.

Unchanged evidence does not clone pages or increment a state/gauge generation.

---

# 22. Storage, codec, durable page directory and residency

The canonical logical field is unbounded; decoded GPU residency is bounded. This
is a production contract, not a later aspiration.

Pages, blocks, segments, codecs, chunks and files are storage only. Their shape,
order and residency may not affect `Ψ`, query results, reverse constraints or
certificates.

The live backing must contain four representation-neutral layers:

```text
published logical page/gauge key and generation directory
durable lossless encoded page/gauge/certificate blobs on device storage
bounded decoded GPU current/shadow residency cache
derived conservative query-support summaries/index
```

The directory and root manifest are append/replace atomically and identify the
latest durable generation independently of resident slot/segment. A clean page is
evictable only after its encoded bytes, gauge metadata, certificate receipts and
query-support summary are durably reachable from a published manifest. Rehydrate
must reproduce the exact decoded representation before it becomes query-visible.

After the `ZEmpty` gate:

- unbacked carrier addresses evaluate as exact `ZEmpty`;
- `NULL` codec blocks decode to exact `ZEmpty`;
- `CONST`, `AFFINE`, `DELTA` and `RAW` are lossless encodings of the same field;
- clean pages may encode/evict/rehydrate transparently;
- capacity pressure changes residency, never logical world size.

The current `NULL/CONST/AFFINE/DELTA/RAW` codec is a donor for state payloads. The
production schema must additionally encode/fingerprint the exact `chi/kappa`
normal form or its page/region reference, gauge generation, program/default fingerprint and
conservative support summary required to interpret a nonresident page. The
S4‑08.6 `64×64` page and `8×8` block geometry remains frozen; adaptive density is
represented by `chi/kappa`, not by silently changing this ABI.

Page lookup, encode, durable write, eviction, asynchronous rehydrate and GPU cache
replacement must be implemented before S4‑08 closure. A resident allocation fault
is backpressure/page-fault work, not proof that the logical carrier is full. The
published eye/readout root remains usable while cold rehydrate/refinement work is
in flight.

Persist:

```text
non-default sparse S16 pages/generations
normalized gauge-map generations and transport fingerprints
Merkaba program and algebra fingerprints
canonical default fingerprint
minimal exact certificates
unresolved constraints and only source evidence still required
calibration/pose metadata needed by those records
conservative nonresident query-support summaries or their deterministic rebuild input
```

Clear-data removes the durable manifest/blobs and resident cache as one explicit
user operation; stopping/sleeping the scanner does not. Restart plus the same
accepted observation sequence produces byte-identical normalized representation,
certificates and readouts. Rehydrating into different physical segments produces
the same result.

---

# 23. Pure readouts

Every readout consumes one immutable published root and mutates nothing.

## 23.1 XR eyes

\[
I_L=\mathfrak M_{eye,L}[\Psi],\qquad
I_R=\mathfrak M_{eye,R}[\Psi].
\]

The low-latency eye path may directly project/reduce the field into two retinal
images. It need not build persistent XYZ, mesh, splat or meshlet worlds.

## 23.2 Scanner prediction

Sensor-query evaluation produces disposable predicted order/depth/optical/support
records and possible reverse-support keys. It proposes contractor work but cannot
mint identity.

## 23.3 Rich 3D export

Export evaluates the latest full `Ψ` using the authoritative geometry,
connectivity and appearance entry points of `𝔐`.

The current exact projective donor is

\[
h=G(s),\qquad
X(s)=\left(\frac{h_1}{h_0},\frac{h_2}{h_0},\frac{h_3}{h_0}\right),
\]

and remains valid only if N1R fingerprints it as the authoritative geometry query
or proves an exact replacement. `h_0` is a generated projective support coordinate;
it must not be reinterpreted as generic epistemic confidence.

Connectivity is derived from generated native relation support. Coordinate-local
carrier adjacency is its fast domain; descriptor-proven seam/gauge/nonlocal tuples
may also participate. Neither disposable relation tuples nor caches form a
canonical connectivity graph, and XYZ proximity never welds. Physical appearance
comes from `Ψ`; evidence and certificates prove it but may not secretly supply a
second texture truth.

If unresolved optical detail is needed, close/refine `Ψ` first.

## 23.4 Debug

XYZ, depth, optical response, exact-ZD, near-singular, bracket, evidence and
refinement diagnostics are disposable queries only.

Deleting every readout cache must leave canonical bytes and replay unchanged.

---

# 24. Correct GPU lowering

The semantic model is one relation program and one close/commit. A preferred sparse
physical lowering is:

```text
SelectNativeQuerySupport
    conservative resident/nonresident worklist from generated exact bounds

EvaluateNativeQuery
    generated local query plans over relevant carrier support

ReduceNativeQuery
    whole-query many-to-one order/first-hit/occlusion reduction

ContractNativeQuery
    reverse the same generated plans against measured RGB-D regions
    emit exact directional reverse-action/information witnesses
    retain disjunction as transient contractor scratch

CloseNativeConstraints
    combine all observation leaves, accumulated feasible-set/information
    certificates and native relations
    emit only resolved/common sparse S16 state deltas, normalized support/gauge
    deltas, no-change receipts or unresolved exact constraints

ResolveContractorOverflow          indirect/cold
    continue the same reverse/closure program for cross-block or branch overflow

PrepareChangedRepresentation       indirect
ScatterChangedStatesAndGauge       indirect
CloseAndPublishRevision
```

Names may change. Their semantic limits may not.

`Evaluate/Reduce` are query compiler stages. `Contract/Close` are forward/reverse
execution of the same program. Overflow is continuation of the same solve, not a
scheduler, object manager or second physical authority.

`SelectNativeQuerySupport` is a derived culling lowering with false positives and
zero false negatives. Nonresident candidates produce asynchronous page-fault work
or conservative backpressure; they are never silently omitted.

The generated no-change proof exits before state/gauge allocation and page clone.
Cold refinement, disjunction overflow or SSD rehydrate may continue
asynchronously, but published-root eye queries remain independent.

No CPU pixel loop, CPU mesh, synchronous readback or full-world traversal is
allowed.

---

# 25. Runtime data boundary

Long-lived immutable evidence:

```text
NativeObservationGpu
    observationRevision
    sensorSide
    independenceClass
    poseCalibrationEpoch
    footprint
    depthOrderRegion
    opticalRelationRegion
    firstHitRole
    sensorUncertaintyLaw
    rangeIncidenceBaselineRegion
    poseCalibrationUncertainty
    opticalQualityRegion
    illuminationRegion
    exposureGainTransferRegion
    photometricLawFingerprint
    rawReference
```

Disposable contractor scratch may contain:

```text
observation ordinal
query-program cursor
possible carrier support address/range
branch mask/range
exact interval bounds
forward witness
directional reverse-action witness
exact information-factor update
```

It is cleared/rebuilt and is never persisted as physical identity.

Cross-frame unresolved state is an `UnresolvedConstraintRecord`:

```text
source evidence references
exact program cursor/expression fingerprint
exact admissible set/disjunction encoding
provenance/independence
directional information/coupled-factor certificate refs
best supported sampling bandwidth
no physical branch id
no chart
no extent
```

Physical mutation is `NativeStateDeltaGpu`:

```text
normalized backing address + gauge generation
prior state generation
full S16[16]
changed mask
program witness
evidence receipts
```

Representation-only mutation is `NativeGaugeDeltaGpu`:

```text
affected backing range
prior/new exact chi/kappa fingerprints or inline normal-form record
old-to-new sample/evidence/relation transport range
equivalence and canonical-normalization receipt
```

One `NativeFieldRevisionGpu` owns compact state deltas, gauge deltas, certificate/
directory manifests, the prior root and the proposed root. State/gauge records are
never published independently.

Derived noncanonical storage/query metadata may contain:

```text
NativeQuerySupportSummary
    representation root + program fingerprint
    logical page/gauge range
    exact conservative query-family bounds
    durable encoded-generation locator
```

It can be rebuilt and has no mutation authority.

No packet mixes observation identity with possible support identity.

---

# 26. Host responsibilities

C# owns only:

```text
capture admission and immutable frame leases
pose/calibration epochs
resource and residency lifetime
GPU fences
program/buffer binding
root-last publication orchestration
durable persistence/export orchestration
fault/backpressure reporting
diagnostic readback
```

GPU owns query evaluation/reduction, reverse contraction, branch scratch,
constraint closure, S16 mutation, relation evaluation, gauge work, culling,
compaction and rendering.

Ordinary backpressure does not become a permanent fault. A frame reported as
published must have an advanced compatible root and retained evidence ownership.

---

# 27. Determinism and decomposition invariance

S4‑08.6 freezes the current `64×64` logical page and `8×8` codec-block geometry
as a storage ABI.
Within that ABI, for the same admitted observation sequence, all legal lowerings
produce byte-identical:

```text
normalized chi/kappa gauge records
Psi pages and state/gauge generations
program/canonical-default fingerprints
unresolved constraint encodings
certificates and provenance
durable directory/manifest logical contents
query results at exact query parameters
publication roots
```

The same static independent evidence presented in any permitted order must also
produce the same normalized representation. This is not achieved by seeding an
allocator with revision/provenance order. Before publication,

\[
\boxed{
N_g(\mathcal R_a)=N_g(\mathcal R_b)
\quad\text{for every proved }\mathcal R_a\sim_g\mathcal R_b.
}
\]

`N_g` must be constructive over the admitted finite support, include local density
maps and be fingerprinted with the program. If gauge symmetry cannot be normalized
without choosing physics from observation/allocation order, the update remains
unresolved.

Changing any of the following may change cost only:

```text
workgroup shape
dispatch grid
branch scratch window
query-support compaction order
segment count
residency placement
proof scratch size
GPU scheduling
```

`page/block dimensions` are deliberately absent from that list. Changing them
changes physical backing bytes and page-generation decomposition, so it is not a
legal S4‑08.6 lowering variation.

A future storage-ABI migration may change page/block geometry only with an
explicit migration theorem and a backing-independent canonical logical
serialization, for example the stable ordered stream

\[
\operatorname{CanonicalSerialize}
\{(N_g(\mathcal R)_r,\widehat\Psi_r,
\text{state/gauge generations},\text{program/default fingerprints})\}.
\]

Such a migration must prove byte identity of that logical serialization,
certificates and exact query results; it may not claim byte identity of differently
shaped backing pages. It is outside this closure run.

Reverse branches use stable generated order only for representation. Ordering may
not choose physics. Gauge allocation order operates only after equivalence proof.

Illegal Vulkan dimensions, arithmetic overflow, missing program bindings, capacity
exhaustion and unresolved required relations fail closed before root publication.

---

# 28. Performance ownership

Work must scale with new information:

```text
query-visible support
active reverse constraints
changed S16 locality
unresolved contractor frontier
touched pages
```

It must not scale with:

```text
entire logical world
pixels × carrier segments
full optical edge universe
one persistent window per revision
page pairs as world lifetime
all historical evidence replayed every frame
```

The `cac9ab0` Release profile is the replacement baseline:

```text
266 dispatches / 61 kernels
compute                         4520.946 ms
ClosePendingEdges              2328.958 ms
BuildRgbSourceCells             937.364 ms
EvaluateCandidateMeets          837.657 ms
top three                      90.78%
```

These costs are evidence that topology, modality-specific inverse and candidate
association were computed as three separate systems. The replacement removes
those semantic duplications; it does not merely rename or micro-optimize them.

Release telemetry is one-shot opt-in and records actual per-kernel GPU timestamps
without enabling the Unity development profiler. The timestamped submission is
diagnostic-contaminated and is not used as an ordinary wall baseline.

## 28.1 GPU dispatch-collapse contract

Dispatch count is an acceptance gate independent of elapsed time. Deleting old
symbols or reducing milliseconds while recreating a 100--200-dispatch graph under
new names is a structural failure.

The measured baseline is exactly:

```text
cac9ab0
    266 recorded Vulkan compute dispatches
     61 distinct exercised compute kernels
```

The current Release timestamp path explicitly wraps direct and indirect
`DispatchCompute` calls; it does not automatically count Vulkan raster draws. For
the budgets below define the structural graph-command count

```text
D_budget = recorded PRISM compute dispatches + recorded PRISM raster draws
```

and report both terms separately. Every required PRISM raster draw consumes one
budget slot rather than escaping the gate under a different Vulkan verb. If a
replacement readout uses raster draws, it must add explicit stage-correct Release
instrumentation before acceptance. The `cac9ab0` baseline term is 266 compute
dispatches; no unmeasured draw is retroactively claimed by that sample.

For one admitted scan frame, the counting interval is:

```text
admitted observation becomes GPU-owned
    -> terminal Fault / NoChangeReceipt / published-root disposition
```

It includes every hot PRISM submission needed for:

```text
query-support selection
forward sensor query and whole-query reduction
reverse contractor and native-relation closure
mandatory compaction, prefix or indirect-argument preparation
cache/relation preparation required by that frame
representation preparation and state/gauge scatter
publication validation and close
```

`generated`, `helper`, `scan`, `prefix`, `compaction`, `indirect preparation`,
`cache update` and `relation preparation` are not exclusions. Meta/camera capture
before PRISM ownership, timestamp instrumentation itself and unrelated Unity/XR
compositor work are excluded. Explicit cold continuations are excluded from the
hot total only under section 28.3.

For an eye readout, count from query invocation against one immutable published
root until both retinal outputs are complete. Both eyes share one budget; a
separate allowance per eye is forbidden.

For bounded warm-resident work, the counted graph-command sequence must be
invariant to:

```text
total world size
resident segment count
total carrier/page count
historical revision/observation count
candidate/provider/page-pair count
pixel-edge count
```

Those quantities may change compact worklist cardinality, dispatch dimensions and
workgroup count. They may not create one host-recorded dispatch sequence per page,
segment, candidate class, relation family, S16 lane, E22 relation, bracket/IR node,
evidence class or sort level. Required work is lowered to compact worklists plus a
fixed small number of direct/indirect submissions. Generated specialization is
legal only where it reduces actual parallel work; it may not mechanically turn
the IR into a kernel-per-node orchestration graph.

The hard staged budgets are:

```text
N3R warm ordinary base-density chi_0/kappa_0 scan
    D_budget <= 80

N4R warm ordinary same-sampling informative scan
    D_budget <= 64

N5R same warm-resident ordinary fixture
    D_budget <= 56

N6R/N7R stable exact no-change revisit
    D_budget                 <= 20
    NativeStateDelta count     = 0
    NativeGaugeDelta count     = 0
    page clones                = 0

N6R/N7R ordinary warm-resident bounded 320x320 informative update
    D_budget <= 40

N6R/N7R direct immutable-root eye query producing both eyes
    D_budget <= 8
```

The N4 ordinary fixture does not trigger regional reparameterization. Genuine
new `chi/kappa` refinement is cold, is counted separately and must disappear from
the next equivalent warm frame after completion. At N5, increasing resident
segment/page count while query-visible work stays fixed may not increase the hot
count. SSD page-fault, encode, eviction and rehydrate submissions are cold and may
not be inserted into every warm frame.

Final warm scan fixtures may exercise at most 24 distinct hot GPU entry points;
the final eye-pair fixture may exercise at most 6. These sanity limits do not
authorize one serial generic interpreter kernel. Generated kernels remain
specialized, fixed-schedule and parallel; the goal is to remove duplicated host/
GPU orchestration, not hide it inside one pathological dispatch.

Passing a timing gate never waives a dispatch-count failure. In
particular, any ordinary warm-frame replacement that removes the old graph but
records `D_budget >= 100` is categorically rejected regardless
of LOC or latency.

## 28.2 Required dispatch telemetry and scaling controls

Every N3R--N7R Quest acceptance capture emits one machine-readable row per
stage/entry point containing:

```text
stage and kernel/entry-point fingerprint
compute-dispatch count and raster-draw count
GPU total ms and p95 where applicable
work-item cardinality
query-visible page count
active constraint count
resident segment count
HOT or COLD classification plus cold-reason code
```

and totals:

```text
HOT_COMPUTE_DISPATCHES
HOT_RASTER_DRAWS
TOTAL_HOT_DISPATCHES
COLD_COMPUTE_DISPATCHES
COLD_RASTER_DRAWS
TOTAL_COLD_DISPATCHES
DISTINCT_HOT_KERNELS
DISTINCT_COLD_KERNELS
DISTINCT_HOT_ENTRY_POINTS
DISTINCT_COLD_ENTRY_POINTS
```

The capture stores the exact source commit, APK, program/query/default and
representation-root fingerprints. `TOTAL_HOT_DISPATCHES` is the normative
umbrella count `D_budget = HOT_COMPUTE_DISPATCHES + HOT_RASTER_DRAWS`; it is not a
Vulkan queue-submission count. A count mismatch, unmatched timing event or missing
mandatory entry is a failed capture, not zero work.

Negative controls vary independently:

```text
resident segments       1 / 2 / 7
query-visible pages      fixed
world pages              small / large
historical revisions     short / long
```

For identical query-visible physical work, the hot sequence/count and canonical
result remain identical. A second fixture increases query-visible support: grid/
work-item cardinality may grow, but the hot graph remains the same bounded
sequence rather than adding one dispatch chain per page.

## 28.3 Cold-work boundary

Only the following may exceed the final hot-path budgets:

```text
genuine contractor overflow
new regional chi/kappa refinement
SSD page fault / rehydrate
durable encode / eviction
rare canonical-normalization continuation proved irreducible
```

Cold work is explicitly reason-coded and separately timestamped, is absent when
its trigger is absent, never becomes a permanent frame stage and never blocks the
published-root eye query. Warm work may not be relabelled cold. After cold work
completes, the next equivalent warm frame must return to the normal hot budget.

If exact generated law demonstrably needs a higher count, stop the active run and
report the exact irreducible dispatch dependency and why batching, fusion,
compaction or indirect lowering would change semantics. Budgets are not relaxed to
make an implementation pass.

The old `1500/1800 ms` pair is an S4‑08.6 **recovery ceiling** for early live
replacement cuts, not a final realtime product contract:

```text
N3R/N4R recovery NativeCloseCommit compute  <= 1500 ms
N3R/N4R recovery admitted-frame wall         <= 1800 ms
```

Final S4‑08 acceptance has distinct hot/cold/readout contracts:

```text
stable exact no-change revisit at 30 Hz ingress
    admitted-frame proof wall                 <= 33.3 ms
    state/gauge deltas and cloned pages       0

ordinary bounded informative 320x320 update
    NativeCloseCommit p95                     <= 100 ms
    no revision/segment/residency slope

XR eye query from immutable published root
    <= 13.89 ms at 72 Hz
    <= 11.11 ms at 90 Hz
    independent of concurrent scan/refinement/page-fault work

cold exact work
    large disjunction/refinement/SSD page fault may exceed hot budget
    but uses backpressure/indirect continuation and never blocks eye readout

memory
    bounded decoded residency + active unresolved/new-information proof
    no growth per duplicate revisit
```

All timing gates are Release/Vulkan p95 gates with the exact commit, query/program
fingerprint, work cardinalities, residency state and warmup recorded in evidence.
The stable no-change gate measures admitted-observation ownership through terminal
GPU disposition after a warm published root. The ordinary informative fixture is
warm-resident, excludes a cold SSD fault/refinement overflow and freezes its
query-visible pages plus active constraint frontier in the evidence manifest; a
different workload cannot be substituted under the same number. The eye gate
measures `Select/Evaluate/Reduce` to completed retinal outputs over at least 1000
frames while scan work is queued. Cold work has no fake hot deadline, but its
backpressure may not stall or mix the published eye root.

Pre-admission sensor sampling may be deterministic when ordinary informative work
cannot consume every captured frame, but an admitted frame remains owned. Stable
room revisits must reach the no-change path rather than permanent decimation.

---

# 29. Forbidden architecture

The following are hard failures:

- semantic XYZ/RGB/property lanes inside S16;
- physical chart/sheet/object/branch entities beside `Ψ`;
- physical `ABSENT` state or support bitmap beside S16;
- accepting current `ZNullDyad` from `Gz=0` without full-program proof;
- forcing all native law through E22 without contextual separation and
  complete-law sufficiency;
- independent geometry/RGB/topology physics not generated from one program;
- ContactFilm/Grid16/pressure-buffer/cellular physical state, force iteration,
  gradient mutation or ray carving;
- scalar/factorized information that discards exact disjunction or coupled
  locality constraints and then claims mutation authority;
- first-hit decided by one carrier value;
- one-winner association or mutation of every possible preimage;
- persisted hypothesis/branch identity;
- minimum-change selecting among physical supports;
- freezing one query kernel despite complete-program coupling;
- exact ZD confused with near-singular or projective order;
- separate stitch/topology solver or canonical relation graph;
- PENDING/NOVEL/LatentGerm lifecycle;
- pixel/global-bbox canonical gauge;
- `chi/kappa`/`g` treated as a second physical field, floating metric authority, chart
  manager or observation-order allocator;
- state and gauge generations published under different roots;
- refinement by metres, pixels or 3D voxels;
- texture/detail reconstructed outside `Ψ` for export;
- page/segment/residency identity affecting physics;
- finite resident page pairs treated as logical world capacity;
- query-support culling without a generated zero-false-negative proof;
- optical quality/confidence used instead of a calibrated illumination/sensor
  transfer nuisance law;
- proof/evidence growth proportional to duplicate revisit count after convergence;
- hot dispatch sequences multiplied by pages, segments, candidates, relation
  families, lanes, IR/bracket nodes, evidence classes or sort levels;
- a renamed ordinary warm graph with `D_budget >= 100`, or timing used
  to waive any staged dispatch budget;
- permanent hot work relabelled as contractor/refinement/pager cold work;
- a serial generic interpreter kernel used to fake a low entry-point count;
- legacy/fallback runtime path after cutover.

---

# 30. Exact oracle gates

Before live mutation:

1. every generated expression, arity and bracket is sourced and fingerprinted;
2. CPU semantic oracle and packed-Q48 GPU plan are bit-identical;
3. forward and reverse interval contractors are mutually sound;
4. every supplied E22 relation arity, operand position, admissible context and
   bracket is fingerprinted;
5. `ZEmpty` has representation parity between unbacked, explicitly allocated and
   NULL-decoded storage for every required query and relation context;
6. current `ZNullDyad` is accepted only if it passes gates 5 and 7;
7. an all-`ZEmpty` neighbourhood yields reducer identity, `DEFAULT_SAT` and no
   active work, while mixed default/support boundaries retain defined semantics;
8. E22 contextual separation is proven for every supplied arity/operand position
   or direct S16 dependencies remain;
9. exact ZD and one-LSB nonzero residual classify differently;
10. bracket-swap negative controls differ where TOE says they must;
11. two support preimages remain a union until exact elimination/common delta;
12. no persisted contractor branch gains physical identity;
13. current feasible `Ψ` yields canonical no-change;
14. ambiguous non-equivalent placement yields no mutation;
15. gauge-equivalent placement and disconnected-support discovery permutations
    normalize to one byte-identical `(chi, kappa, Psi-hat)` representative;
16. a query-transparent but program-coupled mode can update when later evidence
    constrains it;
17. a proven complete-program fibre preserves its prior representative;
18. first-hit/occlusion is scene/query-level;
19. behind-hit remains no evidence;
20. static pass-through can remove a false manifestation;
21. local and descriptor-proven nonlocal relation evaluation is
    cache/decomposition invariant;
22. refinement preserves the prior field before adding finer information;
23. clear pre-hit plus measured first-hit produces the exact directional mould
    action, while behind-hit produces no constraint/action;
24. near false support and a farther measured mould converge only through a proven
    common deformation or exact remove-plus-admit closure, never carving/dragging;
25. compatible weak evidence cannot broaden a stronger locality certificate;
26. weak-to-strong and strong-to-weak independent evidence produce the same exact
    feasible field;
27. optical blur/exposure/incidence changes exact uncertainty regions and cannot
    overwrite stronger supported appearance by recency;
28. locality information compression is equivalent to retained exact constraints
    and preserves coupled/disjunctive dependencies;
29. refinement transports S16, exact feasible sets, directional information,
    evidence, relations and bandwidth with pre-addition readout parity;
30. static-incompatible moving evidence remains unresolved for S4‑09 rather than
    becoming an elastic smear;
31. the generated `chi/kappa` map/reconstruction family, exact arithmetic,
    prolongation/transport and canonical normalizer are CPU/GPU/replay identical,
    and gauge-only transport preserves pointwise full-S16 state before query
    parity is considered;
32. a gauge-only revision changes no sensor/eye/relation/export query, and any
    mixed state/gauge fault exposes neither half;
33. a fresh all-`ZEmpty` world admits uniquely or gauge-equivalently proved base
    support without a NOVEL/pending/pixel path;
34. exhaustive small-domain evaluation finds no contribution omitted by the
    sparse query-support index, including nonresident and refined regions;
35. missing/stale support summaries fail closed instead of hiding a page;
36. the calibrated optical nuisance law distinguishes broad lighting/exposure
    uncertainty from genuine appearance contradiction;
37. deterministic certificate minimization is equivalent to exhaustive evidence,
    and 10,000 redundant compatible revisits have bounded proof bytes;
38. encoded eviction/rehydration reproduces normalized gauge/state/certificate
    bytes and exact query results in different physical segments;
39. the exact no-change fast path emits no state/gauge delta or page clone and
    meets the stable-revisit cadence gate;
40. direct eye queries meet XR cadence while scan/refinement/page faults are in
    flight.

Live Quest acceptance additionally requires the staged `D_budget` limits
`N3<=80`, `N4<=64`, `N5<=56`, final no-change `<=20`, final ordinary informative
`<=40` and eye pair `<=8`. Final exercised entry points are `<=24` for the warm
scan graph and `<=6` for the eye pair. Timing success cannot waive these gates.
The fixed-query-visible-work segment/world/history negative controls must preserve
both byte-identical results and an identical hot graph-command sequence.

---

# 31. Physical acceptance corpus

## Scan and multi-pass

```text
same wall 1× / 5× / 20×
front plus grazing views
near view-axis false support plus farther real wall
same mould fixture from multiple independent angles
right-eye-only discriminating evidence
two possible sheets in one footprint
two supports remaining ambiguous
two supports producing one common delta
thin board with different front/back appearance
fold and crease
alcove and occlusion
stairs and multi-floor loop
subpixel relief
approach artifact followed by clear pass-through
strong close scan followed by many weak far/grazing scans
weak far scan followed by strong close scan
sharp RGB followed by dim/blurred RGB
poor-light RGB with valid depth
changed illumination with static geometry
inconsistent moving object under one static epoch
observation-order permutations of the same independent evidence
fresh all-ZEmpty world first support without legacy proposal kinds
disconnected support A/B discovered in both orders
10,000 duplicate/weak compatible static revisits after convergence
```

Expected: evidence monotonically preserves or tightens the feasible field; no
false duplication; ambiguity does not mutate; supported detail survives later
weaker views.

## Default, refinement and persistence

```text
unbacked / allocated-ZEmpty / NULL-codec parity
all-default quiescence and mixed default/support boundary semantics
current ZNullDyad full-program negative or positive proof
simple affine field vs high-variation field storage cost
local sampling refinement with exact evidence transport
same-locality contraction without density growth
directional information/certificate transport through refinement
old-field/readout parity before adding higher-frequency information
gauge-only revision atomicity and canonical normalization
uniform versus locally refined chi/kappa representation parity
restart/rehydrate and deterministic replay
long scan beyond resident capacity with transparent eviction
nonresident page contributing to a sensor/eye query
stale/missing query-support summary fail-closed path
clear-data versus stop/sleep durable-lifecycle separation
```

## Dispatch scaling and telemetry

```text
resident segments 1 / 2 / 7 with fixed query-visible pages
small / large total world with identical visible work
short / long revision history with identical visible work
larger query-visible support with the same bounded hot graph
cold refinement followed by an equivalent warm frame
cold SSD fault/rehydrate followed by an equivalent warm frame
machine-readable per-stage timing/count/cardinality table
```

Expected: identical visible work has an identical hot graph-command sequence and
byte-identical result. Larger visible work changes grid/work cardinality, not one
dispatch chain per page. Cold graph commands are separately reason-coded and vanish
after completion. Every staged and final count/entry-point budget passes
independently of timing.

## Eye and export

```text
binocular disparity
head translation/rotation
fold crossing
thin two-sided sheet
sleep/wake/resume
eye readout while scan close/refinement/SSD page fault is active
export after 1 pass and after repeated refinement
opposite-side appearance
export after restart
```

Deleting eye/prediction caches changes no canonical bytes or export result.

---

# 32. S4‑08.6 closure sequence

## N0R — algebra-medium correction

- replace scanner-administration ontology with this specification;
- preserve forensic evidence unchanged;
- freeze one carrier, one S16 field, one generated relation IR;
- runtime diff zero.

## N1R — authoritative Merkaba program

- locate/hash the supplied TOE artifact and freeze query-boundary inputs `I_Q`;
- extend/replace the current closed scalar operator vocabulary only where the
  source-backed TOE/query law requires it, then generate one relation-program IR;
- freeze expression provenance, arities/brackets/couplings;
- freeze the exact `chi/kappa` map/reconstruction family, representation decoder, transport and
  constructive canonical gauge normalizer;
- prove contextual E22 separation or retain direct S16;
- prove `ZEmpty` representation parity and all-default quiescence or block;
- generate forward/reverse/reference plans, including exact directional
  first-hit action and uncertainty/information pullback;
- generate calibrated illumination/exposure/transfer nuisance relations;
- generate conservative query-support summary/bound plans with zero-false-negative
  proof;
- generate exact locality information-form/certificate compression without scalar
  confidence or loss of coupled/disjunctive constraints;
- derive bounded deterministic evidence minimization and the carrier-
  reparameterization theorem against complete `𝔐`;
- no live mutation.

## N2R — non-mutating forward/reverse oracle

- implement query evaluation/reduction and exact reverse contractor;
- preserve complete disjunction in scratch;
- verify whole-query first-hit, coupling and common-delta rules;
- prove pre-hit pressure/mould correction, behind-hit zero action, multi-view
  equilibrium and non-carving;
- prove monotonic directional information/non-degradation, optical-quality
  behaviour under the calibrated nuisance law and exact refinement transport;
- prove gauge-only parity/canonical normalization and base-density
  `ZEmpty -> supported` admission;
- prove sparse query-support completeness against exhaustive resident/nonresident
  small domains;
- prove bounded certificate bytes under duplicate revisit permutations;
- compare against exhaustive small-domain oracle.

## N3R — joint NativeCloseCommit live cutover

- route coherent stereo observations through the generated reverse program;
- use its exact directional action rather than a generic candidate replacement;
- evaluate intrinsic/ZD/nonassoc relations through the same active feasible set
  before any delta is publishable;
- emit and publish resolved/common S16 deltas only after joint closure;
- admit the N1/N2-proved base-density `ZEmpty -> supported` case so a fresh field
  scans without a legacy NOVEL path;
- bind/fingerprint only the uniform `chi_0/kappa_0` representation in N3; adaptive
  regional density begins in N4;
- retain unresolved constraints/evidence in-session without branch identity;
- regenerate the sole frame ABI in this commit and delete every generated
  Candidate/Pending/Continuation/Novel/DirtyEdge symbol;
- delete source-cell builders, proposals, provider×segment evaluation and global
  target sort/reduce in the same commit;
- delete the optical-edge universe, XYZ qualifier, label propagation and separate
  topology/stitch authority in that same commit;
- keep only cold indirect continuation of the identical contractor program;
- any support outside the proved base-density admission remains unresolved; no
  legacy path fills the gap.
- require `D_budget<=80` for the warm ordinary base-density fixture in addition to
  the `1500/1800 ms` recovery ceiling; every named old dispatch/entry point,
  generated ABI path and generator declaration is absent and unregeneratable.

The named-entry absence gate includes exactly:

```text
BuildFrameProposals
BuildDepthSourceCells
BuildRgbSourceCells
EvaluateCandidateMeets
BuildPendingEdges
GatherEdgeEndpoints
ClosePendingEdges
RelaxPendingLabels
```

There is no publication-capable sensor-only intermediate run.

## N4R — durable constraints, exact gauge density and certificate cutover

- implement proven `ZEmpty` backing equivalence and all-default quiescence;
- replace N3 in-session retention with durable/reclaimable unresolved constraint
  records and deterministic exact certificate minimization;
- persist only the minimal exact locality information/coupled-factor certificates
  required for tomography, non-degradation and refinement;
- implement normalized `NativeGaugeDelta` plus exact sampling-density refinement
  of the same field;
- transport state/evidence/relations/information/bandwidth and atomically publish
  state plus gauge generations;
- make `chi/kappa` gauge metadata and fingerprints mandatory carrier/codec ABI;
- implement static false-support correction through exact mould/exclusion closure;
- delete pending projection/retention, global pixel bbox and physical chart logic.
- require `D_budget<=64` for a warm ordinary same-sampling frame; genuine regional
  refinement is separately reason-coded cold work and is absent from the next
  equivalent warm frame.

## N5R — representation-aware sparse commit and durable pager

- feed compact state/gauge deltas directly to touched representation generations;
- retain complete evidence/certificate ownership;
- implement the durable logical page/gauge directory, encoded on-device blob
  backing, bounded decoded GPU residency, asynchronous eviction/rehydration and
  exact clear/restore lifecycle;
- persist conservative nonresident query-support summaries and prove their rebuild;
- root exchange atomically selects gauge/state/certificate/directory and remains
  last;
- delete superseded second mapping/sort/publication orchestration.
- require `D_budget<=56` on the same warm ordinary fixture; changing resident
  segment/page count at fixed visible work leaves the hot sequence unchanged.

## N6R — pure readouts

- eye, prediction, relation, debug and export use generated query plans;
- remove live mesh/halo/readout state that claims continuity or physical authority;
- keep only disposable caches and on-demand rich export;
- meet 72/90 Hz eye-query budgets independently of concurrent scan/refinement/
  page-fault work.
- produce both eyes in `D_budget<=8` with at most 6 exercised GPU entry points;
  instrument and count any raster draws explicitly.

## N7R — hard deletion and physical closure

- gross-delete at least 10000 production LOC versus `cac9ab0`;
- add at most 4000 production LOC;
- net at most -6100 versus `cac9ab0` and -5500 versus `d3b83e1`;
- zero legacy/fallback symbols;
- exact oracle/Vulkan/Release gates;
- recovery ceiling, stable no-change, ordinary informative-update and XR-eye
  performance contracts all pass;
- final hot dispatch gates pass independently: no-change `<=20`, ordinary
  informative `<=40`, eye pair `<=8`, scan entry points `<=24`, eye entry points
  `<=6`, with no serial generic-interpreter evasion;
- every Quest capture contains machine-readable compute/draw/total-dispatch,
  hot/cold, timing,
  cardinality, residency and fingerprint telemetry; scaling negative controls
  preserve the hot sequence at fixed visible work;
- long scan exceeds decoded residency without world-size fault;
- 10,000 redundant revisits have bounded evidence/certificate growth;
- source archive, APK and Quest corpus from the identical commit;
- stop before S4‑09.

Every accepted run is one commit with regenerated code graph and current controls.

---

# 33. Definition of done

S4‑08 is complete only when all of the following are true:

```text
Psi : Sigma_2 -> S16 is the sole physical world
Sigma_2 is one carrier/gauge, not an object/chart database
one authoritative generated relation IR owns every physical query
ZEmpty has complete-program proof, all-default quiescence and transparent backing
forward and reverse use the same bracketed expressions
disjunctive preimages remain scratch/constraints, never entities
complete-program coupling governs hidden-mode preservation
first-hit is whole-query reduction
exact directional reverse action corrects pre-hit artefacts and is zero behind hit
locality feasible sets/information become monotonically tighter, never scalar confidence
native relation is a query, not topology state
chi/kappa is exact representation gauge/reconstruction, never a physical field
gauge-equivalent support normalizes independently of discovery/allocation order
refinement increases sampling of the same Psi and publishes gauge/state atomically
refinement transports exact directional information and reproduces old readouts first
fresh ZEmpty world admits proved support without legacy proposal kinds
only resolved/common full-S16 state plus proved representation deltas publish root-last
query-support culling has a generated zero-false-negative proof
durable SSD backing makes decoded residency independent of logical world size
certificate growth follows new/unresolved information, not revisit count
optical nuisance law handles calibrated illumination/sensor transfer without hiding contradictions
eyes/prediction/export/debug are pure readouts
storage and execution decomposition are physically invisible
hot GPU submission graph is bounded and invariant to world/segment/history size
dispatch budgets 80 -> 64 -> 56 -> final 40/20/8 pass independently of timing
final warm scan/eye entry-point limits 24/6 pass without a serial interpreter
no-change, informative-update, eye cadence, negative-LOC and device gates pass
```

The final architecture is:

```text
                         ONE NATIVE MEDIUM
                         Psi : Sigma_2 -> S16
                                  |
                  exact normalized representation
                      (chi, kappa, Psi-hat)
                                  |
                    one generated Merkaba program M
                       /          |          \
                      /           |           \
        forward sensor/eye   intrinsic relation   export/debug
                shadows             query             shadows
                      \           |           /
                       exact reverse constraints
                                  |
                     one NativeCloseCommit
                                  |
                sparse state + proved gauge deltas
                                  |
              root-last representation publication
                                  |
          durable directory/codec <-> bounded GPU residency
```

The core rule is:

\[
\boxed{
\textbf{There is no scanner ontology: only one S16 medium and operations on it.}
}
\]
