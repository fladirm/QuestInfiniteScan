# FinalScan RISK REGISTER

C00 deliverable. Authority: `FINALSCAN_V4_CONTRACT.md`. Every risk names the evidence that raised it, the contract section it threatens, the cut that mitigates it, and the acceptance that closes it. A risk is CLOSED only by a device receipt or an official document, never by reasoning.

Evidence keys: `SS` = SimpleScan audit (`prace/simplescan`, branch `fix/merkaba-runtime-root-causes`); `US` = UniScan audit (`prace/uniscan`, `refactor/m8-dual-sphere-flower-rev-b`, HEAD `9cfed011`); `OS` = OtherScan (`prace/otherscan`, `forensic/n4r-cut-e-scheduler`); `DEV` = device logs in `m16scanner-device-evidence`, `simplescan-evidence/d1-20260830`, `otherscan/evidence`; `WEB` = `SOTA_RESEARCH_LEDGER.md`.

| # | Risk | Evidence | Contract impact | Mitigation cut | Closing acceptance | Status |
|---|---|---|---|---|---|---|
| R1 | Native Camera2 NDK access to PCA (IDs 50/51) with `HEADSET_CAMERA` may be refused or deliver no usable AHardwareBuffer; zero-copy ingest becomes impossible | DEV: `PcaCameraOpenXR::isSupported XR_ERROR_FUNCTION_UNSUPPORTED`, delivery via `Camera2Tracker: Camera2 50 is opened`; WEB A/D | §5 ingest, §3.2 profiles | C01-A spike, C03 adapter | C01 receipt: native path opens both cameras, delivers AHB frames, latency measured vs MRUK; else MRUK adapter accepted with copy-in-callback | OPEN |
| R2 | `SENSOR_INFO_TIMESTAMP_SOURCE` may be UNKNOWN; camera→XrTime mapping unprovable | WEB A (Android CaptureResult semantics); SS `SensorClockMapper` bracket hack | §4.2 clock gate, §4.1 | C01 probe, C03 gate A/B/C | C01 receipt of TIMESTAMP_SOURCE value + measured jitter; C03 telemetry `clockGatePath` never reports "exact" without gate A/B | OPEN |
| R3 | OpenXR guarantees only ≥50 ms pose history; PCA latency 20–40 ms (up to 60 ms) consumes most of it | WEB C (OpenXR 1.1 §Time); WEB A latency | §4.3 pose ring | C03 own pose ring + locate on camera callback | C01 reprojection test at fast yaw: managed `GetCameraPose` vs native `xrLocateSpace` error < 2 px; C03 ring depth ≥ 2 s | OPEN |
| R4 | Rolling shutter of PCA cameras undocumented; fast yaw smears static geometry | WEB A (no source); contract §68 of v3 | §4.5, §21.1 | C01 row-time measurement, C03 angular-velocity gate | C01 receipt: row time or global-shutter proof; C21 acceptance "rapid head movement does not smear" | OPEN |
| R5 | Distortion model undocumented; pinhole-only intrinsics may leave residual that breaks epipolar rectification | WEB A (MRUK Intrinsics = pinhole; no distortion coefficients) | §4.6 rectification, §7.2 stereo | C01 checkerboard/reprojection test, C10 | C01 residual < 0.5 px or own correction fitted; C10 rectified epipolar error < 0.3 px | OPEN |
| R6 | Second VkQueue in family 0 may time-slice, not overlap (Adreno 7xx LPAC mapping unknown); Unity reports `supportsAsyncCompute=False` | DEV: family 0 = G\|C\|T ×4, no compute-only family; OS `injected=1 result=0`; WEB E (chipsandcheese, Khronos async sample) | §15.2, §21.7 | C01 overlap measurement, C02 scheduler, C30 | C01 receipt: timestamps of both queues on one axis show overlap or not; architecture accepted either way with single-queue bounded fallback | OPEN |
| R7 | Low-light drops sensor cadence to 50 Hz; `NORMAL_60` cannot be sustained | DEV: `nominalRateHz=50`, `images: color 50.0/50.0` | §3.2 profiles | C01, C03 LOWLIGHT profile | C01 receipt of app-side fps in low light per profile; C03 hysteresis transition logged | OPEN |
| R8 | Thermal throttling degrades GPU clock / frame pacing over 15–60 min | WEB A (Laghari 2025); DEV GPU pinned 456 MHz, MRSS timeouts | §21.9, §3.2 THERMAL_30 | C01 15-min profile, C31 | 60-min run: readout stays 72 Hz, only acquisition profile degrades | OPEN |
| R9 | Working memory budget is ~3.5 GB, not "8 GB" | WEB F (5.75 GiB PSS limit); DEV m16 PSS 1.73–1.87 GB baseline | §16 governor | C01 per-component PSS, C15 | C31: 1-h run in bounded steady regime under watermark | OPEN |
| R10 | Graphics PSS 1.33–1.43 GB immediately after XR start (Unity + passthrough + PCA) leaves little for atlas | DEV m16 meminfo; OS GPU 2085 MiB 100 % allocated | §16, §14 | C01, C15, C17 | C01 receipt per component; C17 resident tile budget enforced | OPEN |
| R11 | Monolithic SPIR-V fails Adreno link (-13) or takes minutes; drain kills process | US `lasttrue.md:8282-8306, 8709`, `8e2b760`, `d635c22` | §15.6 kernel envelope | C01 link-envelope benchmark, C02 build gate | Gate enforced fail-closed; C01 receipt of largest SPIR-V linking < 1 s / 5 s | OPEN |
| R12 | Pipeline binaries invalidated on every Horizon OS/driver update; forced extensions crash device creation | US `MerkabaPipelineBinary.h:76-138`, cpp:883-884; WEB H | §15.7 | C02 cache fallback + warm-up, C30 | Binary mismatch → `VkPipelineCache`/SPIR-V warm-up path exercised on device; no scan-time compile spike | OPEN |
| R13 | Unity 6000.5 render-pass semantics (`subPassIndex=0` with null renderPass, `stride=0`) break any native render-pass intercept | US `47a223d`, `7ec07b0` | §13.1 variant A default | C05 | C05: one Unity-issued `DrawProceduralIndirect`, no intercept; measured on synthetic world | OPEN |
| R14 | `DrawProceduralIndirect` under single-pass instanced stereo needs doubled instance count and multiview-correct shader | SS `f4970df`; WEB D | §13.1 | C05 | Both eyes render identical surfel set; SPI instance doubling verified by counter | OPEN |
| R15 | Env Depth double acquire per frame → `XR_ERROR_LIMIT_REACHED`; subsystem dead after resume | DEV SimpleScan log line ~49894; SS `b34910b` | §6, §19 | C09, C21 | C21 acceptance: sleep/wake cycle ×10 with depth cadence restored, single acquire counter | OPEN |
| R16 | Camera HAL emits non-monotonic timestamps and drops late color sets | DEV `timestamp N is not increasing` ×44; `92 late Color frame set were dropped` | §4.4 pairing | C03 | Telemetry `timestampNonMonotonic` counted and frames rejected; pairing acceptance > 90 % | OPEN |
| R17 | MRSS passthrough watchdog stalls (960 ms) and `TimeWarp::RetireFrame wait sync timed out` under load | DEV m16 logcat ×32 / ×91 | §3.1 render, §15.4 quanta | C02, C30 | Long run shows zero watchdog expirations attributable to FinalScan submits | OPEN |
| R18 | `/mnt/aidisk` is 99 % full (5.3 GB free): repo growth or a Library folder on aidisk breaks the machine | `df -h` 2026-09-14 | §0 clean repo | C00 (this run) | `.gitignore` excludes Library/Temp/APK/.so; host project and builds live on `/mnt/kingston-unity`; repo < 50 MB | OPEN |
| R19 | Two agents editing one checkout corrupt state | SS `.claude/MERKABA_RISKS.md` RISK-5 | §2.9 of review, process | C00 rule | One writer per checkout; forks write disjoint files only | OPEN |
| R20 | Source-text tests (ReadAllText + IndexOf) give false confidence | SS RISK-4 (13 of 17 test files) | §2.9 host tests | C08 host Vulkan tests, every cut | Behavioural tests on Linux host for hash/association/split-merge/free-space; no source-text assertions | OPEN |
| R21 | Performance claims without device timestamps | US `7ec07b0` retraction; SS session with 0 integration invocations and HUD "Active" | §20 device receipt rule | every cut C09–C13 | No cut accepted without GPU timestamps on the Unity frame axis | OPEN |
| R22 | Meta OpenXR plugin registers its own Vulkan interceptor; wrong priority disables ours or theirs | WEB D (WebRTC issue #686) | §15.2 | C02 | C01/C02 receipt: both interceptors run; passthrough + our device patch both active | OPEN |
| R23 | Android crash with `AddInterceptInitialization` + Optimized Frame Pacing | WEB D (Unity issue tracker) | §15.2 | C02 | Frame pacing setting fixed and documented; no crash over C21 lifecycle tests | OPEN |
| R24 | Appearance data explodes memory/storage (keyframes, decoded images, atlas tiles) | contract §14/§16; OS GPU 100 % allocated | §14, §16 | C16–C17, C15 | Stationary camera produces zero storage growth; resident tile budget bounded in 1-h run | OPEN |
| R25 | Hash tombstone growth and rehash stalls | contract §9.1; WEB H | §9 | C08 | Stress test: 1 M insert/delete cycles, bounded probe count, no frame stall, tombstone compaction measured | OPEN |
| R26 | Thin-wall two-sheet association fails on 320² depth prior quality | WEB B (edges unreliable); contract §8.3 | §8.3, §21.4 | C12 | Geometry acceptance: partition thickness within 10 mm, zero false merges both sides | OPEN |
| R27 | Loop-closure correction causes a one-frame world jump | contract §10 | §10 presentation smoothing | C20 | User test: correction > 5 cm interpolated over ≤ 250 ms, canonical applied immediately | OPEN |
| R28 | Export mesh from adaptive surfels (holes, T-junctions, two-sheet walls) | SS RISK-2 cracks 19 mm at σ=5 mm on old membrane | §18.5 | C27–C28 | Watertight where surfel coverage is complete; thin walls export as two faces with correct normals; GLB validates | OPEN |
| R29 | PCA 1280×1280 vs 1280×960 changes intrinsics/FOV; hard-coded 4:3 breaks | WEB A (v83 known issue) | §3.2, §4.6 | C01, C03 | Intrinsics read per frame from runtime; both resolutions pass reprojection test | OPEN |
| R30 | `GraphicsFence`/`AsyncQueueSynchronisation` unsupported on Quest; per-frame exceptions | US `57ab534`; DEV m16 1567×/12 s | §15.3 | C02 | C# side uses async readback tokens or timeline-semaphore poll only; zero `NotSupportedException` in logs | OPEN |
| R31 | One global native job slot serializes scan/readout (donor 6.2 Hz) | SS/US `_activeJob`, readout 42–99 ms wall | §15.4 scheduler | C02 | Scan ≥ 2 in flight, publish micro-jobs, measured cadence ≥ 15 Hz with 72 Hz readout | OPEN |
| R32 | Exposure/ISO metadata not exposed; atlas radiometric normalization cannot rely on them | WEB A [UNVERIFIED] | §14 | C17 | Normalization uses overlap-based gain estimation; passes textured-wall consistency test | OPEN |

## Process rules derived from this register

* R18: run `du -sh` on the repo before every commit; never create `Library/` under `/mnt/aidisk`.
* R19: forks write disjoint files; the parent is the only writer of shared files.
* R21: every performance statement in any ledger carries a log path or is labelled *provisional*.
