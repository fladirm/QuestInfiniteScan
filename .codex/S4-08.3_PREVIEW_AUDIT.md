# S4-08.3 current-device forensic checkpoint

This is the sole execution checkpoint for this closed audit milestone. Canonical
authority remains `new_spec.md`; complete evidence and reasoning are in
`s4-083-audit.md`.

## Frozen invariants

- The only canonical world is exact `Psi : Sigma_2 -> S16`.
- Page/block/bundle/raw/proof limits are execution windows, never evidence caps.
- Page layout, prediction, association and XR preview have no physical authority.
- No XYZ identity, CPU canonical control/readback, FP mutation, dropped evidence,
  weakened S16 algebra or parallel geometry world is permitted.
- Audited/deployed runtime is commit
  `c2720b51637643bb04eef16894d7dd9bf9702720`.
- The dirty telemetry patch is post-deployment diagnostics and cannot explain the
  captured run.

## DONE milestones — never reopen during packaging

### A0 live evidence

- Captured current device logs, direct motion video/frames, wake/relocalization pair
  and five manual Quest screenshots from the second run.
- Excluded every legacy `com.fladirmacht.voxelscanner-*` artifact.

### A1 publication/readout

- Renderer draws every manifest-current page: publication 27 equals
  `663552 / 24576 = 27` drawn pages; the first run independently gives 28.
- The board is canonical current coverage, not hidden room geometry or a readout cap.
- Wake displacement realigns without publication; tracking recovery is separate
  from the persistent tilted-board acquisition defect.

### A2 page identity

- Live 320 x 320 depth becomes 10 x 10 32-pixel blocks and exactly 5 x 5
  64-pixel gauge pages.
- Screenshots show that exact 25-page board. Pages 26 and 27 restart over the first
  footprint with new hues/logical coordinates; they are not newer generations.

### A3 prediction/association/ingress

- Prediction renders both surface and supported-contact footprint passes.
- A frame may produce MATCHED work and fresh unmatched GAUGE blocks concurrently.
- Every surviving unmatched gauge candidate receives a new monotonic Morton logical
  coordinate before pending/compatible candidate identity is resolved.
- The 64 resident bundle slots fill in roughly three all-unmatched frames.
- Unselected frame candidates increment skip/exhaustion but are not immutably sealed
  before capture release; side/rear evidence is therefore lost.

### A3.1 proof/raw liveness

- Raw tiles 0..4095 are transient bundle storage; durable proof allocation starts at
  4096 and monotonically consumes 4096..8191 without reuse or segmentation.
- After exactly 4096 closed blocks, the next `EMIT_RAW` cannot allocate. Source
  cursor/state do not advance and one proof owner repeats forever.
- Device fingerprint is `diag.proof=[4097,4096,0,0]` with growing allocation-failure
  telemetry and frozen publication.
- Separate latent violation: `PROOF_SPILLED` at the 12-record execution window has
  no production continuation consumer.

### A4 scheduler/load

- Current HEAD refills deterministic deficits in all eight rounds; earlier
  round-zero-only analysis is stale.
- Static costs are models, not GPU timings. Deployed profiler recorders returned
  zero blocks, while KGSL was about 91 percent busy at 456 MHz.
- Duplicate preview cost grows linearly: 28 pages submit 688128 billboard vertices
  per XR frame. No blind budget increase is authorized.

### A5 causal closure

- Final causal graph and minimal ontology-preserving repair are closed in
  `s4-083-audit.md`.
- Confirmed chain: stale/unmatched association -> fresh 5 x 5 gauge identities ->
  64-slot unsealed ingress loss -> 4096-block proof/raw hard stall -> no side/rear
  publication; renderer faithfully draws the resulting board.

## CURRENT exact task

Package and publish this audit checkpoint only. Do not modify reconstruction
runtime, build an APK, tune budgets or open S4-09.

## CURRENT files

- `s4-083-audit.md`
- `.codex/S4-08.3_PREVIEW_AUDIT.md`
- `.codex/{STATE.md,TASK_DAG.json,SESSION_TAIL.md,CODE_GRAPH.json}`
- `docs/architecture/CODE_GRAPH.md`
- diagnostic-only `SigmaDiagnosticTelemetry.cs`, `SigmaGpuKernelTelemetry.cs` and
  `SigmaStreamingContractTests.cs`

## NEXT milestones

1. Commit the audited report, checkpoint, controls and diagnostic-only follow-up.
2. Create a source-only `git archive` from that exact commit.
3. Build one forensic bundle under `~/Stažené` containing current logs,
   screenshots/video/frames, wake evidence, report, manifest and source archive.
4. Push the exact checkpoint to `origin/prism`.

## Last green gates

- Unity Vulkan EditMode: 73/73 passed on 2026-08-24.
- Generated Sigma operators: current.
- Quest compute UAV validation: passed, limit 8.
- Code graph/control validation: current, active node S4-08.
- `git diff --check`: clean.

## Genuinely unresolved for the next repair/diagnostic build

- Per-reason same-first-hit miss distribution and exact pending/visible compatibility.
- Real per-kernel and raster GPU timings in Release; zero recorder blocks are an
  instrumentation fault, not evidence of idle GPU.
- Repair implementation for pending-or-published association, seal-before-admit,
  reclaimable/segmented raw retention and lossless proof continuation.
