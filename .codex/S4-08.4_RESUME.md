# S4-08.4 direct-frame execution checkpoint

After compaction:

1. read this file;
2. run `git status --short && git diff --stat`;
3. open only CURRENT files/functions below;
4. continue the CURRENT task immediately.

Do not reread old S4-08.x plans/audits or reconstruct the old streaming graph.
Consult `new_spec.md` only for one named unresolved contract and only its section.

Authority: `new_spec.md` v7; `.codex/S4-08.4_DIRECT_FRAME_PLAN.md`; active S4-08.
S4-09 is unopened. `Psi : Sigma_2 -> S16` is the sole canonical world.

## FROZEN INVARIANTS

- Whole coherent RGB_L/R + DEPTH_L/R is one observation/revision boundary.
- Four sources remain independent through exact Q16.48 cell construction and meet.
- First-hit/post-hit, validity, gaps, provenance, mass and topology rules do not
  change from the exact retained primitives.
- Existing/pending/continuation/novel are proposals; exact meet decides identity.
- Pending gauges have local UV/evidence only, no canonical address or world.
- Canonical extent allocation occurs only at promotion in v7 section 18.7 order.
- Page/tile/workgroup/proof-window/storage layout cannot affect canonical bytes.
- Complete evidence is lossless; no 4/8/12/64/4096 capacity is a canonical cap.
- Transition closure touches only incident claimed edges; UNOBSERVED makes no claim.
- One frame revision root exposes all changed page generations atomically.
- GPU owns pixel/inverse/transition/scatter/readout; CPU owns resources/fences only.
- Telemetry/readback is diagnostic and never schedules or mutates.
- New/changed shaders are complete-file implementations after full-file ABI/bind
  review. Never hotfix an old streaming shader.
- No old/new fallback, compatibility switch, CPU canonical path or second world.

## DONE — NEVER REOPEN WITHOUT A PROVEN REGRESSION

- S4-00 through S4-07 and their exact algebra/carrier/readout foundations.
- Four-source inverse math, proof/topology/gauge math and S4-08 pose/SE(3).
- Device forensic closure proving 5x5 tile identity, duplicate Morton gauges,
  64-bundle evidence loss, 4096 raw stall and invalid Release profiler.
- S4-08.3 transaction/bundle/token architecture is superseded. Do not repair,
  rename, wrap or use it as an implementation template.
- M0 normative/control design is frozen in the direct-frame plan.
- M0 gates are green: DAG JSON parses, `git diff --check` passes, generated code
  graph is current, and control validation reports active S4-08.
- M1 generated ABI/resources are complete: seven generated records, whole-frame
  four-source segmented SoA journals, generation-safe leases and profile-backed
  growth with no truncation. Vulkan EditMode is 76/76.

## CURRENT EXACT TASK — M2

Create complete `SigmaFrameInverse.compute` and the source/proposal/resolve portion
of `SigmaFrameGraph.cs`. One captured coherent fixture must produce four independent
exact source cells and frame outcomes without mutating the carrier.

The shader is a complete new file. Read each retained include in full before use.
No old stream shader is edited or copied as a lifecycle template. GPU decomposition
must be coordinate-major and parity-invariant; exact meet alone accepts identity.

## CURRENT FILES / FUNCTIONS TO OPEN

- Plan section 8 A–E only.
- New `SigmaFrameAbi.hlsl`, `SigmaFrameResources.cs` and generated frame layout.
- Full `Sedenion16.hlsl`, `SigmaInverseMath.hlsl`, `SigmaRgbInverseMath.hlsl`,
  `SigmaPoseConsume.hlsl`, `SigmaInverseAbi.hlsl` and carrier ABI.
- Old source/inverse shaders only for locating already-accepted exact helper calls;
  do not port block/page/bundle control flow.
- Host frame/prediction lease contracts and exact calibration upload functions.

## NEXT

- M3 closure/publish/pending/journal/dirty edges + parity; commit.
- M4 hard cutover, delete old graph, profiler + no-reference gate; commit.
- M5 temporary world readout/lifecycle; commit.
- M6 full gates, accepted commit, archive, Release APK/install; stop before S4-09.

## DELETION ORACLE AT M4

All paths in plan section 4 plus `.meta` files are absent. Compiled Runtime/tests
contain no `SigmaStreamingGraph`, `SigmaStreamingResources`, stream transaction,
bundle, Morton gauge allocation, token scheduler, proof owner or old streaming ABI.

Current removable lifecycle ~10,230 LOC. Require net deletion >=5,500 production
LOC; additions >4,200 trigger stop-and-simplify review.

## LAST GREEN EVIDENCE

- M0 commit: `89188a0`.
- Unity Vulkan EditMode 76/76; generated check/diff and 8-UAV ABI fixture green.
- Device evidence is negative acceptance: 5x5 page board, duplicate gauges, lost
  side/rear frames and proof freeze at `[4097,4096,0,0]`.

## GENUINELY UNRESOLVED

- S4-10 supplies durable flash overflow for week/city-scale backlog. S4-08.4 must
  own complete frames and sustain the three-minute device gate without loss, but it
  must not falsely claim infinite retention from finite RAM.
- Exact Quest Release per-kernel timestamp support is unproven. M4 uses
  `ProfilerRecorder` GPU markers and reports unsupported capability explicitly.
- No other architectural choice is open. If implementation appears to require a
  transaction, tile identity, page closure or new world, stop: the lowering is wrong.
