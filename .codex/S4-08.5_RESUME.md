# S4-08.5 sole execution checkpoint

After the mandatory `AGENTS.md` resume reads, use this cursor and do not reconstruct
the completed repair history.

## Frozen invariants

- One canonical world: exact `Psi : Sigma_2 -> S16`.
- Windows/pages/segments/workgroups are execution/storage only.
- Do not redesign or reopen R1-R4 during this device repair.
- Illegal physical dispatch dimensions fail closed before command execution.
- Device root/publication/readout zero after an execution fault is invalid evidence,
  never an exact-inverse no-change result.
- No retired transaction/bundle/Morton/token/proof-owner graph or fallback.
- Device acceptance is required; source compilation cannot close S4-08.

## DONE — never reopen

- R1 complete four-source materialization and normalized target identity.
- R2 stable full-target grouping and exact per-target reduction.
- R3 exact first-hit/four-source/S16 closure and incident-only deferral.
- R4 direct root-last publication/evidence lifecycle source cut at `87d33f2`.
- Device forensics for `87d33f2` captured three illegal direct dispatches per
  320x320 frame: `ReduceTargetWindow=102400`, `ClosePendingEdges=204800`,
  `PersistPendingTargets=102400`; root/R4/draw remained zero.
- Binding/storage windows are again derived solely from the legal Vulkan binding
  range: production 320x320 uses one 102400-footprint window. Only the three
  one-group-per-item kernels use two-dimensional linear dispatch grids:
  `ReduceTargetWindow=51200x2`, `ClosePendingEdges=51200x4` and
  `PersistPendingTargets=51200x2`.
- Both production direct-dispatch wrappers validate every dimension in `1..65535`
  before recording/executing a command.
- The large-binding 320x320 test records the actual source, reduction, closure and
  publication graph; Vulkan EditMode passes 65/65 and Quest eight-UAV validation
  passes.
- Host completion is truthful: PUBLISHED alone retains evidence and increments
  committed frames; legal NO_CHANGE releases without either action; shader fault,
  incomplete post-fence state and stale revision return/latch Faulted. Vulkan
  EditMode passes 66/66 without an HLSL/R3/R4 change.
- GPU sampler/recorder instrumentation is disabled by default in non-development
  Release. Dispatch wrappers retain the same validation, kernel IDs, dimensions
  and order and call Unity dispatch directly; profiling remains available only in
  Editor, Development or explicit `SIGMA_GPU_KERNEL_PROFILING` builds.
- Accepted CONTINUATION targets freeze their CURRENT anchor ordinal in
  `target.Candidate.x` during `FinalizeExactClosure`; `MapFrameTargets` never
  rereads `_PendingLinks`, which remains exclusively mutable component/retention
  scratch. No ABI, buffer, closure classification or publication order changed.
- Commit `63ca337` published roots 1, 2, 4, 6 and 7 without `0x104`; revision 8
  then required 37 new page pairs with only 29 resident pairs free and correctly
  failed closed as `0x20` plus downstream unmapped `0x100`. Capture continued but
  the truthful host latched the `0x120` completion fault.
- Decoded carrier residency is now 1024 MiB by default: 2048 physical pages / 1024
  current-shadow pairs. Segment size is the largest legal even page count allowed
  by the Vulkan binding range, capped at 256 pages, rather than a fixed 32 MiB.
- Published and same-frame corrected predictions use independent four-slot rings;
  corrected saturation is recoverable FIFO backpressure and cannot latch a fault.
- Published evidence performs the existing generation-safe transfer/recycle cycle
  immediately after root proof, so source storage is bounded by active ownership.
- Both 131072-record target sorts use the existing exact 256-record block/stage/tail
  circuit (55 dispatches each); full GPU output equals the 153-stage reference.
- Temporary XR preview alone is half-density, translucent and stable-coloured by
  logical page coordinate. Prediction and canonical readout remain full-density.
- Per-kernel GPU timestamps are an explicit one-submission sample: normal frames
  have no markers; sampled output is ranked by kernel and uses no async readback.

## CURRENT exact task

Do not edit R3/R4 or add another fix. In order:

1. regenerate/validate the code graph and controls;
2. commit the narrow liveness/RAM/sort/preview/profiler cut;
3. push the exact commit;
4. create a source-only `git archive` from that commit;
5. build the Android/Vulkan Release APK from that same commit;
6. install it on the connected Quest and stop.

## NEXT gate

Collect the first new device evidence. Before any further S4-08 work it must show:

```text
published root > 0
published revision > 0
changed page > 0
draw > 0
root advances beyond 13 without corrected-ring fatal or 0x20/0x120
one ranked `Sigma gpu-kernel` sample identifies actual compute costs
resident source storage does not grow by one 131.25 MiB journal per revision
```

Any direct-dispatch error or zero result keeps S4-08 red. S4-09 remains blocked.

## Last green gates

- Unity 6000.5.9f1 Vulkan EditMode: 71/71.
- `SegmentSizingRespectsBindingLimitAndGenerationPairs`: PASS; 1 GiB is 2048
  physical pages / 1024 pairs and binding-safe decomposition is 8-9 segments.
- `Production320FrameLargeBindingRecordsOnlyLegalDispatches`: PASS.
- `ExecutionWindowsAndDirectDispatchesRespectVulkanDimensions`: PASS.
- `TargetReductionIsExactPermutationAndWindowInvariant`: PASS for one/four
  binding windows with bit-identical complete reduction output.
- `BlockBitonicSortIsBitIdenticalToGlobalReferenceCircuit`: PASS over the full
  131072-record production sort capacity.
- `PredictionRingBusyIsRecoverableBackpressure`: PASS.
- `PublishedEvidenceRecyclesWithoutRevisionScaledResidency`: PASS for 32
  sequential published revisions with one recycled source storage identity.
- `FrameCompletionDispositionIsTruthfulAndFailClosed`: PASS.
- `ProfilingDisabledPreservesProductionDispatchContract`: PASS; identical kernel
  IDs/order/dimensions, zero registered sampler entries while disabled.
- `PendingRetentionCannotOverwriteContinuationAnchor`: PASS; retention mutates the
  root link while publication retains the original CURRENT anchor and clears
  `0x004/0x100`.
- Quest eight-UAV validator: PASS.
- `git diff --check`: PASS.
