# S4-08.5 sole execution checkpoint

After mandatory `AGENTS.md` resume-order reads, use only this execution cursor,
`git status --short` and `git diff --stat`. Do not reconstruct completed audit.

## MODE

**IMPLEMENTATION — negative-LOC R3/R4 replacement.**

`refact.md` is the completed forensic record. Do not reopen its audit unless a
named implementation contradiction requires one exact section.

## Frozen invariants

- One canonical world: exact `Psi : Sigma_2 -> S16`.
- One coherent frame is one complete owned observation/revision.
- Windows/pages/segments/workgroups are execution/storage only.
- Image/XYZ may propose work; only exact four-source/first-hit/S16 semantics decide.
- Complete D_L/D_R/RGB_L/RGB_R evidence is retained once per observation.
- Revisions/generations hold generation-safe references; no per-page evidence copy.
- A frame slot is reusable after complete evidence ownership transfer, independently
  of later deterministic proof minimization.
- Pages are backing only; one root selects one immutable complete revision view.
- No fixed scratch/pool/session cap may truncate or stall canonical evidence.
- No retired transaction/bundle/Morton/token/proof-owner graph or fallback.
- Shader changes are complete-file replacements after complete manual reads.
- Final production LOC is negative against `d3b83e1`.

## DONE — never reopen after compaction

- R1 complete four-source materialization and normalized target identity.
- R2 stable full-target grouping and exact per-target reduction.
- R3 complete-file exact closure replacement accepted: optical adjacency only
  proposes work; exact first-hit source cells qualify claims; generated S16
  annihilator/associator closure decides transitions; unresolved claimed edges
  defer only incident changed mutations before any pending union.
- R3 Vulkan/EditMode gate passes 64/64 with no shader error or missing binding;
  full-S16 oracle, separated-cell NO_CLAIM and cross-window fixtures are green.
- Full R3/R4 forensic audit in `refact.md`.
- Old ConstraintLedger live graph confirmed absent; it must not be restored.
- `SigmaTopologyMath.hlsl`, generated topology tables, `SigmaFrameAbi.hlsl`,
  `SigmaFrameInverse.compute` and all of `SigmaFrameClosure.compute` manually read.
- Dirty `SigmaFramePublish.compute` manually read completely and rejected as the
  final R4 design: it duplicates lossy evidence per page and mutates visibility
  before its root.

## CURRENT exact task — R4 replacement

Open only:

```text
Runtime/Resources/SigmaPrism/SigmaFramePublish.compute
Runtime/SigmaPrism/SigmaFrameGraph.cs
Runtime/SigmaPrism/SigmaFrameResources.cs
Runtime/SigmaPrism/SigmaCarrier.cs
Tests/Editor/SigmaFrameInverseTests.cs
```

Replace the rejected dirty R4 cut, do not extend it:

1. retain the complete four-source journal once per observation in generation-safe
   segmented ownership; reduced joint cells remain only witness/cache;
2. release the owned frame immediately after that transfer, never after proof
   minimization and never while a revision points into recyclable frame scratch;
3. consume only ACCEPTED+CHANGED+non-deferred R3 targets;
4. update CURRENT in its source segment and allocate exact promoted extents without
   image/page/XYZ identity;
5. scatter paired reclaimable shadow generations without fixed session cliffs;
6. build an inactive immutable multi-segment view, then expose it by one final root
   exchange; every prediction/inverse/readout consumer resolves through that root;
7. delete page-owned evidence copies, pre-root CurrentFlags mutation and duplicate
   publication orchestration while replacing the complete shader/host cut.

R4 gate: two readable segments update correctly, UNCHANGED emits nothing, complete
evidence survives frame-slot reuse, faults leave the old root byte-identical, and a
multi-page revision becomes visible by exactly one root flip.

## NEXT

1. R4: replace publication/resource/host cut as one coherent ownership design:
   observation-owned segmented journal, CHANGED-only shadow scatter, reclaimable
   paired pages, inactive immutable visibility view and final root exchange.
2. Bind prediction/inverse/readout exclusively through the root-selected view.
3. R5: delete obsolete dirty-R4 buffers/kernels/orchestration and audit no legacy,
   no fallback, no duplicate lifecycle; production LOC must be negative.
4. Run generated, Vulkan bind/UAV, exact parity and full EditMode gates; regenerate
   code graph and validate controls at each accepted checkpoint.
5. Commit final S4-08 candidate, `git archive` that commit, build/install its Release
   APK, run the three-minute Quest gate, then stop before S4-09.

## Last trustworthy gates

- HEAD entering R3: `fd0fee1`; R1/R2 exact target reduction fixtures green.
- R3 working tree gate: Vulkan/EditMode 64/64, `git diff --check` green.
- Earlier generated/eight-UAV/source compiler gates green.
- Dirty R4 remains rejected and has no accepted compile/device gate.
- Device/S4-08 acceptance remains negative; S4-09 stays blocked.
