# S4-08.5 sole execution checkpoint

Read this file after compaction, then only `git status --short`, `git diff --stat`
and the CURRENT files/functions below. Do not reconstruct prior runs or reread broad
controls unless one named unresolved contract question requires a specific section.

## Frozen invariants

- One canonical world: exact `Psi : Sigma_2 -> S16`; no fallback or second graph.
- One coherent frame is one complete owned observation/revision.
- Windows/pages/segments/workgroups are execution/storage only.
- All records for one target reduce exactly before one final state is built.
- Pending/continuation/current/novel are proposals; exact four-source/first-hit/S16
  closure decides identity.
- Claimed unresolved edges defer only incident changes to fixed point.
- Evidence/certificates/raw and revision/page handles are generation-safe before
  publication/reuse.
- Shader changes are complete-file replacements after full-file/call-site review.
- Final production LOC is negative against `d3b83e1`; no disabled legacy remains.

## DONE — never reopen without regression evidence

- S4-08.4 removed the old transaction/bundle/Morton/token/proof-owner graph.
- Exact Q16.48/S16 operators, four-source cell mathematics, pose correction,
  first-hit semantics, carrier codec, topology algebra and geometry readout remain.
- MRUK room-scene dependency is removed; OVR spatial-anchor localization remains.
- Device root cause: first 320x320 submission throws from `Single()` on a legal
  segmented buffer, then `_completionFaulted` stops all inverse work.
- Audit confirms gaps G1-G14 in `S4-08.5_DIRECT_FRAME_CLOSURE_PLAN.md`.
- R1 records all source/proposal/meet/compact work over one aligned footprint
  window decomposition; no source record is truncated or processed twice.
- R1 target records preserve coordinate plus segment/page/generation/sample;
  packed right-eye identity is removed from the sample address.
- Vulkan `SigmaFrameInverseTests` passes 4/4, including a real 320x320 frame over
  seven binding windows and two distinct normalized CURRENT segment targets.
- Vulkan `SigmaFrameResourceTests` passes 3/3.
- R2 stable-orders the complete normalized target stream by full
  coordinate/segment/page/generation/sample/source key, reduces every equal range
  across all execution windows by exact max/min with validity, gaps and source
  provenance, and constructs exactly one minimum-change final S16 candidate.
- R2 preserves CHANGED/UNCHANGED/CONFLICT/PENDING/FAULT distinctly, masks packed
  eye bits out of canonical sample addresses and retains the reduced joint cell as
  the live certificate input for the R3/R4 ownership handoff.
- Vulkan `SigmaFrameInverseTests` passes 5/5, including a two-window duplicate
  target fixture whose bounds, validity and final state are bit-identical under
  reversed source order; `SigmaFrameResourceTests` remains 3/3.
- R3 retains unresolved exact S16 state/cells/provenance under generation-safe
  pending handles, proposes them on later coherent frames before NOVEL, and keeps
  candidate identity invariant across one, two and seven Vulkan binding windows.
- R3 closes evidence-qualified intrinsic S16 edges from immutable gathered endpoint
  snapshots; unobserved adjacency makes no claim, thin unresolved edges defer only
  incident changes and execution-window boundaries do not alter closure.
- Focused Vulkan R1-R3 gates pass 11/11; the sole remaining red inverse fixture is
  the deliberately disconnected R4 atomic publication contract.

## CURRENT — R4 segmented publication and evidence ownership

Consume only R3 ACCEPTED+CHANGED+non-deferred targets. Resolve CURRENT updates in
their original readable segment; allocate promoted extents only for exact NOVEL or
CONTINUATION closure. Scatter into segmented shadow pages, transfer a complete
immutable exact evidence journal before owned-frame release and publish one
variable-length revision manifest/root. UNCHANGED creates no generation. Allocation,
evidence and revision storage reclaim/grow without a session cliff.

### Exact implementation cursor

- DONE: R3 exact edge closure, incident-only deferral, persistent pending handoff,
  cross-frame reuse and 1/2/7-window plus boundary Vulkan parity fixtures.
- CURRENT: replace `SigmaFramePublish.compute` as one complete-file R4 publication
  lowering and record its segmented carrier/evidence/revision ownership in
  `SigmaFrameGraph.cs`; do not reopen R1-R3 shaders.
- First gate: CURRENT from two readable segments updates the correct segment,
  UNCHANGED emits nothing, then a multi-page changed set becomes visible through
  exactly one root flip while its evidence survives frame-slot reuse.

### CURRENT files

```text
Runtime/SigmaPrism/SigmaFrameResources.cs
Runtime/SigmaPrism/SigmaFrameGraph.cs
Runtime/Resources/SigmaPrism/SigmaFramePublish.compute
Runtime/Resources/SigmaPrism/SigmaFrameAbi.hlsl
Runtime/SigmaPrism/SigmaConstraintLedger.cs
Runtime/Resources/SigmaPrism/SigmaConstraintLedgerAbi.hlsl
Tools/sigma/generate_sigma_operators.py
Runtime/SigmaPrism/Generated/SigmaGeneratedFrame.cs
Tests/Editor/SigmaFrameInverseTests.cs
Tests/Editor/SigmaConstraintLedgerTests.cs
```

## NEXT milestones

1. R4 segmented carrier/evidence/revision ownership and atomic publication.
2. R5 production bind matrix/full source gates/Release build.
3. R6 same-commit Quest three-minute acceptance; stop before S4-09.

## Last green / negative evidence

- HEAD entering R0: `d3b83e1c47abfffe33ec759875a8b6545b78435b`.
- Generated, Unity Vulkan 54/54, eight-UAV and Release compile were green before
  device execution.
- R1 focused Vulkan gates: `SigmaFrameInverseTests` 4/4 and
  `SigmaFrameResourceTests` 3/3; `git diff --check` green.
- R2 focused Vulkan gates: `SigmaFrameInverseTests` 5/5 and
  `SigmaFrameResourceTests` 3/3; generated ABI check and `git diff --check` green.
- R3 focused Vulkan gates: 11/11 applicable inverse tests pass, including retained
  cross-frame handle reuse, 1/2/7-window proposal parity and regular/thin closure
  across a physical window boundary; eight-UAV and `git diff --check` pass.
- Device acceptance is NEGATIVE: coherent capture runs, graph reports 320x320,
  14 owned frames/182 MiB initial residency, then segmented-window exception;
  revision/readout/topology stay zero.
- S4-09 remains pending and blocked by S4-08 closure.

## Genuinely unresolved

- None architectural. Implementation must follow the fixed R1-R6 order.
- S4-10 supplies durable flash overflow beyond RAM; R4 must nevertheless reclaim
  and grow without a fixed three-minute/session cliff.
