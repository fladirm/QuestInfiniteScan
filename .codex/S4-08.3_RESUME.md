# S4-08.3 execution checkpoint

This file is the sole routine resume checkpoint for the frozen S4-08.3 run.
After compaction: read this file, run `git status --short` and `git diff --stat`,
open only the CURRENT files/functions below, and continue coding immediately.

## FROZEN INVARIANTS

- `Psi : Sigma_2 -> S16` is the only canonical physical world. No parallel 2D/3D
  geometry, topology, history, texture, object, chart or proximity identity.
- Canonical decisions are checked exact Q16.48 with validity bits and outward
  interval rounding. FP may propose disposable work only.
- GPU owns association, exact inverse, proof, transition validation, mutation,
  topology/readout scheduling and rendering. No CPU readback/scheduler/fallback.
- A canonical page/region transaction may span XR frames, but publication remains
  atomic through one completed revision manifest. Partial Psi is never visible.
- Scheduling quanta are not semantic units: 64-sample proof blocks, 16-sample
  execution microtiles, 8-source scratch and 12-candidate arrays are working
  windows only, never evidence/proof caps.
- Every sealed source and proof candidate is retained losslessly across quanta;
  overflow pauses/spills and never truncates. Canonical coalescing then reverse-order
  redundancy run only after closure and to a fixed point.
- Partitioning, interleaving, token budget and bundle count must not change Psi,
  validity, gaps, proof, transition signature or provenance.
- `SealedSourceBundle` owns immutable GPU payload and provenance. No transaction
  surviving ingress retains a capture or prediction ring lease.
- Candidate transition validation is computed directly from candidate exact S16
  state. Derived `_TransitionCache` never authorizes canonical publication.
- L/R novel classification is exact compatibility, including mixed association;
  prediction contact bits are accelerators, never existence conditions.
- Dormant evidence means unresolved/non-progressing, not rejected. It reactivates
  only when a dependency capable of changing the result changes.
- Chunk/page/block/codec/arena boundaries have zero physical meaning.
- Shader/kernel changes in this run are complete-file replacements, not layered
  hotfix branches. No legacy active path remains after cutover.
- S4-09 stays unopened until S4-08.3 is committed, Release-built, installed,
  archived and handed to the user for device audit.

## DONE — NEVER REOPEN WITHOUT A PROVEN REGRESSION

- Baseline S4-08.2 at `8087a6ef1688af760642bb953eb2f2dd51509610`:
  exact pose/corrected calibration, point-to-plane solver, integer acceptance,
  SE(3), provenance, reusable raw arena, coordinate-major gauge demand, safe
  nonblocking completion tickets and temporary XR carrier preview.
- Generated streaming ABI/resources/operator-cost foundation exists.
- Persistent transaction/source/proof/transition/publication state and indirect
  opcode worklists exist; canonical transaction is separated from dispatch quantum.
- Whole-file streaming passes exist: ingress, source bundle, inverse, proof,
  transition, publication and derived scheduling.
- `SolveAndCommitPage` is coordinate-major (`16 samples x 16 coordinates`), not one
  full S16 sample per thread.
- Source/proof streams are generation-safe; 8/12 storage is treated as a window,
  not a canonical cap. Spill/non-progress never publishes an incomplete proof.
- CB1 ingress, CB2 bounded canonical work and CB3 derived/readout have separate
  nonblocking completion tickets.
- Revision-manifest visibility is wired into forward readout; readers consume only
  a completely published revision.
- CB3 materializes topology/readout only after canonical publication and then
  retires the completed transaction.
- Per-ingress slot owns corrected calibration, pose, raw provenance, metric/depth,
  active/unmatched buffers and LUT lease; capture leases are not transaction state.
- Historical retained-source revalidation is integrated: conditional indirect MRT
  clear/raster uses bundle-owned corrected Q48 calibration and raw payload, rebuilds
  association on GPU, then replays the full sealed source chain.
- Dormant transactions park their complete generation-safe source/proof ownership,
  release scarce live transaction/association/page slots and reactivate only on a
  meaningful dependency change or new compatible independent evidence.
- The active nine streaming compute assets and both historical raster passes pass a
  Vulkan kernel/pass compile contract; the manifest-aware forward-readout fixture is
  green. No retained transaction holds a capture or prediction lease.
- Renderer and inverse teardown retain every owned GPU resource behind the final
  graphics-queue completion ticket; polling faults quarantine ownership. The prior
  async-queue retirement implementation is absent and the active Runtime contains
  no async fence wait/readback/GetData scheduler path.
- Historical revalidation is a generation-sealed manifest snapshot: one stable
  256-lane capture pins exact page generations, raster advances by one page quantum,
  and finalize/cancel releases every pin. Later publications never restart an
  in-flight snapshot; stale dependency epochs schedule the newest current snapshot
  after closure, so intermediate revisions need not be replayed.
- Generated operator costs plus the checked-in execution manifest account for the
  actual revalidation and dormant multi-pass work. Revalidation costs 578 tokens,
  accumulates deterministic closure deficit, and no longer hides a whole-carrier
  raster behind one cheap scheduling token.
- Proof minimization uses persistent exclusive prefix/suffix state, bounded pair
  and removal windows, and one reverse-order candidate decision per quantum.
  A 30-candidate Vulkan fixture closes to the same 16-certificate essential set;
  12 records remain an execution window, never a canonical proof cap.
- Every active compute kernel is at or below the Quest eight-UAV limit. Page
  visibility is an SRV in the work graph because admission only observes its pins.

## CURRENT EXACT TASK

Commit the manually reviewed unconditional proof-reducer replacement on top of
the five-stage inverse, then build/install the Release APK and create the
source-only Git archive from its exact commit.

## CURRENT FILES / FUNCTIONS ONLY

- `.codex/STATE.md`
- `.codex/TASK_DAG.json`
- `.codex/CODE_GRAPH.json`
- `docs/architecture/CODE_GRAPH.md`
- `Runtime/Resources/SigmaPrism/SigmaInverseWorkGraph.compute`
- `Runtime/Resources/SigmaPrism/SigmaStreamDerived.compute`
- `Runtime/Resources/SigmaPrism/SigmaStreamInverse.compute`
- `Runtime/SigmaPrism/SigmaStreamingGraph.cs`
- `Runtime/SigmaPrism/SigmaStreamingResources.cs`
- `Runtime/SigmaPrism/Generated/SigmaGeneratedStreaming.cs`
- `Runtime/Resources/SigmaPrism/SigmaStreamingAbi.hlsl`
- `Tools/sigma/sigma_kernel_execution_manifest.json`
- `Tests/Editor/SigmaStreamingContractTests.cs`
- `Runtime/Resources/SigmaPrism/SigmaStreamProof.compute`
- `Runtime/Resources/SigmaPrism/SigmaStreamPublication.compute`
- `Runtime/Resources/SigmaPrism/SigmaStreamRevalidation.compute`
- `Runtime/Resources/SigmaPrism/SigmaStreamTransition.compute`
- Release build/deploy scripts only after the commit

## NEXT MILESTONES

1. Regenerate code graph, validate controls, one S4-08 commit.
2. Release Android/Vulkan APK from that exact commit, install to Quest, create
   source-only `git archive` ZIP, then stop before S4-09.

## LAST GREEN GATES

- Baseline commit: `8087a6ef1688af760642bb953eb2f2dd51509610`.
- Generated Sigma operator gate: green at baseline.
- Quest Vulkan 8-UAV validation: green at baseline.
- Control-plane/code-graph validation: green at baseline.
- S4-08.3 Vulkan active-kernel/pass contract: green (2026-08-23).
- Manifest-aware forward-readout Vulkan fixture: green (2026-08-23).
- Generation-sealed historical revalidation plus stale-epoch inverse/proof kernels:
  Vulkan active-kernel/pass contract 1/1 green (2026-08-23).
- Generation-sealed snapshot/pin/revision Vulkan execution fixture: 1/1 green
  after complete-file revalidation shader replacement (2026-08-23).
- Lossless proof closure fixture: 16-certificate canonical proof is bit-identical
  for 1/2/7/max windows and three interleavings (2026-08-23).
- Generated operator/cost outputs are current and `git diff --check` is green
  (2026-08-23).
- Bounded GPU proof fixed-point fixture and all four streaming contract fixtures:
  green on Vulkan (2026-08-23).
- Final full EditMode suite: 69/69 green (2026-08-23).
- Quest compute UAV validation: green at limit 8 after the complete-file work-graph
  replacement; streaming compile contract remains 4/4 green (2026-08-23).
- Complete-file Android/Vulkan portability replacements: focused Vulkan streaming
  suite 4/4, Quest UAV limit 8 and `git diff --check` green (2026-08-23).
- Second complete-file Release inventory replacement: inverse, proof, transition,
  revalidation, derived and scheduler focused Vulkan suite 4/4; UAV/diff green
  (2026-08-23).
- Complete-file inverse compiler-lowering replacement: all fixed exact reductions
  preserve their sequential Q48/validity order under bounded `[loop]`; focused
  Vulkan streaming suite 4/4 and Quest UAV limit 8 are green (2026-08-23).
- Full EditMode after shared ABI repair: 64 passed / 1 fixture-binding failure;
  that binding was repaired and its targeted fixture is now green.
- Five-stage inverse replacement explicitly compiles every stage in the Vulkan
  streaming contract (4/4), full EditMode is 69/69, generated cost/ABI outputs are
  current, Quest UAV validation is green at 8 and `git diff --check` is green.
- This exact dirty implementation has not yet passed Release build or device
  validation; never report those as green before running.
- Release `3924c6e` compiled inverse/scheduler but was rejected for one proof
  barrier defect repeated over nine variants. The complete proof reducer now keeps
  all 64 lanes through lane-zero canonical publication, closes each source with a
  uniform sync, updates generated barrier cost and passes focused Vulkan 4/4.
- Release `d006686` was rejected by the same 45 proof diagnostics because two
  entrypoint returns still guarded the four reducer calls. The complete proof file
  was reviewed again; the entrypoint now always executes the same 64-lane barrier
  schedule and masks invalid/spilled work to zero. Focused Vulkan proof/streaming
  passes 4/4 (2026-08-23).

## GENUINELY UNRESOLVED

- Resident spill must remain lossless. Durable unbounded storage belongs to S4-10,
  but S4-08.3 may not silently truncate or publish under resident exhaustion.
- Device runtime behavior of historical revalidation/dormant replay remains unrun;
  only compilation and focused Vulkan fixtures are green so far.
