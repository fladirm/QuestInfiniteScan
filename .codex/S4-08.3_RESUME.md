# S4-08.3D execution checkpoint

Routine resume is strictly: read this file; run `git status --short` and
`git diff --stat`; open only CURRENT files/functions; continue immediately.
Do not reconstruct earlier S4-08 runs or reread broad control/spec history.

## FROZEN INVARIANTS

- `Psi : Sigma_2 -> S16` is the only canonical physical world.
- Canonical decisions are checked exact Q16.48 plus validity/provenance. FP and
  telemetry never authorize work, mutation or publication.
- Scheduling windows, token budgets and interleavings cannot change Psi, proof,
  gaps, transition signature or provenance.
- Sealed source/proof ownership is lossless; no execution-window cap may truncate.
- Transaction publication stays revision-atomic; derived topology/readout is not
  canonical authority.
- GPU owns inverse/proof/transition/publication. C# records fixed work and owns
  resources/fences only; async readback is diagnostic-only.
- No capture/prediction lease may survive ingress into a multi-frame transaction.
- No CPU scheduler/readback control, fallback, second world, heuristic acceptance,
  legacy branch or monolithic page batch.
- Any touched shader is read completely with ABI/bind/dispatch ownership and
  emitted as one coherent complete-file replacement.
- S4-09 remains unopened until clean Release install/archive and device acceptance.

## DONE — NEVER REOPEN WITHOUT A PROVEN REGRESSION

- S4-08.2 exact pose/corrected calibration, rigid SE(3), pose provenance, reusable
  raw arena, graphics-queue completion contract and temporary XR carrier preview.
- Persistent S4-08.3 bundle/transaction/source/proof/transition/publication/dormant
  arenas, generated costs, indirect worklists and manifest visibility.
- Coordinate-major five-stage inverse, streaming proof fixed point, candidate
  transition validation and generation-sealed historical revalidation.
- Eight/twelve records are execution windows; 30-candidate GPU proof closes to the
  same 16-certificate result and 1/2/7/max interleaving parity is green.
- CB1 ingress, CB2 canonical and CB3 derived/readout have nonblocking completion;
  teardown quarantines faulted ownership and never early-recycles.
- Android portability replacements, Quest eight-UAV gate, generated outputs,
  focused Vulkan streaming 4/4 and full EditMode 69/69 were green before device
  forensics.
- Device forensic audit is closed in `s4-083-audit.md`; runtime was not modified by
  that audit.
- S4-08.3C execution/publication closure is committed at `3430929` and its 71/71
  source gates are preserved.
- Device transition/dormancy forensics are closed; do not repeat them.
- Deterministic low-code repair contract is frozen in `.codex/S4-08.3D_PLAN.md`.

## CURRENT EXACT TASK

The throughput diagnostic checkpoint is source-green. Regenerate/validate the code
graph, commit, build the same revision as Quest Android/Vulkan Release and install
it. The next device run must distinguish actual per-kernel GPU cost from scheduler
deficit/owner pressure before any further token-cost tuning. Stop before S4-09.

## CURRENT FILES / FUNCTIONS

- `Runtime/SigmaPrism/SigmaGpuKernelTelemetry.cs`
- `Runtime/SigmaPrism/SigmaDiagnosticTelemetry.cs`
- `Runtime/SigmaPrism/SigmaStreamingGraph.cs`
- production C# compute dispatch sites using the central timing wrapper

## NEXT MILESTONES

1. Code graph/control validation and commit.
2. Release APK build and Quest install.
3. Pull per-kernel GPU timestamps plus scheduler-pressure telemetry.
4. Tune only the proven bottleneck; stop before S4-09.

## LAST GREEN GATES

- Audited device/source revision: `082eda09e16d6dea3e00a00d3c61540ce94effba`.
- Prior source gates: Vulkan streaming 4/4, EditMode 69/69, Quest UAV limit 8,
  generated operator/cost check and `git diff --check` green (2026-08-23).
- Device run proved coherent ingress but empty publication/readout and unset inverse
  ledger/probation properties; it is negative forensic evidence, not acceptance.
- S4-08.3C source implementation is committed. Generated output, `git diff --check`,
  Quest eight-UAV and full Unity Vulkan EditMode 71/71 were green before D-run.
- Device D-run baseline: proof 1024/1024, transitions 131072, annihilator actions
  22020096, accepted transition/publication/readout zero, resident bundles 64/64.
- S4-08.3D source gates: focused Vulkan streaming 6/6, full Vulkan EditMode 71/71,
  generated output, `git diff --check` and Quest eight-UAV all green (2026-08-23).
- Final wiring repair: full Vulkan EditMode 71/71 (including an 8/8 occupied
  association-pool preemption fixture), `git diff --check` and Quest eight-UAV are
  green (2026-08-23).
- Exact bootstrap lift repair uses the centre of the complete accepted Q48 meet,
  derives information mass from its independently supported width and corrects the
  quantized Hadamard residual through state lane zero. The production Vulkan fixture
  proves width-dependent mass, exact cell containment, source-order parity and
  one-eye fail-closed behaviour; full EditMode is 72/72 and the UAV limit remains 8.
- Direct preview replacement: explicit XR tracking-to-world transform, conservative
  transparent surface triangles, wireframe edges and isolated-contact fallback;
  full Vulkan EditMode 72/72, `git diff --check` and Quest UAV limit 8 are green
  (2026-08-24).
- Throughput diagnostic checkpoint: every production compute dispatch is profiled,
  all eight bounded rounds refill existing deficits, async pressure telemetry is
  observation-only; full Vulkan EditMode 73/73, generated output, diff and Quest
  eight-UAV gates are green (2026-08-24). Device timestamps remain unrun.

## GENUINELY UNRESOLVED

- Durable arbitrarily long paging remains S4-10. D-run must nevertheless transfer
  unresolved residue to the existing ledger before reclaim and may never silently
  advance/drop an ingress source that has not become GPU-owned.
- Temporary audit telemetry is allowed only as async observation; it must never
  become a CPU scheduling or canonical decision input.
