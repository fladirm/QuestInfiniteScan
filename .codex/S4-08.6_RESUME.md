# S4‑08.6 sole execution cursor

Canonical authority: `new_spec.md`, `CPQ4-2026-08-25-S16-v8.1`.
Forensic facts: `analyza.md` §§4–8 and §16.
Frozen run: `.codex/S4-08.6_NATIVE_CLOSURE_PLAN.md`.

## Do not reopen

- The `cac9ab0` two-run device evidence, 266-dispatch/61-kernel profile, source P0
  matrix and causal chains are complete.
- The germ-first v8 N0 commit `f24ecc3` is superseded conceptually by N0R before
  any N1 runtime work; do not implement its E22-unary, per-germ shadow, LatentGerm
  or Omega/Xi split.
- Old S4‑08.3/08.4/08.5 plans remain historical only.
- Q16.48, signed-XOR algebra primitives, sparse immutable carrier/codec primitives,
  capture/pose/XR, root-last publication and Release timestamps remain donors only
  where v8.1 permits them.

## Frozen mental model

```text
CANONICAL
    full Psi : intrinsic atlas Sigma_2 -> S16
    + authoritative TOE Merkaba/eigenmode native law

OBSERVE
    two coherent RGB-D whole-field shadows

UNDERSTAND
    each measured footprint defines a union of possible native supports

CLOSE
    observation unions + native law + atlas incidence in one feasible closure

UNRESOLVED
    evidence branch, no germ/chart/extent

PUBLISH
    only resolved or branch-common full-S16 NativeStateDelta

READOUT
    eye/prediction/export/debug are pure whole-field shadows
```

`ABSENT != NATIVE_NULL`. Exact ZD != near-singular != direct order. Minimum-change
never chooses a physical hypothesis.

## Accepted run

N0R is accepted by the documentation/control checkpoint containing this cursor:
spec/audit/plan/control/code-graph validation passed and Runtime/Resources diff is
zero.

## Current run

`N1R — authoritative native descriptor`.

First action:

1. locate/hash the supplied authoritative TOE `K_M` operator/frame/kernel source;
2. freeze relation family arities and every bracket;
3. define manifestation, local contribution and whole-scene reducer plans;
4. prove E22 faithfulness or retain direct S16 dependencies;
5. generate exact/near-ZD, fibre-preservation and modal-refinement plans;
6. build semantic oracle and CPU/HLSL parity before live mutation.

If the artifact is missing or incomplete, mark N1R blocked with that single reason.
Do not infer it from current B/G/F, inverse or topology code.

## Size cursor

Runtime baseline: `cac9ab012f4ce574e5eb9bee88290982fd9c4fe8`.

```text
gross delete vs cac9ab0      >=10000 production LOC
new production              <=4000 LOC
net vs cac9ab0               <=-6100 LOC
net vs d3b83e1               <=-5500 LOC
legacy/fallback              0
```

N3R onward stays net-negative and deletes each replaced branch in the same commit.

## Completion

Only N8R archives/builds/installs and physically accepts the exact same commit.
S4‑09 remains unopened until then.
