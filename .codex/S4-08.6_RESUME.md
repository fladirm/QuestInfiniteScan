# S4‑08.6 sole execution cursor

Canonical authority: `new_spec.md`, `CPQ4-2026-08-25-S16-v8`.
Forensic matrix: `analyza.md`.
Frozen run: `.codex/S4-08.6_NATIVE_CLOSURE_PLAN.md`.

## Do not reopen

- `cac9ab0` device evidence and 61-kernel timing audit are complete.
- v7 right-eye/pending/RGB/mass/edge/gauge/evidence/residency gaps are listed in
  `analyza.md`; do not repeat the forensic audit.
- Old S4‑08.3/08.4/08.5 plans are historical only. Never restore their transaction,
  proposal-kind, optical-edge, pending-window or publication graphs.
- Q16.48, signed-XOR algebra, sparse carrier/codec, pose gauge, root-last primitive,
  capture/XR and Release timestamps are retained primitives.

## Frozen architecture

```text
S16 --E22/Merkaba descriptor--> manifestation/query shadow
sensor evidence --same descriptor reverse pullback--> admissible S16 germ
neighbour germs --same descriptor stitch--> native relation stratum
changed germs + proof --sparse root-last--> Psi revision
eye/prediction/export/debug = pure forward readouts
```

The 22 relations are one overcomplete atlas of one S16 state, never 22 canonical
channels. `CURRENT/PENDING/CONTINUATION/NOVEL` have no v8 physical meaning.

## Accepted run

`N0` is accepted by the v8 documentation/control checkpoint containing this
cursor. Its spec/audit/plan/control/code-graph gates are green and Runtime is
unchanged.

## Current run

`N1 — one generated descriptor, four evaluators`.

First action after N0 commit:

1. identify/freeze the supplied authoritative TOE E22 semantic input;
2. extend the existing operator generator schema only;
3. emit one descriptor consumed by forward, pullback, stitch and oracle;
4. add exact fixtures before any live runtime cutover.

No TOE equation may be guessed. If the exact E22 artifact is absent, mark N1
blocked with that single reason; do not substitute v7 B/G/F or invent edges.

## Size cursor

Baseline current runtime/resource diff vs `d3b83e1`: `+569` net LOC.
Final required vs `cac9ab0`: gross delete `>=10000`, net `<=-6100`.
Final required vs `d3b83e1`: net `<=-5500`.
New production code cap: `<=4000`.

After N3, every run must remain net-negative. Delete a replaced branch in the same
commit; never merely disconnect it.

## Completion

N8 must archive/build/install and physically accept the exact same commit. S4‑09
stays pending until then.
