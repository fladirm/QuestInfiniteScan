# S4‑08.6 sole execution cursor

Canonical authority: `new_spec.md`, `CPQ4-2026-08-25-S16-v8.3`.
Forensic facts: `analyza.md` §§4–8 and §16.
Frozen run: `.codex/S4-08.6_NATIVE_CLOSURE_PLAN.md`.

## Do not reopen

- `cac9ab0` device evidence and 266-dispatch/61-kernel profile are complete.
- Germ-first v8 and v8.1 branch/chart/materialization models are superseded before
  live N1 work.
- Old S4‑08.3/08.4/08.5 plans remain historical only.
- Current B/G/F/RGB/topology formulas are donors/tests, not inferred TOE law.

## Frozen mental model

```text
ONE WORLD
    Psi : Sigma_2 -> S16; physical content is [Psi]_gauge

ONE REPRESENTATION
    exact chi : signed-64 backing -> Sigma_2
    generated exact kappa : intrinsic support/measure/reconstruction
    normalized (chi, kappa, Psi-hat, certificate, directory), one root
    local Riemann/coastline density is representation, not physics

ONE LAW
    M = Compile(A_S16, I_TOE, I_Q)
    one IR with algebra/TOE/query-boundary provenance

QUERIES
    Y_q = M_q[Psi]

SCAN
    exact reverse constraints of the same program;
    pre-hit/first-hit directional mould action, zero behind hit

INFORMATION
    exact locality feasible-set/coupled-factor certificate;
    optional proved directional compression, never confidence physics

DISJUNCTION
    contractor scratch, never physical branch identity

DEFAULT
    ZEmpty backing parity + all-default quiescence;
    supported->ZEmpty remains physical mutation

REFINEMENT
    S16 contraction or exact normalized chi/kappa density of the same Psi

SUPPORT INDEX
    generated conservative bounds; zero false negatives; resident/nonresident

BACKING
    durable logical page/gauge directory + encoded blobs
    bounded decoded GPU residency; capacity is not world size

COMMIT
    resolved/common full-S16 deltas, root last
```

Exact ZD, near-singular, direct order and brackets remain distinct. A transparent
mode is preserved only if the complete accumulated program proves it belongs to an
indistinguishable fibre.

## Current cursor

N4.2R XR execution/lifecycle is physically accepted and frozen. The user activated
the normative N5R A--E durable-backing run from `03d0c7a`. Its coherent dirty
device candidate now contains HEAD-last immutable RootObject/page-map/blob
durability, dirty-only native codec staging, one sparse full-key GPU locator, one
dense ResidentSlotTable, exact async rehydrate/clean eviction, completion/lease
retirement, retained-observation `0x120` continuation/replay, restart and atomic
clear. No N4 algebra, hot dispatch, queue slice or FRONT/BACK rule changed.

Current verified gates after the conservative nonresident-support repair:

```text
Unity Vulkan                         163 / 163 PASS
generator/check                              PASS
Quest hot graph                      14 / 16 PASS
Quest SPIR-V / spirv-val             16 / 16 PASS
native executor pipelines      16 hot + 3 cold PASS
UAV                                      <= 8 PASS
```

The corrected exact-SHA Quest run advanced durable HEAD through root 97 / extent
564050, converted the former `0x120` resident stop into bounded eviction and exact
replay, restarted from 138 durable logical pages into the same bounded 56-pair
resident cache, and continued through root 111 / extent 648545. No KGSL/MMU/device
lost/fence/revision/root fault occurred. Device evidence exposed two execution-only
defects: per-object `Flush(true)`/directory-fsync made dirty commits 1--3.7 seconds,
and the first batching APK spent more than 67 seconds before shell readiness because
startup repeatedly decoded and re-encoded every durable page on the Unity thread.

The active narrow repair preserves the exact N5 object graph and HEAD-last
transaction. One immutable-object batch receives one filesystem durability barrier;
HEAD remains separately flushed, atomically renamed and directory-fsynced last.
One authoritative GPU-stage-vs-CPU-EncodePage parity proof remains. Restart reads and
SHA-256 verifies every complete reachable immutable object, validates its encoded
framing/fingerprints/PageRecord metadata without materializing 4096 S16 samples,
and reuses the resulting sorted inventory/extent for restore.

Physical evidence proved that old summaries could never naturally request a cold
supported page because prediction raster traverses resident readout only. The
current candidate stores one generated-plan-fingerprinted exact Q48 projective hull
per page, rebuilds one disposable incremental AVL support broad phase, treats
missing/legacy/stale/corrupt summaries as unbounded MAY-CONTRIBUTE, and revalidates
every selected full generation against pinned HEAD before using the existing exact
pre-admission rehydrate path. Scanner prediction uses identity pose gauge, and the
durable hull is the exact CPU spelling of the same generated geometry readout
consumed by the raster. The index owns residency selection only; exact GPU
PREDICTION_SUPPORT and every N4 semantic remain unchanged. Full Unity Vulkan passes
163/163; generator/check, <=8 UAV, graph 14/16, Quest SPIR-V/spirv-val 16/16 and
embedded native pipelines 16 hot + 3 cold pass. The exact next action is a committed-
SHA Release build and device measurement of `openMs`, `stageMs`, `commitMs` and total
latency, followed by a fresh-v2 long scan, physically evicted supported-page revisit
with observed `COLD_REHYDRATE`, restart, explicit clear and XR cold-work evidence.
N5R is not accepted until those gates pass.

Fresh-v2 physical evidence now crosses the former boundary cleanly: empty HEAD
424 advanced through root 479 / extent 226373; revision 480 received `0x120`,
legally evicted two clean pages, replayed the same retained transaction and
published root 480 / extent 234308 with fault=0 and unresolved=0. The complete
device log is retained under untracked `evidence/N5R/`.

That run also isolated the remaining XR collapse. A conservative 58-page support
working set correctly exceeds the 56-pair resident cache and must backpressure,
but each retry synchronously resolved every selected page and +X/+Y/+XY halo
through a 32-level immutable Merkle tree from disk on the Unity thread. The active
narrow repair maintains one disposable current-HEAD PageRecord dictionary built
from the already verified startup inventory and advanced only with dirty records
after successful HEAD-last swap. It is bound to the exact RootObject hash, is not
canonical authority, and old pinned roots retain Merkle fallback. Missing keys do
not become ZEmpty. Repeated identical backpressure warnings are throttled and log
selection milliseconds plus exact index lookup count. Unity Vulkan is 163/163;
generator/check, <=8 UAV, graph 14/16 and Quest SPIR-V/spirv-val 16/16 pass. The
exact next action is a committed-SHA Release build and same-view Quest run proving
the valid backpressure no longer stalls XR, followed by cold rehydrate/revisit,
restart/clear and final N5 acceptance.

The first installed support-index candidate also supplied a 448-page restart
measurement: complete verification of 686,398,878 reachable PageBlob bytes restored
root 423 / extent 1,832,063 and the bounded 56-pair cache without fault, but occupied
the Unity initialization thread for `openMs=17643.732`. The current source moves
that unchanged complete HEAD verification into one background Task before module
publication. XR can present the initializing shell; scanner admission and explicit
clear await the same task; carrier/pager publication occurs only after verification.
Unity Vulkan remains 163/163 PASS. Regenerate controls/code graph, amend the local
N5 commit, rebuild and prove `unityThreadBlocked=0` on Quest before the fresh-v2
cold-revisit corpus.

The fresh-v2 evidence also proves that the selected 58-page support set cannot fit
the original 56 current/shadow pairs. The active source candidate adds exactly one
second equal Vulkan binding bank. It does not add a second world or authority:
publication root, durable HEAD/PageRecord map, sparse full-key locator, dense
ResidentSlotTable and runtime completion state remain singular. Native close reads
both banks and writes one selected target bank; persistence closes dirty pages from
both in one HEAD-last transaction and retires superseded physical copies only after
durability. Every individual buffer remains under the 128 MiB Quest binding ceiling
and bank/slot is execution-only. Current gates are pager 13/13, carrier 11/11,
native frame 44/44, full Unity Vulkan 165/165, generator/check, <=8 UAV, graph
14/16, Quest SPIR-V/spirv-val 16/16 and embedded executor 16 hot + 3 cold. The exact
next action is controls/code-graph validation, one exact-SHA Release APK, then
physical restore, same-view scan, former-0x120 continuation, cold revisit, restart
and clear evidence.

The first two-bank Quest restore opened durable HEAD 486 / extent 265451 with 65
resident pairs in `2484.481 ms` off the Unity thread, but revision 487 failed closed
with `PageFault 0x1`. The exact cause was physical target selection: the freest bank
did not own the current partially filled append-tail page. The active source pins
native target and target-capacity eviction to the bank owning that exact durable
generation. It also corrects the independent room-frame defect proven in the same
capture: old carrier bytes were restored under a newly created system
`OVRSpatialAnchor` every launch. A successfully persisted anchor UUID is now loaded
and localized before admission, and sleep/resume gates admission until that same
anchor is tracked again. Neither repair changes canonical bytes, N4 execution,
FRONT/BACK or the durable graph. Pager is 14/14, full Unity Vulkan 167/167,
generator/check, <=8 UAV, graph 14/16 and Quest SPIR-V/spirv-val 16/16 pass. The
exact next action is one committed-SHA Release APK and physical proof that revision
487 advances without `0x1`, followed by same-UUID restart/sleep and the remaining
cold-revisit/clear closure.

The committed `d866db5` APK physically proves that append-tail repair: root 486
advanced through 523 with zero fault/unresolved or GPU/lifecycle fault, and restart
published root 524. The subsequent `accepted=2` observation is a different,
truthful no-free-slot condition: 113 exact conservative support candidates exceed
the singular two-bank cache's 112 resident pairs while the physical depth provider
continues producing frames. The same run proves the legacy world is unsuitable for
an anchor-resume oracle because HEAD 486 predates persisted anchor identity and was
then extended under the first migration anchor. Its former room frame is not
recoverable from page bytes.

The active narrow lifecycle candidate therefore forbids creating a new system
anchor over any nonempty durable graph, waits for five consecutive stable tracked
translation/rotation poses before canonical admission, and hides disposable FRONT
while the anchor epoch is unverified. No N4/N5 canonical or shader path changes.
Unity Vulkan is 169/169; generator/check, <=8 UAV and Quest SPIR-V/spirv-val are
16/16. Next: exact-SHA APK, explicit clear to a new empty HEAD while retaining the
saved UUID, clean scan, sleep/wake and static-revisit evidence. Do not delete or
reinterpret old immutable objects automatically.

Do not reopen N4.1 performance, queue probing, FRONT/BACK ownership, native slice
scheduling or the 5 Hz aperture. Do not open N6R or S4-09.

## Historical pre-N4.2 cursor — superseded, do not resume

Corrective N1R-7 and corrective N2R-7 are accepted checkpoints. N3R remains the
accepted one-footprint live bootstrap. The following older full-frame cursor and
metrics are retained only as history; they are not current instructions.

```text
program                       CPQ4-S16-MERKABA-N1R-7
fingerprint                   948a856d1bfe7a760d0c1e1b1624343e2d07cafed5548295c7aeee1034a4b3a6
IR                            39 opcodes / 61 nodes / 70 operands / 17 expressions
chart assignment              all 4! = 24 abstract-sector bijections
chart quotient                3 D4 orbits x 8 assignments
set expression                FOOTPRINT -> BOUNDARY -> CLOSE
N2 proof submissions          exactly 2, test-only
N2 pair mapping               one 256-thread group per relation
N2 close mapping              24 x 8 lanes in one bounded case group
GPU final semantics           component-local assignment/D4 quotient; no host repair
fixed contraction rounds      8, no while/redispatch/recursion
Unity Vulkan EditMode         80/80
generator/check/UAV/SPIR-V    passed
new stitch uninitialized      0
N2 test/oracle delta          +1605/-1
Runtime/Resources             +0/-0
live native graph             unchanged at 9 dispatches
```

The scanner-level constructor is explicitly owned by `I_Q` + `I_REP`: exact
calibrated finite first-hit boundary contact AND complete native Merkaba relation.
It is not claimed as an upstream TOE theorem. No fixed native-sector-to-square-side
convention survives. The GPU proof evaluates all native sector pairs and all 24
chart candidates, then retains multiple non-gauge orbit classes as unresolved.
Sample boundary sides cannot select one. The bounded N2 case size and chart scratch
width are test fixture properties only; N4 must use compact parallel worklists with
submission count independent of footprint, stitch, component, page and history.

Current N4R clean-restart WIP commits are `05b43f5` (full-frame tile close) and
`a637252` (global close lowering), local above accepted/pushed base `b4399db`.
CUT A--D are executable inside the unchanged nine-dispatch graph. Cached Vulkan
passes 18/18 production frame tests plus the focused cross-tile/disconnected/
inconsistent-cycle fixture. Production diff versus `b4399db` is
`+3338/-264`, net `+3074`; generated activation is net `+221` and handwritten
production net `+2853`. Authority JSON/capsule and generator remain byte-identical.

Exact blocker before CUT E: accepted N1 complete component ordering requires the
full locality payload and full directed native stitch receipt (raw S16 link and
associator factors, normalized intervals/classes, transport, bracket, provenance,
chi/kappa, certificates and level). Runtime HLSL exposes assignment/D4 helpers but
no generated complete component serializer/comparator, while the current six-word
boundary receipt retains only masks/hashes/sign/contact/bracket metadata. The plan
explicitly forbids inventing a hash/tile/index winner. The required next artifact
is one generated GPU-executable complete component canonical serialization/
comparison receipt with CPU/Vulkan parity. N4R is hard-stopped on that primitive;
do not edit CUT E publication/refinement or open N5.

## Accepted runs

N0R v8.3 (`61e9f1a`, `438e7bd`, `df5200f`) replaced spec/audit/plan/control only. It retains every v8.2 one-medium,
ZEmpty, E22, first-hit mould and locality-information correction, and additionally
freezes exact chi/kappa gauge normalization/atomicity, calibrated optical nuisance law,
zero-false-negative nonresident query support, bounded proof minimization, fresh-
world N3 plus generated-ABI ownership, durable N5 pager and separate recovery/
no-change/informative/eye performance gates. Forensic device facts remain
unchanged and Runtime/Resources diff is zero.

The premature acceptance at `b541635` was revoked by source audit. Corrective N1R
is accepted by the current checkpoint. Its sole TOE input is:

```text
Tools/sigma/authority/I_TOE_S16_K16_NATIVE_CLOSURE.md
declared upstream SHA-256:
9d2e3604846305cfe5244a4ef49f169632c60582cf895256fadc36426dc5786f
```

It is the only admitted TOE input. The full upstream monograph and `~/Stažené`
copies are not generator inputs. Capsule §8 now freezes `G=2A^TA=-2A^2`, exact
primitive-normalized link/associator/plaquette factors and their direct-sum closure
with no `epsilon_cl` or independent continuous weight.

Independent inputs are `I_Q_SIGMA_PRISM_V1.json` and
`I_REP_SIGMA_PRISM_V1.json`. The sole self-hashed generator emits one numeric
CPU/HLSL IR with 35 opcodes, 55 nodes, 58 operands, 16 expressions and seven query
entries. Program fingerprint:

```text
c98855216dd16d059ebaf0c33652250b7acac4681b01e0d585ab0ba28de67af3
```

Direct S16 is retained because E22 inventory and shadow-decoupling proof are
absent. Exact ZD, near singular, diffraction kernel and algebra zero remain
distinct. Algebra zero is `ZEmpty`; legacy nonzero `ZNullDyad` is rejected.

Mandatory corrective evidence:

```text
actual IR reverse domain             4913 zero+basis triples
set-valued outputs                   31/31 ambiguous; max preimage 3065
total reverse fixtures               5615
query-support                        512 exhaustive; 0 false negatives
refined/nonresident                  256/128
default backing/query parity         3 x 7
certificate revisit                 10000 -> bounded exact factor
gauge normalization orders           24; recursive/multilevel + 6-field transport
fresh shadow/preimage                127 fixtures; 125 admitted; 2 unresolved
fresh dual lift/boundary relation    125/125; external relation truth inputs 0
fresh exact one-LSB nonkernel        resolved; diffraction-kernel probe unresolved
Unity Vulkan                         102/102; focused N1R 11/11
N2 consumer compile                  14/14; generated action warning absent
production diff vs df5200f           +0/-0
```

Generated plans remain test-only until a consuming run; no live mutation or second
graph was introduced.

The premature N2R acceptance at `869f848` and incomplete correction at `b4d88d6`
were revoked by source audit. Superseding N2R is accepted by the current
checkpoint. Its only implementation files are the
Editor/Vulkan semantic oracle, generated oracle math include, two non-production
compute shaders and focused fixtures. They execute the N1R fingerprinted program
without a runtime binding or canonical mutation path.

That accepted oracle remains the base. The corrective N2 checkpoint now also
proves the N1R-3 fresh-admission expression from coherent observation input.

```text
single-query field entry/reducer matrix                        5 x 15 = 75
coherent EYE_PAIR + distinct EXPORT/DEBUG reducers             passed
complete native relation corpus incl. general Q16.48           130 tuples
refined same-support grouping before first-hit                 passed
full U64 keys 1 / 33 / 2^40+1                                 distinct
relation/fibre truth derived from generated relation+gauge     passed
right-eye joint-direction intersection                         passed
three-channel calibrated two-segment PWL optical law           passed
whole-query support / only-A / ambiguous / common-delta        passed
first-hit / behind NO_CLAIM / directional mould                passed
128 same-support / 96 mixed / 129 cold receipt                 passed
support-index exhaustive worlds                                256
duplicate compatible revisits                                  10000 bounded
execution windows                                               1/2/7 + reversed
Vulkan hot + indirect overflow contractor parity               passed
Unity Vulkan                                                   101/101; N2 14/14
fresh raw-observation admission                                unique/common/order/right-eye passed
fresh tangent CPU/Vulkan matrix                                18 nonzero half-step points, signed rows
behind-hit/no-evidence/ambiguous                               unresolved, no mutation
fresh Vulkan graph                                             3 dispatches, existing Contract/Relation entries
fresh hot overflow                                             >4 explicitly cold, never truncated
corrective N1R-4 consumer Vulkan                               104/104; N2 15/15
```

The fixed proof graph is four query/relation entry points plus three contractor/
overflow entry points. One 256-thread workgroup evaluates one complete relation:
the 16x16 signed-XOR pair plane, bracket phases, two G reductions and all 168
annihilator actions remain inside that group. The 128-thread reducer performs full-
key bitonic grouping, segmented accumulation and a parallel first-hit minimum;
support/relation cardinality changes workgroups/data, never dispatch count. There
is no serial generic interpreter or dispatch per relation, lane, support, PWL
segment or execution window.

`NONE/DEBUG` returns the requested generated relation projection only;
`EXPORT_RELATION_GATED` separates manifestation from descriptor-permitted native
connectivity. `EYE_PAIR` carries one coherent revision/pose-calibration context and
evaluates two distinct retinal query rows in one dispatch. General fractional,
multi-lane and large valid Q16.48 relation factors use an exact signed 256-bit
parallel `G` reduction rather than the rejected integral `[-32,32]` shortcut.
Queries above the bounded 128-contribution hot reducer never truncate: they emit an
explicit reason-coded cold-continuation receipt.

The joint oracle retains every coherent query-direction witness. Branches remain
test-assembly scratch and no branch ABI, query shader, field mutation or
publication symbol exists in production. Generator/check and UAV gates pass under
the unchanged N1 program fingerprint. The generated directional-action compiler
warning is constructionally covered across pre-hit, `NO_CLAIM` and mould roles;
the N2 reducer uninitialized warnings are absent.

## Accepted N3R live cutover

N3R binds the accepted calibrated raw-boundary reverse expression and native
relation program into the only publication-capable scan path. The complete native
close is exactly nine GPU dispatches over a fixed graph; work cardinality changes
workgroups, never submissions:

```text
BuildNativeObservation                         1
ContractNativeQuery                            2
EvaluateNativeRelation                         2
PrepareNativeRevision                          1
PrepareNativePage                              1
ScatterNativeState                             1
CloseAndPublishNativeRevision                  1
native close total                             9
full measured scan submission incl. donors    13
```

The relation workgroup is 256-way: the 16x16 signed-XOR product plane, explicit
bracket phases, two exact G reductions and all 168 annihilator probes are parallel
within one relation group. There is no serial relation interpreter and no dispatch
per relation, support, page, segment, lane, source leaf or execution window.

The generator/frame ABI no longer emits Candidate, Pending, Continuation, Novel
or DirtyEdge records. The old proposal, depth/RGB source-cell, provider x segment,
optical-edge, XYZ qualifier, label propagation and sort/reduce publication entry
points and shaders are deleted and cannot be regenerated.

Cold-start/device repair remains inside the N3 boundary. The 1024 MiB decoded
budget is a ceiling; startup allocates only the initial two-page current/shadow
pair. Cross-frame unresolved evidence is copied into a compact 272-byte exact
in-session record after the GPU fence, and the full coherent capture lease is
released immediately after readback requests are issued. Unresolved outcomes no
longer retain RGB/depth textures or exhaust the eight-slot capture ring.

Quest Release evidence:

```text
graph-ready to first terminal receipt          about 114 ms
fresh all-ZEmpty first published root          revision 9
published state/gauge deltas                   1 / 0
profiled native/full graph dispatches          9 / 13
profiled distinct full entry points            11
unresolved-path GPU checksums                  0.670 ms, 0.729 ms
native-close steady mean                       about 57-59 ms
native-close observed maximum                  92.23 ms
continuous terminal receipts                   revisions 1 through 840
stop/start second interval                     revisions 582 through 840
RingExhausted / fault                          0 / 0
Unity 6000.5.9f1 Vulkan EditMode               65/65
generator --check / UAV <=8 / diff check       pass / pass / pass
Release APK build/install                      passed / passed
```

The GPU checksum is explicitly an unresolved-path lower bound, not an informative
root GPU timing claim. Revision 9 separately proves publication. Later unattached
observations stay unresolved because N3 owns only the proved single-cell
`chi_0/kappa_0` bootstrap; N4 owns durable constraints, extended support and local
representation refinement.

The current temporary preview remains the pre-N6 GeometryG consumer. It is wired
to the publication root/readout-dirty flag, but N3 supplies only one base sample
and its Merkaba tangent lift is not equivalent to the old G projection. A blank or
imperceptible preview is therefore not evidence of a missing root. The generated
pure eye/readout cutover remains N6; N3 adds no projection heuristic or second
geometry authority.

Requested UX adjustment is included without scan/readout mutation: the menu is
anchored above the left controller, left thumbstick toggles only the menu, and the
right controller/index trigger owns pointing/clicking.

## Previous corrective authority/oracle context

Corrective N1R-4 supersedes N1R-3 as the generated authority. It freezes and
executes the one constructive Quest capture boundary:

```text
StereoRigFrameLease + calibrated per-view instrument fields
    -> 8 separately retained depth/optical leaves
    -> calibrated cone-ray signed/permuted Merkaba rows
    -> outward finite-footprint/direct-order/code regions
    -> existing exact fresh reverse/relation/gauge program
```

Program fingerprint is
`89d6d581391978f78eb9fc3bd461a65d36575e69eb01ed0c6b4189c9e076e435`;
capture-boundary fingerprint is
`2b492bf2deba23077ff873275f8672a3949e460a2b1ec2429c199fcd62691ba2`.
Focused CPU/Vulkan parity passes 12/12 and Runtime/Resources remain `+0/-0`.

The N1R-3 N2 proof is superseded. Corrective N2R-4 begins from calibrated raw
instrument boundaries and proves the N1R-4 adapter through FreshAdmission or
Unresolved on CPU and Vulkan. Runtime/Resources remain `+0/-0`; N3 may now be
restored and resume from the recorded hard stop.

The narrow corrective N1R checkpoint now owns the previously missing executable
fresh-admission expression:

```text
coherent left/right reverse shadow cells
    -> exact tangent selector
    -> full-S16 dual-frame lift
    -> forward shadow verification
    -> internally derived (state,ZEmpty,ZEmpty) native relation
    -> one relative chi_0/kappa_0 level-zero support cell
    -> unique/common normalized admission OR UNRESOLVED
```

No proposed state/gauge/relation truth enters its ABI; no pixel, XYZ, candidate or
NOVEL authority exists. The expression preserves complete branch disjunction and
admits only byte-common full-S16/support/relation results modulo the generated
global-translation gauge.

The corrective N2R supplement is accepted. It begins with immutable coherent
left/right instrument boundaries (calibrated room rays, finite footprints,
metric order and raw depth/RGB code intervals), not host-prebuilt query rows, a
proposed S16 state or gauge. The generated adapter executes inside the existing
contractor workgroup. Unique/common,
branch-order, ambiguous, right-eye-only, behind/no-first-hit, missing-evidence and
all-`ZEmpty` bootstrap cases pass CPU↔Vulkan. One workgroup owns one reverse branch;
the fixed graph is `ContractNativeQuery -> EvaluateNativeRelation ->
ContractNativeQuery`, exactly three dispatches using two existing entry points.
No dispatch is added per eye, branch, lane, relation or support. Runtime remains
`+0/-0`. Focused Vulkan passes 15/15 and full EditMode Vulkan passes 104/104;
generator/check, UAV and changed-shader warning gates pass.

N3R consumed this operation without reimplementing it. N4R is stopped at the
full-frame authority gate below and is not accepted; S4-09 remains unopened.

## N4R audit checkpoint — not accepted

The current working tree has the structural N4 representation path and keeps the
live native graph at nine dispatches. Exact four-child arbitrary-parent
`chi/kappa` refinement, representation-aware codec, state/gauge/certificate
root atomicity, static exclusion and durable exact unresolved evidence pass
74/74 Unity Vulkan tests and the Release APK builds/installs.

Quest execution found one hard blocker in the host persistence lowering, not in
GPU closure or readback volume. A completion batch is only 16 x 352 bytes, but
each decoded factor calls `SigmaExactConstraintJournal.Add`, which scans all
historical entries and globally sorts them. `CompareRecords` recreates allocated
canonical byte arrays for both operands on every comparison. `Load()` repeats the
same sort at startup. At more than 2600 retained unresolved factors, consecutive
16-boundaries produced 0.8–0.9 second gaps and native completion max reached
4.432 seconds while GPU dispatch count remained nine.

The forensic tree now contains a bounded-certificate correction for that host
defect plus parallel normalized refinement/certificate transport. It is still not
an accepted N4 result. The bounded full-frame closure audit proves that the frozen
authority stops at one coherent footprint -> one relative level-zero fresh cell.
It does not construct the relative `Sigma_2` incidence/displacement of several
simultaneously unattached fresh supports, nor a canonical independent-component
translation normal form. Exact audit:

```text
.codex/S4-08.6_N4R_PRE_CLOSURE_AUDIT.md
```

Do not accept N4R, open N5R or add another runtime placement helper. The missing
scanner-level relative-support authority is now frozen and corrective N1R-5 has
generated its non-live CPU/HLSL set expression. Corrective N2R-5 is the sole next
gate; only after it passes may N4 consume the generated operation. Pixel, tile,
XYZ, hash, page/sample, allocation and workgroup order remain forbidden placement
authorities.

## Size cursor

Runtime baseline: `cac9ab012f4ce574e5eb9bee88290982fd9c4fe8`.

```text
N2R baseline production LOC      17274
corrective N1 vs a129a85         +0 / -0 / net 0 production
corrective N1 authority/test     +1677 / -136 / net +1541
N2R vs eacf261                    +0 / -0 / net 0 production
N2R test/oracle source            +5003
N2R Unity metadata               +27
N2R correction vs b4d88d6        +1085 / -233 test source
fresh N2 supplement vs 63c042a    +1013 / -2 test source
corrective vs b541635             +0 / -392 / net -392
N1R vs df5200f                    +0 / -0 / net 0
N2R gross/new/net vs cac9ab0      0 / 0 / 0
N2R net vs d3b83e1                +601
N3R Sigma production              +2498 / -10008 / net -7510
N3R all Runtime incl. UX          +2607 / -10110 / net -7503
current Sigma production LOC      9764
current vs cac9ab0                gross 10008 / new 2498 / net -7510
current net vs d3b83e1            -6909

final gross delete vs cac9ab0     >=10000
final new production              <=4000
final net vs cac9ab0              <=-6100
final net vs d3b83e1              <=-5500
final legacy/fallback             0
```

N2R does not claim the final deletion targets. The corrective N1 checkpoint is
production-negative because it removes the rejected 392-line dead runtime
lowering; cumulative N1R+N2R is production-neutral while the authority and oracle
remain non-production generator/test assets.

N3R is accepted as the single publication-capable sensor/native-relation cutover.
N4R remains the active node but is stopped pending the narrow corrective N2R-5
proof of the now-generated multi-fresh relative-support authority. N5R owns
durable backing before final acceptance. Only
N7R archives/builds/installs and physically accepts the identical commit.
S4‑09 remains unopened.
