# S4‑08.6 sole execution cursor

Canonical authority: `new_spec.md`, `CPQ4-2026-08-25-S16-v8.3`.
Forensic facts: `analyza.md` §§4–8 and §16.
Frozen run: `.codex/S4-08.6_NATIVE_CLOSURE_PLAN.md`.

## Do not reopen

- `cac9ab0` device evidence and 266-dispatch/61-kernel profile are complete.
- Germ-first v8 and v8.1 branch/chart/materialization models are superseded before
  live N1 work.
- Old S4‑08.3/08.4/08.5 plans remain historical only.
- Current B/G/F/RGB/topology formulas are donors/tests, not inferred TOE law.

## Frozen mental model

```text
ONE WORLD
    Psi : Sigma_2 -> S16; physical content is [Psi]_gauge

ONE REPRESENTATION
    exact chi : signed-64 backing -> Sigma_2
    generated exact kappa : intrinsic support/measure/reconstruction
    normalized (chi, kappa, Psi-hat, certificate, directory), one root
    local Riemann/coastline density is representation, not physics

ONE LAW
    M = Compile(A_S16, I_TOE, I_Q)
    one IR with algebra/TOE/query-boundary provenance

QUERIES
    Y_q = M_q[Psi]

SCAN
    exact reverse constraints of the same program;
    pre-hit/first-hit directional mould action, zero behind hit

INFORMATION
    exact locality feasible-set/coupled-factor certificate;
    optional proved directional compression, never confidence physics

DISJUNCTION
    contractor scratch, never physical branch identity

DEFAULT
    ZEmpty backing parity + all-default quiescence;
    supported->ZEmpty remains physical mutation

REFINEMENT
    S16 contraction or exact normalized chi/kappa density of the same Psi

SUPPORT INDEX
    generated conservative bounds; zero false negatives; resident/nonresident

BACKING
    durable logical page/gauge directory + encoded blobs
    bounded decoded GPU residency; capacity is not world size

COMMIT
    resolved/common full-S16 deltas, root last
```

Exact ZD, near-singular, direct order and brackets remain distinct. A transparent
mode is preserved only if the complete accumulated program proves it belongs to an
indistinguishable fibre.

## Accepted runs

N0R v8.3 (`61e9f1a`, `438e7bd`, `df5200f`) replaced spec/audit/plan/control only. It retains every v8.2 one-medium,
ZEmpty, E22, first-hit mould and locality-information correction, and additionally
freezes exact chi/kappa gauge normalization/atomicity, calibrated optical nuisance law,
zero-false-negative nonresident query support, bounded proof minimization, fresh-
world N3 plus generated-ABI ownership, durable N5 pager and separate recovery/
no-change/informative/eye performance gates. Forensic device facts remain
unchanged and Runtime/Resources diff is zero.

N1R is accepted by this checkpoint. Its sole TOE input is the authoritative
workspace capsule:

```text
Tools/sigma/authority/I_TOE_S16_K16_NATIVE_CLOSURE.md
declared upstream SHA-256:
9d2e3604846305cfe5244a4ef49f169632c60582cf895256fadc36426dc5786f
```

It is the only admitted TOE input. The full upstream monograph and copies in
`~/Stažené` are not generator inputs. Independent workspace inputs are
`I_Q_SIGMA_PRISM_V1.json` and `I_REP_SIGMA_PRISM_V1.json`. The sole self-hashed
generator emits 19 source/provenance records and CPU/HLSL program fingerprint
`ce356a5913c689908325a2f79bcf2350bc28691e917e3d0d71d7c51417193343`.

N1R retains direct S16 because no E22 inventory or shadow-decoupling proof was
supplied; it emits only the orientation-independent shell-square recurrence and
leaves an unproved native modal admissibility region unresolved. The new
representation selects algebra zero as `ZEmpty`, rejects legacy nonzero
`ZNullDyad`, proves all-default quiescence, exact dyadic prior transport,
permutation-stable base gauge normalization, bounded duplicate certificates,
branch-preserving primitive reverse soundness and conservative support inclusion.

Verification: generator/check/JSON/UAV/diff gates pass; warm Unity 6000.5.9f1
Vulkan EditMode is `85/85`, N1R fixture `8/8`. Production diff is `+392/-0` and
has no live call site. No subsequent replacement run is active.

## Size cursor

Runtime baseline: `cac9ab012f4ce574e5eb9bee88290982fd9c4fe8`.

```text
gross delete vs cac9ab0      >=10000 production LOC
new production              <=4000 LOC
net vs cac9ab0               <=-6100 LOC
net vs d3b83e1               <=-5500 LOC
legacy/fallback              0
```

N3R is the single publication-capable sensor/native-relation cutover, owns fresh
support and removes the legacy ABI from the sole generator. N5R owns durable
backing before final acceptance. Only N7R archives/builds/installs and physically
accepts the identical commit.
S4‑09 remains unopened.
