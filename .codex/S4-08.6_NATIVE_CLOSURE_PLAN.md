# S4‑08.6 native S16/Merkaba closure replacement

**Source baseline:** `cac9ab012f4ce574e5eb9bee88290982fd9c4fe8`
**Canonical baseline:** `CPQ4-2026-08-25-S16-v8`
**Forensic authority:** `analyza.md`
**Routine cursor:** `.codex/S4-08.6_RESUME.md`

This run replaces the remaining classical scanner decomposition with one generated
native relation descriptor. It does not optimize or rename v7 R3/R4. Each cut
lands a complete vertical replacement and deletes its old branch in the same
commit.

## 1. Frozen semantic equation

Forward/readout:

\[
\mathcal S_q
=
\Pi_q\circ\mathcal M\circ E_{22}:S16\rightarrow O_q.
\]

Inverse:

\[
\mathcal A_q=\mathcal S_q^{-1}(O_q).
\]

Intrinsic stitch:

\[
\tau_{ij}=\overline{s_i}s_j,
\qquad
\mathcal N_{ij}=\operatorname{StitchNative22}(s_i,s_j,e_{ij}).
\]

Canonical update:

\[
\Psi_{t+1}
=
\operatorname{RootLastCommit}
\left(
\operatorname{StitchNative22}
\left(
\operatorname{NativeMinChange}
\left(
\mathcal C_{prior}\cap\bigcap_q\mathcal A_q
\right)
\right)
\right).
\]

The 22 relations share one S16 preimage. Forward, pullback, stitch and reference
oracle are generated from one descriptor and exact bracket trees.

## 2. Frozen implementation constraints

- `Psi : Sigma_2 -> S16` remains the only physical world.
- Q16.48, carrier codec, immutable roots and exact evidence remain authoritative.
- No TOE expression is invented in scanner code. The authoritative E22 descriptor
  must be imported, generated and fingerprinted before mutation.
- No persistent `Edge[22]` world; relation values are disposable/certificates.
- No depth/RGB/topology-specific physical solvers after cutover.
- No CURRENT/PENDING/CONTINUATION/NOVEL physical kinds.
- Candidate discovery is complete multi-fibre shadow work; pruning needs proof.
- Same-germ conjunction/reduction remains exact even though the global sort graph
  is deleted.
- First-hit is produced by the query descriptor before native closure.
- Intrinsic stitch dirtiness is endpoint/evidence generation based.
- Latent chart is intrinsic; sensor/image/XYZ/page coordinates are provenance only.
- Only `GermDelta` can mutate canonical state.
- Root exchange is the final visible instruction.
- No legacy/fallback, manager, scheduler, ledger graph or second publication path.

## 3. Baselines and size gates

Before N1 record:

```text
git rev-parse HEAD
git diff --numstat cac9ab0 -- Runtime/SigmaPrism Runtime/Resources/SigmaPrism
git diff --numstat d3b83e1 -- Runtime/SigmaPrism Runtime/Resources/SigmaPrism
```

Final gates:

```text
gross deleted production LOC from cac9ab0     >= 10000
new production LOC                           <= 4000
net production LOC vs cac9ab0                 <= -6100
net production LOC vs d3b83e1                 <= -5500
generated tables and tests                    reported separately
```

After every edit, deleted dead code stays deleted. A replacement cut cannot leave
the old asset disconnected-but-present. Any new subsystem/abstraction outside the
two semantic solves triggers stop-and-simplify.

## 4. Required file direction

Preserve/adapt:

```text
SigmaNumericDomain / SigmaS16 / exact backend gate
generated signed-XOR tables and operator IR
SigmaCarrier / SigmaCarrierCodec / immutable root primitive
capture, calibration, pose-gauge, completion, timing and XR lifecycle
```

Target native production surface:

```text
Generated/SigmaNativeRelationDescriptor.cs
Resources/.../Generated/SigmaNativeRelationDescriptor.hlsl
SigmaNativeClosure.compute
SigmaNativeCommit.compute
SigmaNativeReadout.compute or shader
SigmaNativeGraph.cs
SigmaNativeResources.cs
thin lifecycle/readout/persistence consumers
```

Retire during N3–N7:

```text
SigmaFrameAbi.hlsl
SigmaFrameInverse.compute
SigmaFrameClosure.compute
SigmaFramePublish.compute
old SigmaFrameGraph.cs
old SigmaFrameResources.cs
superseded SigmaInverseMath/SigmaRgbInverseMath live paths
superseded SigmaTopologyMath/SigmaIntrinsicTopology live paths
pending projection/label/link/retention ABI
old global target/page mapping/sorting circuits
global novel bbox and pixel continuation
page halo/live persistent XYZ/mesh authority
```

## 5. Commit/run order

At most one run is `in_progress`. Do not reread prior history after compaction; use
the compact resume cursor and inspect only the files owned by the active run.

### N0 — canonical rebase

Status: accepted by the N0 documentation/control checkpoint.

Files:

```text
new_spec.md
analyza.md
.codex/S4-08.6_NATIVE_CLOSURE_PLAN.md
.codex/S4-08.6_RESUME.md
AGENTS.md working-contract rebase
compact GOAL/STATE/TASK_DAG/DECISIONS/SESSION_TAIL updates
generated code graph
```

Gate:

- v8 baseline appears consistently in active controls;
- no normative section retains v7 sensor-cell/proposal/topology authority;
- Markdown/math and JSON/control validation;
- code graph regenerated;
- runtime diff is zero;
- commit N0 separately; this checkpoint is that commit.

### N1 — one descriptor, four evaluators

Order:

1. freeze the supplied authoritative TOE E22 semantic artifact;
2. extend the existing generator with descriptor/bracket/CSE IR;
3. emit CPU and HLSL descriptor tables;
4. emit `ForwardNative22`, `PullbackNative22`, `StitchNative22`;
5. emit one slow `Native22ReferenceOracle`;
6. add focused exact fixtures;
7. run generated-output, CPU, Vulkan and fingerprint gates;
8. regenerate code graph and commit.

No live runtime mutation changes in N1.

Reject N1 if:

- any of 22 relation equations is guessed/hand-maintained twice;
- reverse propagation changes a bracket tree;
- accepted candidate fails forward verification;
- bounded uncertainty returns false accept/conflict rather than unresolved;
- generated optimization changes one bit.

### N2 — native sensor shadow/pullback oracle

Order:

1. define compact `ShadowRelationPacket` and `GermCandidate` generated ABI;
2. implement `ProjectSensorShadow` with actual L/R projection and first-hit sector;
3. compact every conservatively viable native fibre; no one-winner buffer;
4. implement non-mutating `ConstrainNativeGerms` owner path;
5. prove source independence and hidden-fibre preservation;
6. compare shared forward observables against captured Quest frames;
7. keep canonical writer disabled; commit.

Mandatory fixtures:

```text
right-eye-only information
two or more compatible fibres at one pixel
nearest incompatible + second compatible fibre
source/order/work-window permutations
HIT / PRE_HIT_EXCLUSION / NO_CLAIM / behind-hit
RGB pullback invariant to depth/prior initialization
linear kernel and nonlinear fibre preservation
320x320 legal physical grids
```

### N3 — carrier-pull owner live cutover

First replace, then delete in the same commit.

Implementation:

```text
one compact owner range per complete NativeGermKey/LatentSeedKey
one exact native conjunction over all packets
one deterministic minimum-change state
one forward verification
one GermCandidate
```

Delete:

```text
BuildFrameProposals
BuildDepthSourceCells
BuildRgbSourceCells
EvaluateCandidateMeets provider×segment evaluation
four global source-cell SoA worlds
old provisional candidate states
global target sort machinery replaced by owner-local reduction
```

Gate:

- no multiple writer;
- equal-key reduction bit-identical across partitions/order;
- failure remains fail-closed before carrier;
- complete source journal retained once;
- production LOC net negative vs `cac9ab0`;
- commit.

### N4 — intrinsic native stitch live cutover

Implementation:

```text
changed germ/latent keys
→ unique incident intrinsic stitch keys
→ endpoint/evidence-generation cache
→ StitchNative22 on cache misses only
→ REGULAR / SINGULAR / NO_RELATION / UNRESOLVED
→ incident-only defer masks
```

Delete in same commit:

```text
BuildPendingEdges
GatherEdgeEndpoints
ClosePendingEdges
ApplyPendingEdges
RelaxPendingLabels and fixed 18 passes
XYZ CONTACT qualifier
full optical edge arrays
separate live topology math/controller/ABI
```

Gate:

```text
CURRENT↔CURRENT equivalent changed edge included
no claim from image/XYZ proximity
regular/fold/thin/parallel/no-relation/unknown fixtures
stable singular signature across independent views
cache hit/forced miss parity
cross-page/segment/workgroup parity
same descriptor fingerprint in inverse/stitch
```

Measured Quest acceptance for N4: old `ClosePendingEdges` absent; dirty stitch work
is proportional to cache misses and does not exceed 300 ms on the frozen frame.

### N5 — latent native relation cutover

Implementation:

- `LatentGerm` owns stable native local chart, NativeRegion, evidence refs and stitch
  refs;
- supported and latent fibre candidates use the same pullback;
- exact absorb/refine/promote/abort receipts;
- promoted chart allocation ordered only by revision/provenance/native seed.

Delete in same commit:

```text
pending winner projection/depth/handle buffers
PENDING/NOVEL/CONTINUATION physical branches
pending gauge SoA/labels/links/promoted-hole allocator
global pixel bbox extent
pixel-delta continuation
revision-scaled pending windows
```

Gate:

```text
10 revisits before support -> one latent germ
right/left/grazing candidate coverage
side reveal absorbs into compatible carrier branch
5 mm sheets remain separate
failed promotion changes no Psi
resolution/page/segment changes preserve chart and result
complete unresolved evidence survives frame recycle
no fixed high-water/session cap or latency slope
```

### N6 — sparse commit and evidence ownership

Implementation:

```text
changed GermCandidates
→ direct touched-page key compact
→ prepare immutable shadows
→ scatter one writer/germ
→ attach complete journal/stitch receipts
→ close revision
→ root exchange last
```

Delete in same commit:

```text
23-kernel old publication shader/host contract
second target/page sorting and mapping graph
per-segment lifecycle counters that only serve that graph
old FrameGraph/FrameResources ownership
page-owned evidence duplication
```

Gate:

- CHANGED only writes pages;
- UNCHANGED can update proof without page generation;
- old-or-new atomicity across segments/pages/readers;
- revision/frame-slot wrap/recycle;
- complete journal survives until deterministic certificate/raw handoff;
- proof-window invariance;
- fault/pressure cannot advance root or report commit;
- commit.

### N7 — pure prediction/eye/export readout cutover

Implementation:

- sensor prediction returns complete conservative fibre set;
- eye readout directly produces two disposable retinal maps;
- export contract reads full latest `Psi`, native stitches and appearance evidence.

Delete after parity:

```text
page halo continuity
live persistent XYZ readout cache as scanner authority
live mesh/meshlet reconstruction world
prediction path that returns only one physical identity
```

Gate:

- stereo disparity/order/world lock;
- fold/two-sided/occlusion behaviour;
- cache deletion/rebuild parity;
- segment decomposition invisible;
- no readout shader binds writable canonical resources;
- export is on-demand and never reads eye cache;
- commit.

### N8 — hard deletion and exact Quest closure

Before build:

```text
all LOC gates
rg/asset/code-graph proof of zero retired paths
generated descriptors current
full exact/Vulkan tests
Quest eight-UAV validation
git diff --check
validate_goal_state
manual whole-file review of every changed shader and its host bindings
```

Then:

1. commit exact candidate;
2. regenerate source-only `git archive` from that commit;
3. build Android/Vulkan Release from the same commit;
4. install that APK;
5. run physical corpus and one-shot Release timestamps;
6. collect logs, counters, memory and stereo screenshots/video;
7. mark S4‑08 done only if every gate passes;
8. if controls change acceptance commit, archive/build/install that exact final
   commit again;
9. stop before S4‑09.

Physical gates:

```text
root/revision/changed pages/eye draw all > 0
>=50 admitted revisions or manual stop without cliff/fault
no false latent extents on revisit/90°/grazing views
thin board and bucket inside/outside remain distinct
latent reuse/absorption/promotion counters demonstrate real lifecycle
stitch regular/singular/no-relation evidence is stable
Omega+Xi compute <=1500 ms, wall <=1800 ms on frozen fixture
<=1% 30-revision steady-state drift
eye readout meets target refresh independently
PSS bounded by active residency/evidence
```

## 6. Verification discipline

- During N1–N7 run only focused exact/Vulkan fixtures plus compile/diff/codegen
  gates proportional to the change.
- Do not repeatedly build Android before N8 unless a platform-only compiler issue
  blocks the active cut.
- Do not change a test oracle to match implementation unless the v8 equation and
  independent semantic oracle prove the old fixture wrong; record that decision.
- Device performance failure authorizes only exact lowering/CSE/compaction changes,
  never fewer relations, lower resolution or weakened closure.
- Any canonical failure preserves the preceding root and complete evidence.

## 7. Compaction cursor rule

After context compaction, read the mandatory repository controls once, then open
only `.codex/S4-08.6_RESUME.md` and the active run's owned files. Do not reconstruct
N0…previous history or reread old S4‑08.3/08.4/08.5 plans. Update the resume cursor
at each commit with:

```text
last accepted commit
active run
exact changed files
last real gates
next first edit/test
current gross/net production LOC
```

## 8. Stop rules

Stop and simplify before committing if any cut introduces:

```text
new scheduler/manager/lifecycle graph
second canonical state/readout authority
independently editable 22-relation world
CPU native decision/readback loop
legacy/fallback branch
pixel/XYZ/page/segment identity
proof/evidence cap
positive production LOC after N3
```

S4‑08.6 is complete only after N8 physical acceptance. Source parity alone cannot
close the run.
