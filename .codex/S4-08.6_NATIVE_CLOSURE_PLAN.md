# S4‑08.6 native full-field closure replacement

**Runtime source baseline:** `cac9ab012f4ce574e5eb9bee88290982fd9c4fe8`
**Canonical baseline:** `CPQ4-2026-08-25-S16-v8.1`
**Forensic authority:** `analyza.md`
**Routine cursor:** `.codex/S4-08.6_RESUME.md`

This is a hard replacement run. It does not implement the superseded germ-first v8
draft literally and does not optimize v7 R3/R4. It lowers one field-level native
feasibility equation and deletes each replaced branch in the same commit.

---

## 1. Frozen semantic equation

### 1.1 Canonical field

\[
\boxed{\Psi:\Sigma_2\rightarrow S16}.
\]

`Σ₂` is a sparse intrinsic atlas namespace with chart components/incidence. Sparse
`ABSENT` is not an allocated descriptor-defined `NATIVE_NULL` state.

### 1.2 Authoritative native law

\[
\boxed{
\mathcal D_M=
(\mathcal A_{S16},\mathcal K_M,\mathcal E_M,\mathcal M,
\Pi_q^{loc},\mathfrak R_q,\mathcal Z,\mathcal B,\Delta).
}
\]

The supplied TOE artifact owns actual Merkaba/eigenmode operator/frame/kernel
semantics, relation arities and every bracket. The scanner invents none.

If the artifact supplies `E22`, exclusive factorization is legal only after

\[
E_{22}(s_a)=E_{22}(s_b)\Rightarrow s_a\equiv s_b
\]

is proven on the canonical domain modulo frozen harmless gauge/null equivalence.
Otherwise direct S16/eigenmode dependencies remain.

### 1.3 Whole-scene shadow

Local contribution:

\[
\phi_{q,\xi}[\Psi]
=
\Pi^{loc}_{q,\xi}
(\mathcal M(\mathcal K_M,\mathcal E_M,
\Psi|_{\mathcal N_r(\xi)})).
\]

Physical shadow:

\[
\boxed{
\mathscr S_q[\Psi]
=
\mathfrak R_q
(\{\phi_{q,\xi}[\Psi]\}_{\xi\in\Sigma_2}).
}
\]

`mathfrak R_q` owns overlap, order, first-hit, occlusion, fold/sheet collapse,
native-null manifestation, query-relevant ZD/nonassoc context and finite-footprint
integration.

### 1.4 Observation and support disjunction

\[
Y_t=(O_L,O_R,\Gamma_t),
\qquad
O_e=(D_e,RGB_e,F_e,H_e).
\]

Depth/RGB leaves are independent within each coherent eye shadow.

For each footprint:

\[
\boxed{
\mathcal P_{q,p}(O)
=
\bigcup_{H\in\mathcal H_{q,p}}
\mathfrak A_{q,p,H}(O).
}
\]

The union is retained. A one-winner and mutation of every candidate are both
invalid.

### 1.5 One canonical semantic operation

\[
\boxed{
\mathcal C[\Psi_t,Y_t]
=
\mathcal C_{prior}
\cap
\bigcap_{q,p}
\left(\bigcup_H\mathfrak A_{q,p,H}\right)
\cap
\mathcal C_{Merkaba/eigen}
\cap
\mathcal C_{intrinsic}.
}
\]

\[
\boxed{
\operatorname{NativeCloseCommit}(\Psi_t,Y_t)
=
\operatorname{RootLastCommit}
\left[
\operatorname{NativeSelect}
(\mathcal C[\Psi_t,Y_t],\Psi_t)
\right].
}
\]

One hypothesis may publish when resolved. Multiple hypotheses may publish only an
identical/proven-common full delta. Otherwise canonical mutation is empty and an
`UnresolvedShadowBranch` retains the evidence.

Minimum change acts only inside an already resolved harmless-equivalence fibre. It
never chooses physical support identity.

---

## 2. Frozen implementation constraints

- No semantic `Omega inverse → Xi stitch/commit` split.
- No `StitchNative22` physical authority; native relations are constraints inside
  `NativeCloseCommit`.
- No `LatentGerm`; unexplained evidence begins as `UnresolvedShadowBranch` with no
  chart/identity/extent.
- `BoundNativeBranch` appears only after independent native-relation proof and is
  still noncanonical.
- Immutable `ShadowObservation` contains no candidate/native/chart key.
- Disposable `ShadowHypothesis` carries support alternatives.
- `NativeStateDelta` is the only canonical mutation ABI.
- Exact ZD and calibrated `NEAR_SINGULAR` remain distinct.
- Direct order, ZD and nonassociative context remain distinct.
- Linear hidden-mode preservation uses generated right-lift/direct-sum proof;
  nonlinear closure preserves prior representative on indistinguishable fibres.
- Scalar confidence is diagnostic/readout only.
- Static clear-path exclusion can correct false same-scene state in S4‑08;
  behind-hit is no claim.
- Eye/prediction/export/debug are pure whole-field shadows.
- Whole-field coverage may use a disposable conservative query-support index, but a
  locality is culled only by an exact zero-contribution bound. Relevant nonresident
  support rehydrates/defers; it is never omitted or replaced.
- Pages, segments, dispatches and caches cannot define field physics.
- Changed shaders are reviewed and replaced as complete programs with complete
  ABI/bind/host/test context.

---

## 3. Baselines and size gates

Starting production replacement surface at `cac9ab0`: 11,922 lines across the old
frame inverse/closure/publication shaders, graph/resources/ABI, sensor-specific
math/live APIs, separate topology and affected controller/renderer/telemetry code.

Final gates:

```text
gross production deletion vs cac9ab0  >=10000
new production code                   <=4000
net vs cac9ab0                         <=-6100
net vs d3b83e1                         <=-5500
retired live symbols/assets            0
legacy/fallback paths                  0
```

Count separately:

```text
handwritten production
generated descriptor tables
tests/fixtures
documentation/controls
```

N3R must make handwritten production net-negative. N3R–N7R each delete the
replaced branch in their own commit. No future deletion credit may justify leaving
dead code disconnected.

---

## 4. Required file direction

Preserve where compatible:

```text
SigmaNumericDomain / exact backend gate
generated signed-XOR S16 algebra primitives
SigmaCarrier exact immutable page/codec primitives
root-last publication primitive
capture/sync/calibration/pose infrastructure
GPU fences/indirect/binding validation
Release Vulkan timestamp infrastructure
XR lifecycle/UI/anchors and GLB plumbing
```

Target production surface:

```text
generated NativeLawDescriptor C#/HLSL
SigmaNativeShadow.compute
    ProjectNativeShadow
    ReduceNativeShadow

SigmaNativeClose.compute
    CloseNativeField
    ResolveClosureOverflow

SigmaNativeCommit.compute
    PrepareChangedPages
    ScatterChangedStates
    CloseAndPublishRevision

SigmaNativeGraph.cs
SigmaNativeResources.cs
minimal eye presentation shader where required
```

Delete by N8R:

```text
SigmaFrameInverse.compute
SigmaFrameClosure.compute
SigmaFramePublish.compute
old SigmaFrameGraph.cs / SigmaFrameResources.cs / SigmaFrameAbi.hlsl
live sensor-specific source-cell math/APIs
proposal CURRENT/PENDING/CONTINUATION/NOVEL ABI
candidate identity embedded in evidence
separate topology/stitch controller/math authority
pending/LatentGerm projection/label/link/retention lifecycle
global novel bbox/pixel continuation
superseded global target/page sort-map-scan orchestration
page halo/live persistent XYZ/mesh authority
```

---

## 5. Deterministic commit/run sequence

At most one run is `in_progress`. A run ends only after its code graph, controls,
evidence and separate Git commit are current.

### N0R — ontology rebase

Status: accepted documentation/control checkpoint. Runtime diff is zero. N1R is
the sole next run.

Files:

```text
new_spec.md
analyza.md
.codex/S4-08.6_NATIVE_CLOSURE_PLAN.md
.codex/S4-08.6_RESUME.md
AGENTS.md
compact GOAL/STATE/TASK_DAG/DECISIONS/SESSION_TAIL
generated code graph
```

Required gate:

- baseline is `CPQ4-2026-08-25-S16-v8.1` everywhere active;
- normative text uses field-level shadow and one `NativeCloseCommit`;
- E22 is conditional on faithfulness;
- immutable observations are candidate-free;
- support alternatives are explicit unions;
- `UnresolvedShadowBranch` precedes any chart/identity;
- `ABSENT != NATIVE_NULL`;
- scalar confidence is noncanonical;
- forensic device/timing/kernel/source/causal facts remain intact;
- Markdown/math/links/JSON/control/code graph valid;
- Runtime/Resources diff is zero;
- commit separately.

### N1R — authoritative native descriptor

Status: in progress; sole active run after the N0R checkpoint.

Expected touch:

```text
authoritative TOE source artifact + frozen provenance manifest
Tools/sigma/generate_sigma_operators.py (extend existing generator)
Runtime/SigmaPrism/Generated/SigmaGeneratedNativeLaw.cs
Runtime/Resources/SigmaPrism/SigmaGeneratedNativeLaw.hlsl
focused semantic/CPU/Vulkan native-law tests
```

Do not add a second generator or live graph path.

Order:

1. identify and hash the supplied authoritative TOE artifact;
2. import/freeze `K_M` operator/frame/kernel semantics;
3. generate `E_M` with explicit arity/domain/brackets;
4. generate manifestation and local contribution plans;
5. generate whole-scene query reducers;
6. generate exact ZD and distinct near-singular definitions;
7. generate nonassoc context plans;
8. generate linear right-lifts and nonlinear fibre equivalence/selector plans;
9. generate native mode visibility/transport/refinement capacity;
10. emit semantic reference plus CPU/HLSL plans/fingerprints;
11. run exact gates and commit.

Mandatory fixtures:

- every descriptor expression/arity/bracket is sourced and fingerprinted;
- E22 faithfulness holds or direct S16 dependency remains active;
- alias-pair fixtures cannot lose a direct native direction;
- `ABSENT` and allocated `NATIVE_NULL` are distinct;
- scene-shadow exhaustive reference includes overlaps/order/first-hit;
- exact ZD and one-LSB residual classify differently;
- bracket-swap negative control differs as expected;
- `R L_R=I` and direct-sum preservation for each linear query branch;
- nonlinear indistinguishable-fibre representative is stable;
- mode/refinement law exists and scanner code does not invent it;
- CPU/HLSL exact parity.

No live mutation in N1R. If the authoritative artifact is missing/incomplete,
mark N1R blocked with that exact reason; do not substitute current B/G/F or guessed
edges.

Checkpoint commit: `S4-08.6 N1R freeze authoritative native law`.

### N2R — non-mutating whole-scene shadow oracle

Expected touch:

```text
Runtime/Resources/SigmaPrism/SigmaNativeShadow.compute
minimal non-mutating host recorder/resource bindings
focused scene-shadow CPU/Vulkan fixtures
```

Do not wire canonical mutation or retain a parallel prediction world.

Implement:

```text
ShadowObservationGpu       immutable, candidate-free
ShadowHypothesisGpu        disposable alternatives
ProjectNativeShadow
ReduceNativeShadow
non-mutating common-delta witness only
```

Order:

1. construct two coherent eye observations;
2. project current materialized atlas localities under one sensor query;
3. compact local contributions without winner selection;
4. reduce whole-scene overlap/order/first-hit/occlusion;
5. emit complete hypothesis groups;
6. compare to exhaustive CPU scene oracle;
7. test legal Vulkan decomposition and commit.

Required fixtures:

```text
two supports, only A valid
two supports ambiguous -> no-mutation disposition
two supports imply identical delta -> common-delta witness
fold/two sheets at one sensor pixel
right-eye-only support
left and right observe different physical sheets
finite footprint/order overlap
native-null manifestation
one/two/seven backing windows and reversed legal order
```

No canonical runtime cutover in N2R.

Checkpoint commit: `S4-08.6 N2R prove whole-scene shadow hypotheses`.

### N3R — joint field closure live cutover

Expected touch:

```text
Runtime/Resources/SigmaPrism/SigmaNativeClose.compute
Runtime/Resources/SigmaPrism/SigmaNativeAbi.hlsl
Runtime/SigmaPrism/SigmaNativeGraph.cs
Runtime/SigmaPrism/SigmaNativeResources.cs
SigmaInverseController live recorder/bind cutover
focused joint-closure fixtures
```

Implement one `CloseNativeField` feasibility owner over:

```text
prior field locality
left/right ShadowObservation leaves
hypothesis union
same-locality authoritative native relations
intrinsic atlas incidence constraints
complete evidence obligations
```

The output is only:

```text
resolved/common NativeStateDelta
UNCHANGED certificate strengthening
UNRESOLVED_SHADOW_BRANCH
exact conflict/fault
```

Hard-delete in the same commit:

```text
BuildFrameProposals
BuildDepthSourceCells
BuildRgbSourceCells
EvaluateCandidateMeets
four source-cell worlds
old global target sort/reduce support superseded by complete closure ownership
```

Gates:

- one resolved branch publishes one delta;
- multiple distinct deltas publish none;
- common byte-identical delta publishes once;
- no multiple writer or hypothesis-order effect;
- depth/RGB and left/right source permutation parity;
- hidden mode preservation;
- behind-hit no-claim and static exclusion correction;
- result forward-verifies both whole-scene shadows and native relations;
- no old/new fallback;
- handwritten production net-negative.

Commit N3R separately.

Checkpoint commit: `S4-08.6 N3R cut over joint native field closure`.

### N4R — native relation and overflow cutover

Expected touch is restricted to the generated native-law plans,
`SigmaNativeClose.compute`, its ABI/binds and focused relation/overflow tests. No
new production subsystem/file is expected.

Fold all same-locality authoritative ZD/nonassoc/eigen/intrinsic constraints into
`CloseNativeField` before selection.

Add one compact `ResolveClosureOverflow` only for:

```text
cross-workgroup coupled hypotheses
cross-block intrinsic relation continuation
nonresident support continuation after rehydrate
bounded contractor continuation cursor
```

It re-enters the same closure state and owns no physical subsystem.

Hard-delete in the same commit:

```text
BuildPendingEdges
GatherEdgeEndpoints
ClosePendingEdges
ApplyPendingEdges
RelaxPendingLabels
XYZ CONTACT qualifier
separate topology controller/math authority
full optical edge arrays
any historical StitchNative22 semantic naming/ABI; N1R may not introduce it
```

Gates:

- regular, exact ZD, near-singular, nonassoc-context, fold/crease when defined,
  null/contact when defined, no-relation and unresolved oracle parity;
- changed supported-to-supported intrinsic relation included;
- cache hit/forced-miss parity;
- same-locality vs forced-overflow parity;
- cross-page/segment decomposition parity;
- independent-view stability where the descriptor requires it;
- no associator edge heuristic or ZD depth ordering.

Commit N4R separately.

Checkpoint commit: `S4-08.6 N4R fold native relations into field closure`.

### N5R — unresolved branch and modal refinement cutover

Expected touch is restricted to `SigmaNativeResources`, `SigmaNativeClose`, their
ABI/binds, authoritative refinement generator output and focused branch/refinement
tests. Replace the old pending resources in place; add no branch manager.

Implement:

```text
UnresolvedShadowBranchGpu
BoundNativeBranchGpu
proof-gated branch binding
proof-gated supported atlas materialization
authoritative modal refinement demand/transport
```

Hard-delete:

```text
pending projection winner
pending states/lower/upper/validity SoA
pending labels/links/retention/promoted holes
LatentGerm terminology/ABI
global novel bbox
pixel-delta continuation
observation-derived physical extent/allocation
```

Gates:

- first unexplained observation has no chart/native identity/extent;
- repeated compatible evidence reuses branch ancestry;
- ambiguous supports remain one explicit disjunction;
- binding requires independent native relation proof;
- materialization requires support plus atlas attachment/component proof;
- deterministic order only chooses among proven gauge-equivalent placements;
- right/left/image resolution/page decomposition cannot change identity;
- stale generation cannot bind/materialize/reclaim a newer branch;
- thin/parallel/fold cases preserve required separation/incidence;
- modal-capacity-insufficient fixture refines; sufficient fixture does not;
- refinement forward-verifies all retained observations;
- no revision-count/session-cap scaling.

Commit N5R separately.

Checkpoint commit: `S4-08.6 N5R replace pending identity with unresolved branches`.

### N6R — sparse root-last commit and evidence ownership

Expected touch:

```text
Runtime/Resources/SigmaPrism/SigmaNativeCommit.compute
SigmaNativeGraph / SigmaNativeResources publication and evidence bindings
SigmaCarrier storage primitive only where ABSENT/native-null separation requires it
focused atomicity/evidence/persistence tests
```

Input is only resolved/common `NativeStateDelta`.

Order:

1. stable-reduce duplicate complete intrinsic keys;
2. revalidate full closure and evidence ownership;
3. compact touched logical pages;
4. allocate/clone unpublished shadows;
5. scatter full exact S16 states;
6. attach complete evidence/certificate receipts;
7. close immutable revision;
8. root exchange last.

Hard-delete:

```text
duplicate target/page mapping/sorting/scanning lifecycle
old SigmaFramePublish.compute
old SigmaFrameGraph.cs / SigmaFrameResources.cs / SigmaFrameAbi.hlsl
page-owned or frame-slot-owned durable evidence aliases
```

Gates:

- only CHANGED resolved/common delta scatters;
- UNCHANGED strengthens proof without generation;
- unresolved/conflict/fault/incomplete coverage cannot advance root;
- readers see all-old or all-new across pages/segments;
- frame-slot reuse preserves evidence;
- certificate minimization window/order parity;
- certificate minimization changes no export geometry/appearance;
- `ABSENT` and allocated `NATIVE_NULL` persist/restart distinctly;
- real capacity pressure backpressures/fails closed, never reports commit.

Commit N6R separately.

Checkpoint commit: `S4-08.6 N6R close sparse native publication ownership`.

### N7R — pure readout cutover

Expected touch:

```text
SigmaNativeShadow query variants
minimal direct eye presentation shader
SigmaRenderer pure query bindings
on-demand export reader inputs
focused eye/prediction/export purity tests
```

No live canonical mesh/texture cache is introduced.

Sensor prediction:

```text
ProjectNativeShadow -> ReduceNativeShadow
complete support-hypothesis output
no allocation/identity authority
```

Eye:

```text
ProjectNativeShadow -> ReduceNativeShadow
direct left/right retinal maps
```

Export:

```text
full latest Psi
+ intrinsic atlas incidence
+ authoritative native relation/eigenmode law
-> rich 3D geometry/connectivity/appearance/GLB
```

Certificates supply proof metadata only. If raw optical evidence contains unresolved
physical detail, refine/close `Ψ` before export.

Remove page halo/live persistent XYZ/mesh authority after parity.

Gates:

- correct stereo disparity/order/folds/overlap/native-null behaviour;
- eye cache deletion changes no `Ψ` or export;
- prediction preserves every conservatively viable support alternative;
- backing segment decomposition is invisible;
- debug/export cannot bind writable canonical resources;
- export after refinement/restart reads latest full state;
- proof minimization changes no physical texture/detail.

Commit N7R separately.

Checkpoint commit: `S4-08.6 N7R cut over pure native field readouts`.

### N8R — hard deletion and exact Quest closure

Before build:

1. run gross/new/net LOC gates from §3;
2. grep/code graph prove every retired asset/symbol/callsite absent;
3. generated outputs and descriptor fingerprints current;
4. full semantic oracle/CPU/Vulkan fixtures pass;
5. Vulkan bindings/dispatches legal and fail closed;
6. regenerate code graph and validate controls;
7. commit the accepted source;
8. create source-only archive from that exact commit;
9. build Release APK from that exact commit;
10. install the exact APK and record hashes.

Physical gates:

```text
root/revision/changed pages/eye draw > 0
ambiguous support never mutates all candidates
common-delta ambiguity publishes exactly once
scene-level first-hit handles folds/overlap/two sheets
static false artifact removed by valid clear-path exclusion
behind-hit state unchanged
ABSENT/native-null distinction survives restart
E22 faithfulness/direct-S16 status visible and correct
exact ZD differs from near-singular
nonassoc bracket negative control passes
front/grazing/repeated views refine authoritative native modes
thin board/bucket/fold/two-sided manifestations remain correct
eye cache deletion changes no Psi/export
proof compaction changes no export physical detail
root advances for >=50 admitted observations or user stop
no fault, silent false no-change or capacity cliff
NativeCloseCommit compute <=1500 ms
admission-to-completion wall <=1800 ms
30-revision latency slope <=1%
bounded memory/evidence and no segment-dependent work multiplication
```

Only N8R may mark S4‑08 done. Stop before S4‑09.

Checkpoint commit: `S4-08 accept full-field native close`.

---

## 6. Verification discipline

- About 90% implementation, 5% focused verification, 5% controls/prose after N0R.
- Use semantic oracle fixtures before live cutover.
- Do not change an oracle merely because current GPU output differs.
- Any intentional oracle change names the exact new authoritative TOE artifact or
  corrected formal rule.
- Read every changed shader as a complete program plus includes, ABI, host
  bindings/dispatches and focused tests before editing.
- Run Release/device builds only at the planned physical checkpoint unless an
  Android/Vulkan-only compiler issue requires one earlier.
- Every accepted run regenerates the code graph and commits separately.
- Device claims cite exact source/APK/evidence hashes.

---

## 7. Compaction cursor rule

`.codex/S4-08.6_RESUME.md` is the sole routine cursor.

After compaction:

1. read that cursor;
2. run `git status --short && git diff --stat`;
3. open only the active run files/functions listed there;
4. continue at its exact next action.

Do not reread v7/v8 audits, rebuild the architecture or restart a completed run.
Update the cursor only at a meaningful checkpoint.

---

## 8. Stop rules

Stop the active run rather than broaden it if:

- the authoritative TOE `K_M`/relation/bracket/refinement artifact is absent;
- E22 faithfulness is unproven and code attempts to discard direct S16 dependency;
- a proposal collapses a support union to one winner;
- code mutates every alternative support;
- minimum-change is used across hypotheses;
- first-hit is moved into a local germ projector;
- a new latent/topology/candidate manager appears;
- `ABSENT` is materialized as native null without proof;
- scalar confidence gains mutation authority;
- proof/certificate supplies physical export appearance;
- a replaced branch is merely disconnected rather than deleted;
- handwritten production exceeds the LOC gates;
- a test is weakened to follow the implementation;
- device state cannot be attributed to the exact installed commit.
